# AI-парсер для исправления математических опечаток в FORMYLA

## Проблема

В базе данных олимпиадных задач (adaptive_tasks) обнаружены опечатки в математической нотации:
- `(a+b)2` вместо `(a+b)^2` (квадрат суммы)
- `x2 + y2` вместо `x^2 + y^2` (степени)
- `a1, a2` вместо `a_1, a_2` (индексы)
- `см2` вместо `см^2` (единицы измерения)

## Решение

Создан AI-парсер на базе DeepSeek API, который:
1. Находит задачи с потенциальными ошибками
2. Отправляет их в нейросеть с СТРОГИМ промптом
3. Различает индексы и степени по контексту
4. Сохраняет прогресс после каждого батча

## Файлы

### 1. `find_math_errors.py`
Поиск задач с потенциальными ошибками в базе данных.

**Использование:**
```bash
python find_math_errors.py
```

**Результат:** Показывает 10 примеров задач с опечатками типа `)2`, `a1`, `см2` и т.д.

### 2. `test_ai_fixer_dry_run.py`
**ТЕСТОВЫЙ ПРОГОН** - обрабатывает только 10 задач для демонстрации.

**Использование:**
```bash
python test_ai_fixer_dry_run.py
```

**Что делает:**
- Выбирает 10 задач с потенциальными ошибками
- Отправляет в DeepSeek API
- Показывает ДО и ПОСЛЕ для каждой задачи
- **НЕ СОХРАНЯЕТ** изменения в БД (только демонстрация)

**Проверьте результаты:**
- AI правильно различает `x_1` (индекс) и `x^2` (степень)?
- AI не меняет русский текст?
- AI не меняет смысл задачи?

### 3. `fix_olympiads_ai_final.py`
**ФИНАЛЬНЫЙ СКРИПТ** - обрабатывает ВСЮ базу с сохранением прогресса.

**⚠️ ВАЖНО:** Запускайте ТОЛЬКО после проверки тестового прогона!

**Использование:**
```bash
python fix_olympiads_ai_final.py
```

**Особенности:**
- Обрабатывает задачи батчами по 10 штук
- Задержка 2 секунды между батчами (защита от rate limits)
- Сохраняет прогресс в `olympiads_fix_progress.json` после каждого батча
- Можно прервать (Ctrl+C) и продолжить позже - прогресс сохранится
- Обновляет БД напрямую (делает коммит после каждого батча)

## Промпт для AI

Промпт специально разработан для различения индексов и степеней:

```
ТИПИЧНЫЕ ОШИБКИ:

1. Квадраты после скобок: (a+b)2 → (a+b)^2
2. Степени в уравнениях: x2 + y2 → x^2 + y^2
3. Индексы в последовательностях: a1, a2, a3 → a_1, a_2, a_3
4. Единицы измерения: см2 → см^2

ПРАВИЛА:
- НЕ меняй русский текст
- РАЗЛИЧАЙ индексы и степени по контексту
- Если сомневаешься - НЕ меняй
```

## Пошаговая инструкция

### Шаг 1: Найти примеры ошибок
```bash
python find_math_errors.py
```
Результат: ~145 задач с потенциальными ошибками

### Шаг 2: Тестовый прогон
```bash
python test_ai_fixer_dry_run.py
```

**Проверьте вывод:**
- Смотрите на секции "БЫЛО" и "СТАЛО"
- Убедитесь, что AI правильно исправляет опечатки
- Убедитесь, что AI не портит корректный текст

### Шаг 3: Запуск на всей базе (ТОЛЬКО после одобрения)
```bash
python fix_olympiads_ai_final.py
```

**Мониторинг:**
- Скрипт показывает прогресс в реальном времени
- Каждый батч сохраняется автоматически
- Можно прервать и продолжить позже

### Шаг 4: Проверка результатов
```bash
python find_math_errors.py
```
Должно остаться меньше ошибок (или 0, если AI всё исправил)

## Восстановление после сбоя

Если скрипт прервался:
1. Проверьте файл `olympiads_fix_progress.json` - там сохранен прогресс
2. Прос