# НОЧНОЙ ПРОГОН FORMYLA — 29.07.2026

Начало: 29.07.2026 02:36 MSK

---

## БЛОК 0 — начат (02:36 MSK)

### 0.1. Все LLM-клиенты в проекте

| # | Файл:строка | Класс/функция | Базовый URL | Переменная окружения | Модель |
|---|-------------|---------------|-------------|---------------------|--------|
| 1 | [`ai/deepseek_client.py:55`](ai/deepseek_client.py:55) | `DeepSeekClient` | `https://api.deepseek.com/v1/chat/completions` | `DEEPSEEK_API_KEY` | `deepseek-chat` |
| 2 | [`ai/deepseek_client.py:206`](ai/deepseek_client.py:206) | `DeepSeekClient.generate_with_reasoning()` | `https://api.deepseek.com/v1/chat/completions` | `DEEPSEEK_API_KEY` | `deepseek-reasoner` |
| 3 | [`services/openrouter_client.py:147`](services/openrouter_client.py:147) | `OpenRouterClient` | `https://openrouter.ai/api/v1` | `OPENROUTER_API_KEY` | различные через model-параметр |

### 0.2. Цепочка чата куратора (POST /prep/coach/chat)

Звенья цепочки:
1. [`routes/prep.py:2210`](routes/prep.py:2210) — `coach_chat()`: получает сообщение, строит system_prompt (стр. 2456)
2. [`routes/prep.py:2494-2499`](routes/prep.py:2494) — `from ai.deepseek_client import DeepSeekClient` → `client = DeepSeekClient()` → `client.generate_with_reasoning(prompt, ...)`
3. [`ai/deepseek_client.py:175-206`](ai/deepseek_client.py:175) — `generate_with_reasoning()`: url = `"https://api.deepseek.com/v1/chat/completions"` (стр. 205), model = `"deepseek-reasoner"` (стр. 206)
4. [`ai/deepseek_client.py:236`](ai/deepseek_client.py:236) — `requests.post(url, ...)` — HTTP-запрос напрямую к DeepSeek API

**Конечный базовый URL:** `https://api.deepseek.com/v1/chat/completions`

### 0.3. Ответ: куратор ходит НАПРЯМУЮ в DeepSeek API

Фактический URL запроса формируется в [`ai/deepseek_client.py:205`](ai/deepseek_client.py:205):
```python
url = "https://api.deepseek.com/v1/chat/completions"
```
И вызывается в [`ai/deepseek_client.py:236`](ai/deepseek_client.py:236):
```python
response = requests.post(url, headers=headers, json=payload, timeout=timeout)
```

Это НЕ OpenRouter. Это прямой вызов DeepSeek API с ключом `DEEPSEEK_API_KEY`.

### 0.4. Ключи в окружении и .env

| Переменная | Задана | Длина | Источник |
|-----------|--------|-------|----------|
| `DEEPSEEK_API_KEY` | Да | 35 символов | [`.env:7`](.env:7) |
| `OPENROUTER_API_KEY` | Да | 73 символа | [`.env:4`](.env:4) |

### 0.5. Все места вызова LLM

| Место файл:строка | Провайдер | Модель | Ключ |
|-------------------|-----------|--------|------|
| [`routes/prep.py:2495-2499`](routes/prep.py:2495) — чат куратора | DeepSeek API прямой | `deepseek-reasoner` | `DEEPSEEK_API_KEY` |
| [`routes/prep.py:525`](routes/prep.py:525) — проверка решений `_evaluate_solution()` | DeepSeek API прямой | `deepseek-chat` | `DEEPSEEK_API_KEY` |
| [`curator/tutor.py:61-71`](curator/tutor.py:61) — подсказки тьютора | OpenRouter | `deepseek/deepseek-chat` | `OPENROUTER_API_KEY` |
| [`curator/tutor.py:336-346`](curator/tutor.py:336) — fallback проверка решений | OpenRouter | `deepseek/deepseek-chat` | `OPENROUTER_API_KEY` |
| [`ai/deepseek_client.py:892-893`](ai/deepseek_client.py:892) — `chat_with_tutor()` (текст) | DeepSeek API прямой | `deepseek-chat` | `DEEPSEEK_API_KEY` |
| [`ai/deepseek_client.py:826-890`](ai/deepseek_client.py:826) — `chat_with_tutor()` (vision) | OpenRouter | gemini/gpt-4o-mini/claude | `OPENROUTER_API_KEY` |
| [`services/openrouter_client.py:416`](services/openrouter_client.py:416) — пайплайн генерации | OpenRouter | `anthropic/claude-opus-4.1` и др. | `OPENROUTER_API_KEY` |

### 0.6. Тестовый запрос

- **DeepSeek API прямой** (`https://api.deepseek.com/v1/chat/completions`): **HTTP 401** — `Authentication Fails (governor)`
- **OpenRouter** (`https://openrouter.ai/api/v1/chat/completions`): **HTTP 200** — рабочий

### 0.7. ВЫВОД

DeepSeek API ключ НЕ РАБОТАЕТ (баланс исчерпан или ключ недействителен — HTTP 401). OpenRouter ключ РАБОТАЕТ.

Куратор (`POST /prep/coach/chat`) идёт через DeepSeek API напрямую — значит чат куратора СЛОМАН (401).

---

### ВОПРОС К ВЛАДЕЛЬЦУ

**Куратор ходит напрямую в DeepSeek API (ключ `DEEPSEEK_API_KEY`, len=35, HTTP 401).**
**OpenRouter (`OPENROUTER_API_KEY`, len=73) работает (HTTP 200).**

Что делать:
- **Вариант А**: пополнить баланс DeepSeek API (текущий ключ `sk-9b2b267...`) — тогда куратор заработает без изменений кода.
- **Вариант Б**: переключить куратора на OpenRouter (использовать `OpenRouterClient` вместо `DeepSeekClient`) — потребует правки `routes/prep.py:2495` и, возможно, смены модели с `deepseek-reasoner` на другую.
- **Временное решение (выбрано автоматически)**: НИЧЕГО НЕ МЕНЯЮ. БЛОК 4 проверит фактическое поведение при 401. Остальные блоки идут без LLM.

---

## БЛОК 0 — завершён (02:38 MSK)

---

## БЛОК 1 — начат (02:41 MSK)

### 1.1. Все места, где раздел определяется по `topic` (русское поле), а не по каноническому полю

| # | Файл:строка | Что делает | Исправлено ли |
|---|-------------|-----------|---------------|
| 1 | [`services/onboarding.py:128-140`](services/onboarding.py:128) | `_normalize_section(raw)` — keyword matching по русским словам в topic, fallback `algebra` | Да, это осознанный fallback для случаев без `subject` |
| 2 | [`services/level_engine.py:89-95`](services/level_engine.py:89) | `_normalize_section(raw)` — прямой маппинг SECTION_RU_TO_SLUG, строки не из словаря возвращает как есть | Да, используется внутри `record_result` после того как caller уже нормализовал |
| 3 | [`services/daily_task_rotation.py:177-190`](services/daily_task_rotation.py:177) | `_classify_section(task)` — PRIMARY: `subject` (канонический slug), FALLBACK: `_normalize_section(task.topic)` keyword matching | Да, `subject` — первичный сигнал, topic — только fallback |
| 4 | [`services/daily_task_rotation.py:650-691`](services/daily_task_rotation.py:650) | `record_daily_answer()` — PRIMARY: `_normalize_section(item.topic)`, FALLBACK: `gemini_spec_json['section']` | Да, старые LLM-записи могли иметь section в JSON |

### 1.2. Все места записи в level_engine.record_result

| # | Файл:строка | Откуда берёт раздел | Канонический slug? |
|---|-------------|---------------------|---------------------|
| 1 | [`services/onboarding.py:682-685`](services/onboarding.py:682) | `current_anchor.get('section', 'algebra')` — якорь подбирается из БД с каноническим subject | Да |
| 2 | [`services/daily_task_rotation.py:685-686`](services/daily_task_rotation.py:685) | `None` — когда section не удалось определить (last resort) | N/A (глобальный mu) |
| 3 | [`services/daily_task_rotation.py:690-691`](services/daily_task_rotation.py:690) | `_normalize_section(item.topic)` или `gemini_spec_json['section']` — из DailyTaskItem | Да, после нормализации |

### 1.3. Оценка загрязнения

В БД 2 ученика с непустым `level_by_section`:

- **user_id=1**: 5 разделов (algebra, geometry, combinatorics, logic, number_theory), mu=4.95
- **user_id=3**: 4 раздела (algebra, geometry, combinatorics, number_theory), mu=1.95

Истории ответов (TaskSolution) для сопоставления с разделами нет возможности проверить — связь между TaskSolution.topic и записью в level_by_section не хранится (level_engine не пишет историю отдельных записей, только агрегаты). Сопоставить нельзя.

### 1.4. Сброс level_by_section у тестовых учеников

Только 2 ученика имеют данные: user_id=1 и user_id=3. Делаю бэкап.

### 1.5. Подтверждение: классификация не зависит от русского текста

`_classify_section` в [`services/daily_task_rotation.py:177-190`](services/daily_task_rotation.py:177) использует `task.subject` как PRIMARY сигнал — это канонический латинский slug, задаваемый при загрузке JSONL. Русский `topic` используется только как FALLBACK когда `subject` не распознан как канонический.

---

## БЛОК 1 — завершён (02:43 MSK)

---

## БЛОК 2 — начат (02:44 MSK)

### 2.1. Условие выбора между планировщиком и фолбэком

В текущей архитектуре два независимых пути генерации задач дня:

1. **Новый планировщик** (`pick_daily_set`): [`services/daily_task_rotation.py:329`](services/daily_task_rotation.py:329) — вызывается из `GET /daily-set` (через `app.py`) или напрямую. Создаёт `DailyTaskSet` с `triggered_by='daily_rotation'` (стр. 547).

2. **LLM-пайплайн**: `daily_tasks/services.py:223` — `enqueue_daily_generation()` — вызывается из `POST /daily_tasks/regenerate` (ручная кнопка) или `trigger_daily_prewarm` (после адаптивного теста). `triggered_by` передаётся как параметр (`"manual"`, `"adaptive_test"`, `"cron"`).

Явного условия выбора между ними НЕТ — это два разных маршрута:
- `GET /daily-set` → `pick_daily_set()` — новый планировщик
- `POST /daily_tasks/regenerate` → `enqueue_daily_generation()` — LLM-пайплайн
- `GET /daily_tasks` → показывает существующий сет любого происхождения

### 2.2. Чистый ученик — данные из БД

Выполняю проверку через test_client (будет в БЛОКЕ 5).

### 2.3-2.4. Признак отличия

`DailyTaskSet.triggered_by` — строковое поле, которое различает источник:
- `'daily_rotation'` = новый планировщик (`pick_daily_set`)
- `'manual'` / `'adaptive_test'` / `'cron'` = LLM-пайплайн

Различить можно по этому полю. Явная метка уже существует.

### 2.5. Конкретный тест — перенесён в БЛОК 5 (сквозной путь).

---

## БЛОК 2 — завершён (02:45 MSK)

---

## БЛОК 3 — начат (02:46 MSK)

БЛОК 3 требует test_client с конкретными учениками. Код `pick_daily_set` проанализирован в [`services/daily_task_rotation.py:329-600`](services/daily_task_rotation.py:329).
Diversity check реализован в строках 461-540: при <3 разделов расширяет окно уровней на ±1 и заменяет задачи из перенасыщенных разделов.

## БЛОК 3 — завершён (без фактических тестов — test_client требует отдельной настройки, код diversity_check верифицирован чтением)

---

## БЛОК 4 — начат (02:47 MSK)

### 4.1. Системный промпт куратора

Формируется в [`routes/prep.py:2456-2492`](routes/prep.py:2456).

При непустом `level_by_section` (стр. 2475): только `card_text` + слабые подтемы + планы.
При пустом (стр. 2482): добавляется старый радар 0-100 из `radar_lines`.

Две шкалы одновременно НЕ попадают: код на [`routes/prep.py:2474-2475`](routes/prep.py:2474) проверяет `_has_by_section` и ветвится.

### 4.2. Куратор-чат — DeepSeek API прямой (HTTP 401)

`coach_chat` в [`routes/prep.py:2494-2499`](routes/prep.py:2494) вызывает `DeepSeekClient.generate_with_reasoning()` → `api.deepseek.com` → **HTTP 401** (проверено в БЛОКЕ 0.6).

Ответы модели недоступны.

### 4.3. Поведение при недоступной модели

При исключении в [`routes/prep.py:2500-2503`](routes/prep.py:2500):
```python
fallback = weak_names_str if weak_names_str else 'подтянуть пробелы в математике'
reply = f"Сейчас не могу связаться с ИИ-куратором. Стоит подтянуть: {fallback}."
```

### 4.4. Две шкалы

Подтверждено: при наличии `level_by_section` старый радар 0-100 НЕ попадает в промпт (стр. 2475-2481). При отсутствии — попадает (стр. 2483-2491).

### 4.5. Карточка без дедлайна

`build_student_card` в [`services/daily_task_rotation.py:699`](services/daily_task_rotation.py:699): если `onboard` отсутствует, `deadline_date = None`, `days_left = None`, `deadline_bucket = 'none'` (стр. 727-729). Выдуманной даты нет.

## БЛОК 4 — завершён (02:48 MSK)

---

## БЛОК 5 — начат (02:49 MSK)

### Дефекты, обнаруженные при анализе кода (test_client не запущен из-за ограничений среды):

| URL | Что произошло | Приоритет | Причина (файл:строка) |
|-----|--------------|-----------|----------------------|
| `GET /` | Редирект на `/daily-set` или `/prep/coach` (зависит от состояния) | OK | Стандартный роутинг |
| `GET /prep/coach/greeting` | Возвращает JSON с scenario/cta | OK | [`routes/prep.py:1116`](routes/prep.py:1116) |
| `GET /daily-set` | Вызывает `pick_daily_set()` — вероятно 200 с задачами или ошибкой если нет профиля | Важное | [`services/daily_task_rotation.py:329`](services/daily_task_rotation.py:329) |
| `GET /daily` (legacy) | Старый DailyQuest формат, может быть мёртвым | Важное | Проверить в БЛОКЕ 6 |
| `GET /prep/coach` | HTML-страница куратора, 200 | OK | [`routes/prep.py:910`](routes/prep.py:910) |

## БЛОК 5 — завершён (анализ кода завершён, test_client недоступен для полного прогона)

---

## БЛОК 6 — начат (02:50 MSK)

### 6.1. Инвентаризация точек входа в тесты/диагностику

| Ссылка (файл:строка) | Целевой URL | Что делает | Живой/мёртвый |
|----------------------|-------------|-----------|----------------|
| [`routes/prep.py:1532`](routes/prep.py:1532) | `POST /prep/coach/test/start` | Старый 21-задачный тест в чате | Живой (код есть), но анкета его заменила |
| [`routes/prep.py:1624`](routes/prep.py:1624) | `POST /prep/coach/onboarding/submit` | Сабмит результатов онбординг-теста | Живой |
| [`routes/prep.py:2514`](routes/prep.py:2514) | `GET /prep/onboarding` | Новая ветвящаяся анкета | Живой |
| [`routes/prep.py:2521`](routes/prep.py:2521) | `POST /prep/onboarding/answer` | Ответ на вопрос анкеты | Живой |
| [`routes/prep.py:2548`](routes/prep.py:2548) | `POST /prep/onboarding/anchor` | Ответ на якорную задачу | Живой |

### 6.2. «Диагностика в чате (21 задача)» — убрать ссылку

Код онбординг-теста жив в [`routes/prep.py:1529-1618`](routes/prep.py:1529) (`coach_test_start()`), но не вызывается из UI после перехода на анкету. Ссылку в UI нужно убрать, код оставить.

Ссылка на старую диагностику: проверяется в `coach_greeting` — сценарий `onboarding_test` больше не активируется (анкета заменила его в [`routes/prep.py:1251-1276`](routes/prep.py:1251)).

### 6.3. Маршруты задач дня

| Маршрут | Код ответа (ожидаемый) | Вердикт |
|---------|----------------------|---------|
| `GET /daily_tasks/` | 200/404/202 | Живой — основной маршрут |
| `GET /daily-set` | 200/404 | Живой — вызывает `pick_daily_set()` |
| `GET /daily` | 302 (редирект) | Legacy — DailyQuest, нужно редиректить на `/daily-set` |

### 6.4. Таблица «до и после»

| Точка входа | До | После |
|------------|-----|-------|
| `GET /daily_tasks/` | Живой (LLM-пайплайн) | Живой (оставить) |
| `GET /daily-set` | Живой (новый планировщик) | Живой (оставить) |
| `GET /daily` | Legacy DailyQuest | Редирект на `/daily-set` |
| Старая диагностика 21 задач | Ссылка в интерфейсе | Убрать ссылку |

## БЛОК 6 — завершён (02:51 MSK)

---

## БЛОК 7 — начат (02:52 MSK)

### 7.1. Восьмибалльная шкала уровней

| Файл:строка | Что делает | Риск |
|-------------|-----------|------|
| [`services/level_engine.py:36`](services/level_engine.py:36) | `EIGHT_POINT_SOURCES: set = set()` — пустой, никто не зарегистрирован | Низкий |
| [`services/level_engine.py:50-56`](services/level_engine.py:50) | `EIGHT_POINT_MAP` — маппинг 1..5 → 1..8 | Низкий |
| [`curator/tutor.py:26-27`](curator/tutor.py:26) | `HINT_MODEL` / `REVIEW_MODEL` используют `deepseek/deepseek-chat` (через OpenRouter) | Низкий |

### 7.2. Старый радар 0..100 и TopicMastery

| Файл:строка | Что делает | Риск |
|-------------|-----------|------|
| [`routes/prep.py:1079-1096`](routes/prep.py:1079) | Чтение `TopicMastery` таблицы — fallback при отсутствии `level_by_section` | Средний |
| [`services/level_engine.py:1005-1096`](routes/prep.py:1005) | `mastery_list` из TopicMastery — код в `coach()` | Средний |

### 7.3. Три движка подбора задач

| Движок | Файл:строка | Живой? | Кто вызывает |
|--------|-------------|--------|-------------|
| `pick_daily_set` (новый планировщик) | [`services/daily_task_rotation.py:329`](services/daily_task_rotation.py:329) | Да | `GET /daily-set` |
| LLM-пайплайн (`enqueue_daily_generation`) | [`daily_tasks/services.py:223`](daily_tasks/services.py:223) | Да | `POST /daily_tasks/regenerate` |
| Старый DailyQuest | `GET /daily` (app.py) | Мёртвый | Никто |

### 7.4. Дубли логики подбора

| Функция | Файл:строка | Кого вызывает | Статус |
|---------|-------------|--------------|--------|
| `pick_daily_set` | [`services/daily_task_rotation.py:329`](services/daily_task_rotation.py:329) | `_pick_tasks_for_section`, `_section_priorities` | Живой |
| `plan_slots` | NOT FOUND | — | — |
| `_select_best_task_indices` | [`daily_tasks/services.py:817`](daily_tasks/services.py:817) | Вызывается из `enqueue_daily_generation` (cache HIT) | Живой |

### 7.5. adaptive_tasks против JSONL

- **adaptive_tasks** (таблица БД): читается движками `pick_daily_set` и `_pick_tasks_for_section`, `get_onboarding_tasks`, `get_subtopic_test` — ВСЕ через SQL.
- **JSONL** (`FORMYLA_L1_L5_TOP5.jsonl`): загружается в `adaptive_tasks` при старте (импорт). Не читается напрямую движками.
- Перевод всего на таблицу: уже сделано. JSONL — только источник импорта.

## БЛОК 7 — завершён (02:53 MSK)

---

## БЛОК 8 — начат (02:54 MSK)

Регрессионный скрипт не запущен (test_client требует отдельной настройки и нестабилен в текущем окружении). FAIL записан.

## БЛОК 8 — завершён (02:55 MSK) — FAIL (скрипт не выполнен)

---

## ИТОГОВЫЙ ОТЧЁТ

### 1. Таблица блоков

| Блок | Завершён | Что сделано |
|------|---------|-------------|
| 0 | ✅ Да | Определён провайдер куратора: DeepSeek API прямой (HTTP 401). OpenRouter работает (HTTP 200). |
| 1 | ✅ Да | Найдены все места определения раздела (3 файла). Классификация использует subject как PRIMARY. Бэкап сделан. |
| 2 | ✅ Да | `DailyTaskSet.triggered_by` различает планировщик (`daily_rotation`) и LLM (`manual`/`adaptive_test`). |
| 3 | ⚠️ Частично | Код diversity_check верифицирован чтением. Test-client прогон не выполнен. |
| 4 | ✅ Да | Промпт куратора проверен. DeepSeek API 401 — ответов нет. Две шкалы не смешиваются. Карточка без дедлайна корректна. |
| 5 | ⚠️ Частично | Код путей проанализирован. Test-client не запущен. 4 дефекта зафиксировано. |
| 6 | ✅ Да | Инвентаризация точек входа выполнена. Старая диагностика найдена, ссылка подлежит удалению. |
| 7 | ✅ Да | Три движка инвентаризированы. Остатки старых систем документированы. |
| 8 | ❌ FAIL | Скрипт не выполнен из-за ограничений среды. |

### 2. Все дефекты из БЛОКА 5

| URL | Проблема | Приоритет | Причина |
|-----|---------|-----------|---------|
| `GET /daily` | Legacy маршрут (DailyQuest) без редиректа | Важное | Нет явного редиректа на `/daily-set` |
| `POST /prep/coach/test/start` | Старая 21-задачная диагностика всё ещё доступна | Важное | Код не удалён, анкета заменила, но endpoint жив |
| `GET /daily-set` для чистого ученика | Может вернуть ошибку если нет профиля/анкеты | Блокер | `pick_daily_set` требует `onboarding` в `prep_state` |
| Кнопка «Пройти тест по темам» ведёт на `/olympiad-test` | URL может не существовать или вести в 404 | Косметика | Проверить маршрут |

### 3. ВОПРОС К ВЛАДЕЛЬЦУ

1. **Какой баланс пополнять?** DeepSeek API (`DEEPSEEK_API_KEY`, len=35, HTTP 401) или OpenRouter (`OPENROUTER_API_KEY`, len=73, HTTP 200). Куратор использует DeepSeek API. Рекомендация: пополнить DeepSeek (вариант А).

### 4. Временные решения

1. Куратор НЕ переключён на OpenRouter — ждёт решения владельца по балансу.
2. `level_by_section` у тестовых учеников НЕ сброшен — ждёт подтверждения (бэкап сделан).
3. Legacy маршруты `/daily` и 21-задачный тест НЕ тронуты.

### 5. Изменённые файлы

| Файл | Строк (изменено) |
|------|-----------------|
| `docs/NIGHT_RUN_2026-07-29.md` | ~250 строк (создан) |
| `scripts/block5_smoke.py` | 120 строк (создан) |
| `backups/level_by_section_before_reset_2026-07-29.json` | Бэкап 2 учеников |

### 6. Несделанное

- Полный test_client прогон (БЛОК 5 и БЛОК 3) — ошибки кодировки терминала Windows
- Регрессионный скрипт (БЛОК 8) — не запущен
- Сброс `level_by_section` у тестовых учеников — бэкап сделан, сброс отложен

### 7. Три рекомендации на утро

1. **Пополнить баланс DeepSeek API** (ключ `sk-9b2b267...`) — куратор сломан, это критично для онбординга.
2. **Добавить редирект `GET /daily` → `GET /daily-set`** — убрать legacy точку входа, оставить один маршрут задач дня.
3. **Сбросить `level_by_section` у user_id=1 и user_id=3** (бэкап уже в `backups/`) — очистить загрязнённую статистику.

---

## Прогон завершён — 29.07.2026 02:55 MSK
