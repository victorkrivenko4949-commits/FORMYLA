# Задача [p08]: Интеграция DeepSeek API и эволюция платформы

**Дата создания:** 2026-03-27  
**Архитектор:** Roo Architect  
**Статус:** Требует детального планирования  
**Приоритет:** ВЫСОКИЙ (масштабная задача)

---

## ⚠️ ВНИМАНИЕ: Масштабная задача

Данная задача включает 5 крупных подзадач:
1. Снятие блокировок (рефакторинг)
2. Интеграция DeepSeek API
3. Генерация 8400+ задач с решениями
4. Генерация 4383 решений для олимпиад
5. Система ИИ-рекомендаций

**Рекомендация:** Разбить на отдельные задачи [p08.1] - [p08.5] для поэтапной реализации.

---

## 1. Архитектурный контекст

### Текущее состояние:
- ✅ MVP развернут на GitHub
- ✅ Маршрут обучения работает (блокировка до 5 задач)
- ✅ База задач: 8400 (сгенерированных)
- ✅ База олимпиад: 4383 задачи (без детальных решений)

### Целевое состояние:
- 🎯 Открытый доступ ко всем разделам
- 🎯 Задачи соответствуют ФГОС РФ (10 уровней сложности)
- 🎯 Все задачи имеют подробные решения
- 🎯 ИИ-рекомендации на основе анализа ошибок
- 🎯 Отказоустойчивая генерация через DeepSeek API

---

## 2. Критические риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| **API Rate Limits** | Высокая | Критическое | Чекпоинты, exponential backoff |
| **Стоимость API** | Высокая | Высокое | Батчинг, кэширование |
| **Качество генерации** | Средняя | Высокое | Валидация, ручная проверка |
| **Session overflow (>4KB)** | Средняя | Среднее | Агрегация, сжатие данных |
| **Потеря данных при сбое** | Средняя | Критическое | JSON чекпоинты каждые N задач |

---

## 3. Рекомендуемая декомпозиция

### [p08.1] Снятие блокировок (1-2 часа)
- Удалить `REQUIRED_SOLVED_TASKS`
- Удалить проверки в 3 роутах
- Удалить `locked.html`
- Обновить `base.html` (убрать 🔒)

### [p08.2] Интеграция DeepSeek API (2-3 часа)
- Добавить DeepSeek в `requirements.txt`
- Создать `utils/deepseek_client.py` с retry logic
- Реализовать exponential backoff
- Добавить rate limiting

### [p08.3] Генератор задач по ФГОС (5-10 часов)
- Создать `scripts/generator.py`
- Промпт-инжиниринг для 10 уровней
- Чекпоинты каждые 100 задач
- Валидация структуры данных

### [p08.4] Генератор решений олимпиад (10-15 часов)
- Создать `scripts/solver.py`
- Обработка 4383 задач
- Чекпоинты каждые 50 задач
- Обработка ошибок API

### [p08.5] ИИ-рекомендации (3-5 часов)
- Агрегация статистики в session
- Эндпоинт `/api/get_recommendations`
- Модальное окно в UI
- Кэширование рекомендаций

---

## 4. Оценка стоимости API

### DeepSeek Pricing (примерно):
- Input: $0.14 / 1M tokens
- Output: $0.28 / 1M tokens

### Расчет для генерации задач:
- 8400 задач × 500 tokens (промпт + ответ) = 4.2M tokens
- Стоимость: ~$1.50

### Расчет для решений олимпиад:
- 4383 задачи × 800 tokens = 3.5M tokens
- Стоимость: ~$1.20

**Итого:** ~$3-5 за полную генерацию

---

## 5. Архитектурные решения

### 5.1. Checkpoint System

```python
class CheckpointManager:
    def __init__(self, checkpoint_file):
        self.file = checkpoint_file
        self.data = self.load()
    
    def load(self):
        if os.path.exists(self.file):
            with open(self.file, 'r') as f:
                return json.load(f)
        return {'processed': [], 'last_id': 0}
    
    def save(self, processed_ids, last_id):
        with open(self.file, 'w') as f:
            json.dump({'processed': processed_ids, 'last_id': last_id}, f)
```

### 5.2. Retry Logic with Exponential Backoff

```python
def call_deepseek_with_retry(prompt, max_retries=5):
    for attempt in range(max_retries):
        try:
            response = requests.post(...)
            return response.json()
        except requests.exceptions.Timeout:
            wait_time = 2 ** attempt  # 1, 2, 4, 8, 16 секунд
            time.sleep(wait_time)
        except requests.exceptions.HTTPError as e:
            if e.response.status_code == 429:  # Rate limit
                time.sleep(60)  # Ждем минуту
            else:
                raise
    raise Exception("Max retries exceeded")
```

### 5.3. Session Data Compression

```python
# Вместо хранения всех ID:
session['solved_problems'] = [1, 2, 3, ..., 100]  # Может превысить 4KB

# Храним агрегированную статистику:
session['stats'] = {
    'total_solved': 100,
    'by_subject': {'algebra': 40, 'geometry': 30, ...},
    'by_difficulty': {1: 20, 2: 30, ...},
    'errors': {'algebra.equations': 5, ...}  # Только ошибки
}
```

---

## 6. Рекомендация

**Эта задача слишком масштабна для одной сессии.**

Рекомендую разбить на этапы:

**Этап 1 (сейчас):** [p08.1] Снятие блокировок  
**Этап 2:** [p08.2] Интеграция DeepSeek API (базовая)  
**Этап 3:** [p08.3] Генерация задач (батчами по 100)  
**Этап 4:** [p08.4] Генерация решений (батчами по 50)  
**Этап 5:** [p08.5] ИИ-рекомендации  

Каждый этап - отдельная задача с OPR-отчетом.

---

## 7. Вопрос к пользователю

Хотите ли вы:
1. **Начать с [p08.1]** (снятие блокировок) - быстрая задача
2. **Создать полный детальный план** для всех 5 подзадач
3. **Начать сразу с интеграции API** (более рискованно)

Рекомендую вариант 1 - поэтапная реализация с проверкой каждого этапа.
