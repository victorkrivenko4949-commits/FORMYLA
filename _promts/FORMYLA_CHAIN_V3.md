# FORMYLA — цепочка версии 3, тринадцать блоков

Третий заход. После v2 (20 блоков, 939/37/17/0) проект готов к новым
фичам: роли, группы, пробный период, серии, приоритет ИИ, аудит стоимости,
проверка памяти.

---

## Что было в v2

Прошли полностью: ревизия R0, снятие 22 errors (R1), фикстуры (F0),
проверка Ч5/Ч6 (P1), Ч8 заново (X8), проверка Ч8 (P2), Ч10 заново (X10),
проверка Ч10 (P3), проверка В9/В10 (P4), решение в задачах дня (D1),
правило показа чертежа (C11), хвосты D9 (D2), запас задач дня (D3),
приём чертежей (I1), удаление старой системы (L1), кредиты (K1),
PostgreSQL (V11), проверка новых слоёв (P7), сквозная проверка (P5),
финал (P6).

Эмодзи вычищены: 5539 -> 0. Тесты: 895/49/16/22 -> 939/37/17/0.
Старая система чертежей удалена. Миграции учитываются в schema_migration_log.

Осталось на решение человека: ключ Kimi (401), боевой ЮKassa,
35 старых миграций под PostgreSQL, прямой sqlite3 в трёх местах,
1396 посторонних hex в шаблонах, целочисленная механика среза,
порядок якорей, обработчик отправки ответа в методах.

---

## Что делает v3

Одиннадцать новых задач плюс ревизия и финал. Базовая линия: 939/37/17/0.

---

## Как запустить

1. Положи этот файл в проект по пути `_promts/FORMYLA_CHAIN_V3.md`.
   Путь важен: блоки ищут друг друга по нему.
2. Убедись, что цепочка v2 остановлена.
3. Новый чат ROO, режим Code. Одно сообщение:

```
Открой файл _promts/FORMYLA_CHAIN_V3.md.
Прочитай раздел УСТАВ целиком и выполняй его без исключений.
Затем найди строку <<<БЛОК 01 НАЧАЛО>>> и строку <<<БЛОК 01 КОНЕЦ>>>,
возьми весь текст между ними и выполни его как своё задание.
Ничего из остальных блоков сейчас не читай и не выполняй.
```

Дальше не вмешивайся.

---

## УСТАВ

Читается каждым блоком перед началом работы. Обязателен целиком.

### Кто ты

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Проект локальный:
`C:\Users\Redmi\Desktop\Новая папка (2)`, Windows, PowerShell, VS Code.
Рабочая база `instance/formyla.db`. Всё на русском.

Прод: Render `srv-d73br5ffte5s73euc56g`, formyla.net, Auto-Deploy OFF,
PostgreSQL. Репозиторий `victorkrivenko4949-commits / FORMYLA`, ветка `main`.

### Состояние базы — это норма

`adaptive_tasks` = 0. `users` = 0. `solution_attempts` = 0. Таблицы
`daily_task_sets`, `daily_task_items`, `daily_generation_jobs`, `task_pool`
пусты. `method_tasks` = 860. Таблиц `grade_tasks` и `figures` в базе нет.

Задач для среза ещё нет. Это штатное состояние проекта, а не поломка.
`adaptive_tasks` можно чистить свободно.

Отсюда правило: **любая приёмка, требующая данных, делается на фикстурах**
из `tests/conftest.py`. Блоки обязаны пользоваться ими, а не искать данные
в рабочей базе.

### Первое действие блока

До чтения кода, до любых команд, блок дописывает в конец `_recon/CHAIN_RUN.md`:

```
## БЛОК <NN> <КОД> — СТАРТ
Время старта: <YYYY-MM-DD HH:MM>
```

Это делается всегда, даже если блок потом сорвётся.

Когда работа окончена, блок дописывает полную карточку по формату ниже.
Строка СТАРТ остаётся на месте, её не редактируют и не удаляют.

### Формат карточки

```
## БЛОК <NN> <КОД> — <ГОТОВ|ЧАСТИЧНО|СОРВАЛСЯ>
Время старта: <YYYY-MM-DD HH:MM>
Время финиша: <YYYY-MM-DD HH:MM>
Файлы изменены: <список с числом строк каждого>
Файлы созданы: <список>
Миграции: <список или НЕТ>
Копия базы: <имя файла или НЕТ>
Коммит: <хеш и сообщение или НЕТ, причина>
Тесты базовая линия: passed=<N> failed=<N> skipped=<N> errors=<N>
Тесты после: passed=<N> failed=<N> skipped=<N> errors=<N>
Приёмка: <пройдено> из <всего> (<процент>)
НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО: <N> / <N> / <N>
NOT FOUND: <список или НЕТ>
СМЕЖНОЕ: <дефекты для чужих блоков или НЕТ>
ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА: <список вопросов или НЕТ>
Подробный отчёт: _recon/<КОД>.md
Следующий блок запущен: <НОМЕР или НЕТ, причина>
```

Журнал дописывается в конец. Чужие записи не трогаются.

### Порог приёмки

- 80 процентов и выше — статус ГОТОВ, цепочка идёт дальше.
- Ниже 80 процентов — статус ЧАСТИЧНО, **цепочка останавливается**.

Нельзя ставить пункту «пройдено», если проверка не выполнялась. Для
отсутствия данных есть фикстуры.

### Порог по тестам

Базовая линия: passed=939 failed=37 skipped=17 errors=0 (из v2).

Если после работы блока `failed` или `errors` выросли хотя бы на единицу —
блок обязан разобрать причину и вернуть показатели к базовой линии. Если
не удалось — статус СОРВАЛСЯ, цепочка останавливается.

Удалять тест, помечать `skip` или `xfail` ради зелёного прогона запрещено.

### Коммит

```
git add -A
git commit -m "chain3: <КОД БЛОКА> <краткая суть>"
```

`git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase —
запрещены. Push в `main` не делает ни один блок.

### Доказательство

Отчёт состоит из фактов. Доказательство — HTTP через `app.test_client()`,
дамп базы, фактический фрагмент HTML или вывод pytest.

Шаблон HTTP-проверки:

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('<путь>', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1500])
```

Шаблон дампа базы:

```powershell
python -c "import sqlite3;c=sqlite3.connect('instance/formyla.db');[print(r) for r in c.execute('''<SQL>''')]"
```

### Правило контрольных блоков

Блоки с кодами, начинающимися на П, работают по правилу
**нашёл — почини — докажи**. В карточке обязательна строка
`НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО` с тремя числами.

Если починить нельзя без нарушения запретов — пишется
`ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА` с точной формулировкой.

### Запрещённые слова в отчёте

примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается.

Нет файла или функции — пишется ровно `NOT FOUND`.

### Общие запреты

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase,
  изменение истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF. Push в `main`
  только по отдельной команде человека.
- Формулы уровня не меняются: `mu += 0.22*(sigma+0.3)`,
  `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5], `DEFAULT_MU = 3.0`,
  `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена. 35 якорей, все level = 3.
- Редактирование текстов задач запрещено. Генерация условий задач кодом
  для рабочей базы запрещена. Синтетические тексты допустимы только внутри
  фикстур `tests/` и только с пометкой `[TEST]`.
- Ключи только из `.env`. В отчётах, логах и коммитах — `СКРЫТО`.
- Перед любой миграцией — копия базы `formyla.db.bak_<КОД БЛОКА>_pre`.
- Методы (`olympiad_theory`, 102 записи) не трогать.
- Никаких ЕГЭ и ОГЭ. Продукт только олимпиадный.
- Ни одного эмодзи в коде, шаблонах, отчётах и сообщениях.

### Механика, которую менять нельзя

Анкета: пять якорей в порядке algebra, number_theory, geometry, combinatorics,
logic. Верный ответ +1, частичный 0, неверный −2. Первые семь дней активные:
пять задач адаптивного теста утром. Всё на пятибалльной шкале. 43 темы.
В подтеме пять задач.

### Эталон дизайна

`--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
`--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF`
`--ok:#3ECF8E` `--warn:#E5AC3A` `--bad:#E86A62`

Радиус карточек 14px, кнопок 10px. Шрифт Satoshi и system-ui. Контейнер 1120px.
Тёмно-синий. Посторонних hex-цветов в шаблонах быть не должно.

### Правило повтора

Перед работой блок читает журнал. Если его номер уже отмечен как ГОТОВ —
работу не повторяет, сразу идёт к передаче следующему.

### Когда блок обязан остановиться

- приёмка ниже 80 процентов;
- `failed` или `errors` выросли и вернуть их не удалось;
- копию базы сделать не удалось, а слой требует миграции;
- `import app` падает;
- выполнение требует нарушить любой запрет устава.

### Передача следующему блоку

Блок создаёт следующий диалог через `new_task`. В сообщении новому диалогу:

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
2. Открой `_recon/CHAIN_RUN.md`. Если блок {num} уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку `<<<БЛОК {nxt} НАЧАЛО>>>` и строку `<<<БЛОК {nxt} КОНЕЦ>>>`.
4. Скопируй весь текст между ними и выполни как своё задание.

Если статус ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог не создаётся.

---

## Реестр блоков

| № | Код | Что делает | Правит код | Чинит найденное |
|---|-----|------------|:---:|:---:|
| 01 | R0_V3 | ревизия: базовая линия после v2, точка отката | нет | нет |
| 02 | T2 | имя из почты + лидеры без почтовых адресов | да | нет |
| 03 | T3 | куратор приветствует по имени | да | нет |
| 04 | T1 | уборка олимпиад: Каталог, Предикт, Прогресс | да | нет |
| 05 | T4 | пробный период: 1 день полный доступ, потом paywall | да | нет |
| 06 | T9 | приоритет ИИ для подписчиков + место в очереди | да | нет |
| 07 | T8 | серия: дни подряд на 100%, выходные каждые 3 дня | да | нет |
| 08 | T7 | план на месяц 2+: авто-продление следующих 7 подтем | да | да |
| 09 | T10 | родитель/учитель + группы + дашборд | да | нет |
| 10 | T6 | перенос функций из Прочее на главную | да | нет |
| 11 | T5 | аудит стоимости каждой функции на сайте | да | да |
| 12 | T11 | проверка памяти Render на 300 учеников | нет | нет |
| 13 | P_FINAL_V3 | финал: полный контроль всех 11 слоёв | да | **да** |

Блок 13 следующего диалога не создаёт. Цепочка на нём заканчивается.

Один контрольный блок из тринадцати. Блоки T7 и T11 совмещают исследование
и починку в одном блоке. T2 создаёт хелпер display_name_from_email, T3 и T10
его используют. T8 создаёт модель серий, T10 её использует. T5 идёт после
всех новых функций, чтобы пересчёт стоимости был актуальным. T11 идёт последним
перед финалом, чтобы оценка памяти учитывала все изменения.

---

---

<<<БЛОК 01 НАЧАЛО>>>

# БЛОК 01 — R0_V3 — РЕВИЗИЯ: базовая линия после v2

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 01 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 01 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 01 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Цепочка v2 завершена, базовая линия
  тестов: `passed=939 failed=37 skipped=17 errors=0`. Этот блок снимает точку состояния после v2
  и создаёт точку отката перед цепочкой v3. Код не правится — только чтение, один коммит и запись
  отчёта.
- `adaptive_tasks` = 0, `users` = 0 — норма, приёмка на фикстурах из `tests/conftest.py`. База
  пуста, и это штатное состояние, а не поломка.
- Блок В11 (цепочка v2) должен был создать таблицу `schema_migration_log` для учёта применённых
  миграций — этот блок проверяет её наличие и фиксирует факт: существует ли таблица, какие у неё
  колонки, сколько в ней записей.
- Блок Л1 (цепочка v2) должен был удалить старую систему чертежей — этот блок проверяет, что
  маршрут `/drawing` возвращает 404, а витрина `/figures` возвращает 200. Если `/drawing` отвечает
  200 — старая система не удалена, это находка для отчёта.
- Эмодзи в коде быть не должно: проверка grep по диапазонам Unicode U+1F300–U+1FAFF и
  U+2600–U+27BF в `app.py`, `templates/`, `routes/`, `services/`. Ожидаемый результат — ноль строк.
- Дизайн-токены: `--bg:#070C18 --surface:#0E1830 --surface-2:#121F3C --border:#1C2B4F
  --text:#E6EBF7 --muted:#8C9ABC --accent:#4C7DFF --accent-hi:#6B95FF --ok:#3ECF8E --warn:#E5AC3A
  --bad:#E86A62`. Радиус карточки 14px, кнопки 10px. Шрифт Satoshi + system-ui. Контейнер 1120px.
- Запрещено: `git reset/stash/checkout/rm`, rebase, force push. Нет прод-базы и Render. Нет
  редактирования текстов задач. Ключи из `.env`. Бэкап базы перед миграциями. Ноль эмодзи.
## Что уже есть

Перед любыми действиями ROO подтверждает наличие в корне репозитория: `instance/formyla.db`,
`tests/conftest.py`, `app.py`, `.env`, каталога `_recon/` и файла `_recon/CHAIN_RUN.md`.
Если `_recon/CHAIN_RUN.md` не найден — писать `NOT FOUND` и продолжать остальные пункты, но строка
СТАРТ в этом случае записывается в создаваемый файл `_recon/CHAIN_RUN.md` как первая запись.
Рабочее дерево может содержать незакоммиченные файлы предыдущих слоёв — это нормальное состояние,
не препятствие для коммита точки отката.

## Что сделать

0. Первым действием, до любых команд и чтения кода, дописать в конец `_recon/CHAIN_RUN.md`:

   ```
   ## БЛОК R0_V3 — СТАРТ
   Время старта: <YYYY-MM-DD HH:MM>
   ```

   Если файл `_recon/CHAIN_RUN.md` не существует — создать его и записать строку СТАРТ как первую
   запись. Строка СТАРТ не редактируется и не удаляется до конца блока.

1. Напечатать `git log --oneline -10` — последние 10 коммитов с хешами и сообщениями. Каждую
   строку привести в отчёте как есть, без сокращения.

2. Прогнать `python -m pytest tests/ --tb=line -q` и зафиксировать итоговую строку. Эталон v3:
   `939 passed, 37 failed, 17 skipped, 0 errors`. Если фактические числа расходятся с эталоном —
   привести оба набора рядом, без оценочных слов, просто зафиксировать факт.

3. Дамп таблиц базы: получить список всех таблиц из `sqlite_master`, для каждой — число строк
   `SELECT COUNT(*)`. Привести в отчёте полный список: имя таблицы, число строк. Особо отметить
   `schema_migration_log` — существует или `NOT FOUND`.

4. Проверить, что таблица `schema_migration_log` существует (должна быть создана блоком В11).
   Если существует — привести её схему через `PRAGMA table_info(schema_migration_log)` и число
   записей. Если не существует — `NOT FOUND`.

5. Проверить, что старая система чертежей удалена (должна быть удалена блоком Л1):
   - HTTP-запрос `GET /drawing` через `app.test_client()` с `follow_redirects=True` — должен
     вернуть `404`.
   - HTTP-запрос `GET /figures` через `app.test_client()` с `follow_redirects=True` — должен
     вернуть `200`.
   Оба запроса выполняются на фикстурах из `tests/conftest.py`, не на рабочей базе.

6. Проверить отсутствие эмодзи в коде — выполнить grep по диапазонам Unicode U+1F300–U+1FAFF и
   U+2600–U+27BF в файлах `app.py`, `templates/`, `routes/`, `services/`. Команда в разделе
   ПРИЁМКА. Ожидаемый результат: `EMOJI_COUNT 0`. Если больше нуля — привести полный список
   найденных строк с именами файлов и номерами строк.

7. Получить список отчётов в `_recon/`: перечислить все файлы `*.md` в каталоге `_recon/` с числом
   строк каждого. Если каталог пуст или не существует — `NOT FOUND`.

8. Сделать коммит точки отката: `git add -A`, затем
   `git commit -m "chain2: V3 точка отката перед ревизией"`. Если нет изменений — зафиксировать
   это как факт, а не как ошибку. Второй коммит в этом блоке не делается.

9. Написать отчёт `_recon/R0_V3.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_<КОД>_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно в этом блоке: никакой правки кода в `.py`, `.html`, `.js`, `.css`; никакой правки
  данных в таблицах (только `SELECT`); никаких миграций; из git-команд разрешены только
  `git status`, `git log`, `git add -A` и один коммит
  `git commit -m "chain2: V3 точка отката перед ревизией"` — второй коммит не делается.

## ПРИЁМКА

### 1. Последние 10 коммитов git log

```powershell
git log --oneline -10
```

Ожидаемый вывод: 10 строк вида `<hash> <сообщение>`. Строка отчёта: коммиты приведены целиком.
Если в репозитории меньше 10 коммитов — привести все имеющиеся.

### 2. Полный прогон тестов, базовая линия v3

```powershell
python -m pytest tests/ --tb=line -q
```

Ожидаемый вывод: итоговая строка вида
`<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`. Эталон v3:
`939 passed, 37 failed, 17 skipped, 0 errors`. Строка отчёта:
`БАЗОВАЯ ЛИНИЯ V3: passed=<N> failed=<N> skipped=<N> errors=<N>`.

### 3. Дамп таблиц базы

```python
import sqlite3
conn = sqlite3.connect('instance/formyla.db')
tables = [r[0] for r in conn.execute("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")]
print('TOTAL_TABLES', len(tables))
for t in tables:
    cnt = conn.execute(f"SELECT COUNT(*) FROM '{t}'").fetchone()[0]
    print(f"{t}: {cnt}")
```

Ожидаемый вывод: `TOTAL_TABLES <N>`, затем построчно `<имя таблицы>: <число строк>`. В отчёт
приводится полный список без сокращения на «и другие».

### 4. Таблица schema_migration_log

```python
import sqlite3
conn = sqlite3.connect('instance/formyla.db')
exists = conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='schema_migration_log'").fetchone()
if exists:
    print('EXISTS')
    cols = conn.execute("PRAGMA table_info(schema_migration_log)").fetchall()
    print('COLUMNS', [(c[1], c[2]) for c in cols])
    cnt = conn.execute("SELECT COUNT(*) FROM schema_migration_log").fetchone()[0]
    print('ROWS', cnt)
else:
    print('NOT FOUND')
```

Ожидаемый вывод: при наличии таблицы — `EXISTS`, список колонок вида
`[('id', 'INTEGER'), ('migration_name', 'TEXT'), ...]`, `ROWS <N>`. При отсутствии — `NOT FOUND`.

### 5. Старая система чертежей удалена

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r1 = c.get('/drawing', follow_redirects=True)
print('DRAWING_STATUS', r1.status_code)
r2 = c.get('/figures', follow_redirects=True)
print('FIGURES_STATUS', r2.status_code)
print('FIGURES_LEN', len(r2.data))
```

Ожидаемый вывод: `DRAWING_STATUS 404`, `FIGURES_STATUS 200`, `FIGURES_LEN` больше нуля. Если
`DRAWING_STATUS` равно 200 — старая система не удалена, это находка, описывается в отчёте.

### 6. Отсутствие эмодзи в коде

```powershell
python -c "import subprocess; r=subprocess.run(['grep','-rPn','[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]','app.py','templates/','routes/','services/'],capture_output=True,text=True); print('EMOJI_COUNT', len(r.stdout.splitlines())); print(r.stdout)"
```

Ожидаемый вывод: `EMOJI_COUNT 0`, затем пустая строка. При ненулевом счётчике — список строк
с эмодзи в формате `<файл>:<номер>:<текст>`. Ненулевой счётчик — находка, описывается в отчёте.

### 7. Список отчётов в _recon/

```powershell
Get-ChildItem _recon/ -Filter *.md | ForEach-Object { "$($_.Name) : $((Get-Content $_.FullName).Count)" }
```

Ожидаемый вывод: построчный список `<имя файла> : <число строк>`. Если каталог `_recon/` пуст
или не существует — `NOT FOUND`.

### 8. Коммит точки отката

```powershell
git add -A
git commit -m "chain2: V3 точка отката перед ревизией"
git log --oneline -1
```

Ожидаемый вывод: сообщение коммита с числом изменённых файлов, либо
`nothing to commit, working tree clean`; последняя строка — хеш и сообщение нового (или
последнего существующего) коммита.

## Отчёт

Писать в `_recon/R0_V3.md`. Обязательные разделы:

1. Заголовок и дата.
2. Полный вывод `git log --oneline -10` — все строки как есть.
3. Базовая линия v3: `passed=<N> failed=<N> skipped=<N> errors=<N>`, время прогона. Сравнение с
   эталоном `939/37/17/0` — совпадает или расходится, оба набора чисел рядом.
4. Таблица «таблица базы -> число строк» по всем таблицам из `sqlite_master`. Особо отмечена
   `schema_migration_log`.
5. Статус `schema_migration_log` — `EXISTS` с колонками и числом записей, либо `NOT FOUND`.
6. Статус старой системы чертежей — `DRAWING_STATUS 404`, `FIGURES_STATUS 200` или иные коды.
   При `DRAWING_STATUS 200` — явное указание, что старая система не удалена.
7. Статус эмодзи — `EMOJI_COUNT 0` или список найденных строк.
8. Список отчётов в `_recon/` с числом строк каждого.
9. Результат коммита точки отката — хеш, сообщение, число файлов, либо факт отсутствия изменений.
10. Раздел «Отклонения» — если что-то не реализуемо буквально (файл не найден, команда
    недоступна) — `NOT FOUND` и объяснение, чем заменено.

Запрещённые слова в отчёте (примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается) допустимы только в этом же
перечислении настоящего абзаца. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 02 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 02 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 02 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 01 КОНЕЦ>>>

---

<<<БЛОК 02 НАЧАЛО>>>

# БЛОК 02 — T2 — ИМЯ ИЗ ПОЧТЫ + ЛИДЕРЫ БЕЗ ПОЧТ

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 02 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 02 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 02 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. `adaptive_tasks` = 0, `users` = 0 —
  норма, приёмка на фикстурах из `tests/conftest.py`. Базовая линия тестов v3 из `_recon/R0_V3.md`:
  `passed=939 failed=37 skipped=17 errors=0`. После правок этого блока `failed` и `errors` не
  должны вырасти.
- Задача: убрать почтовые адреса из публичного отображения в таблице лидеров. В БД почта остаётся,
  в UI не показывается. Вместо `email` выводится `display_name` — имя, извлечённое из почты.
- Создаётся единая функция-хелпер `display_name_from_email(email)` в `services/user_helpers.py`
  (если модуль существует — функция добавляется в него; если не существует — модуль создаётся).
  Функция детерминированная и протестированная.
- Логика извлечения имени: берётся часть до `@`, убираются цифры, производится транслитерация
  en->ru по таблице. Примеры: `victorkrvvk` -> Виктор, `anna1990` -> Анна, `k.petrov` -> Пётр
  (по второй части до точки). Если имя извлечь нельзя (пустая строка, только цифры, только
  символы) — возвращается «Игрок» + первые 3 символа почты (до `@`), в верхнем регистре.
- Транслитерация en->ru: строится по словарю известных имён и фонетической таблице. Словарь: victor
  -> Виктор, anna -> Анна, petr -> Пётр, ivan -> Иван, elena -> Елена, dmitry -> Дмитрий,
  sergey -> Сергей, olga -> Ольга, maxim -> Максим, alexey -> Алексей, maria -> Мария, natalia ->
  Наталья, ekaterina -> Екатерина, andrey -> Андрей, nadezhda -> Надежда. Таблица транслитерации
  покрывает остальные случаи посимвольно (a->а, b->б, v->в, g->г, d->д, e->е, zh->ж, z->з,
  i->и, k->к, l->л, m->м, n->н, o->о, p->п, r->р, s->с, t->т, u->у, f->ф, h->х, c->ц, ch->ч,
  sh->ш, y->й, ya->я, yu->ю, yo->ё, e->э, ju->ю, ja->я).
- Имя выводится с заглавной буквы, остальные строчные. Пример: `VICTORKRVVK` -> `Виктор`.
- Применить в шаблонах лидеров: вместо `{{ user.email }}` (или аналог) выводить
  `{{ display_name_from_email(user.email) }}` или заранее вычисленное имя, переданное в контекст
  шаблона. Найти все шаблоны, отображающие лидеров, через grep по `leader`, `rating`, `top`,
  `board`, `score` в `templates/`.
- Дизайн-токены: `--bg:#070C18 --surface:#0E1830 --surface-2:#121F3C --border:#1C2B4F
  --text:#E6EBF7 --muted:#8C9ABC --accent:#4C7DFF --accent-hi:#6B95FF --ok:#3ECF8E --warn:#E5AC3A
  --bad:#E86A62`. Радиус карточки 14px, кнопки 10px. Шрифт Satoshi + system-ui. Контейнер 1120px.
- Запрещено: `git reset/stash/checkout/rm`, rebase, force push. Нет прод-базы и Render. Нет
  редактирования текстов задач. Ключи из `.env`. Бэкап базы перед миграциями. Ноль эмодзи.

## Что уже есть

Перед правками ROO фиксирует в отчёте:

1. Существование модуля `services/user_helpers.py` — если файл есть, привести число строк и
   список функций. Если файла нет — `NOT FOUND`, модуль создаётся этим блоком.
2. Все шаблоны, отображающие лидеров — выполнить grep по `leader`, `rating`, `top`, `board`,
   `score`, `email` в `templates/` (включая подкаталоги). Построчно выписать найденные файлы и
   номера строк.
3. Маршрут(ы), отдающие страницу лидеров — grep по `leader`, `rating`, `top`, `board` в `routes/`
   и `app.py`. Привести путь функции, номер строки, URL маршрута.
4. Все места в шаблонах лидеров, где выводится `email` пользователя — построчный список с
   номерами строк и цитатой строки шаблона. Если в шаблонах лидеров `email` не выводится —
   `NOT FOUND`, и тогда этот блок ограничивается созданием функции и тестов.
5. Регистрация `services/user_helpers.py` в `app.py` или контексте шаблонов (Jinja2
   `context_processor` или импорт в `routes/`) — если хелпер уже доступен в шаблонах, указать
   как. Если нет — этот блок добавляет регистрацию.

## Что сделать

0. Первым действием, до любых команд и чтения кода, дописать в конец `_recon/CHAIN_RUN.md`:

   ```
   ## БЛОК T2 — СТАРТ
   Время старта: <YYYY-MM-DD HH:MM>
   ```

   Если файл `_recon/CHAIN_RUN.md` не существует — создать его и записать строку СТАРТ как первую
   запись. Строка СТАРТ не редактируется и не удаляется.

1. Создать или дополнить файл `services/user_helpers.py`. Добавить функцию
   `display_name_from_email(email: str) -> str` со следующей логикой:
   - Принимает строку `email`. Если `email` пустой, `None` или не содержит `@` — возвращает
     `Игрок`.
   - Берёт часть до `@`. Убирает все цифры. Если после удаления цифр осталась пустая строка —
     возвращает `Игрок` + первые 3 символа части до `@` (в верхнем регистре).
   - Если часть до `@` содержит точку (например `k.petrov`) — берёт последнюю часть после точки,
     транслитерирует, делает первую букву заглавной. Пример: `k.petrov` -> `petrov` -> `Пётров`.
     Если после точки пусто — берёт всю часть до `@` без точки.
   - Сначала проверяет словарь известных имён (полное совпадение после удаления цифр и приведения
     к нижнему регистру). Если найдено — возвращает значение из словаря.
   - Если в словаре нет — применяет посимвольную транслитерацию en->ru по таблице, затем
     приводит к формату «первая буква заглавная, остальные строчные».
   - Если результат транслитерации пустой — возвращает `Игрок` + первые 3 символа части до `@`
     в верхнем регистре.
   - Функция детерминированная: одинаковый вход всегда даёт одинаковый выход.

2. Создать тесты `tests/test_t2_display_name.py` с пятью проверками:
   - `victorkrvvk@gmail.com` -> `Виктор`
   - `anna1990@mail.ru` -> `Анна`
   - `k.petrov@yandex.ru` -> `Пётров`
   - `12345@bk.ru` -> `Игрок123` (первые 3 символа части до `@` — `123`)
   - `dmitry.sergeyev@gmail.com` -> `Сергеев` (по последней части после точки)
   Каждый тест — отдельная функция `test_display_name_<описание>`, использует прямые вызовы
   `display_name_from_email` без HTTP.

3. Найти все шаблоны, отображающие таблицу лидеров (через grep из раздела «Что уже есть»). В
   каждом найденном шаблоне заменить вывод `email` на вызов `display_name_from_email(email)` или
   на заранее вычисленное имя, переданное в контекст маршрутом. В HTML страницы лидеров не
   должно остаться ни одного символа `@`.

4. Зарегистрировать функцию `display_name_from_email` в контексте шаблонов Jinja2 — через
   `context_processor` в `app.py` или через импорт в `routes/` и передачу в `render_template`.
   Выбрать способ, который уже используется в проекте для других хелперов.

5. Прогнать `python -m pytest tests/ --tb=short -q` после всех правок. Сравнить итоговую строку
   с базовой линией v3. `failed` и `errors` не должны вырасти.

6. Сделать коммит: `git add -A`, `git commit -m "chain2: T2 имя из почты в лидерах"`.

7. Написать отчёт `_recon/T2.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T2_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно в этом блоке: удалять почту из БД запрещено — почта остаётся в таблице `users`,
  убирается только из публичного отображения в шаблонах лидеров. Менять схему БД (добавлять
  колонку `display_name` в таблицу `users`) запрещено — имя вычисляется функцией на лету.

## ПРИЁМКА

### 1. Функция display_name_from_email существует

```powershell
python -c "from services.user_helpers import display_name_from_email; print('FOUND')"
```

Ожидаемый вывод: `FOUND`. Если модуль или функция не найдены — `ModuleNotFoundError` или
`ImportError`, в отчёте пишется `NOT FOUND`.

### 2. Пять адресов -> пять ожидаемых имён

```python
from services.user_helpers import display_name_from_email
cases = [
    ('victorkrvvk@gmail.com', 'Виктор'),
    ('anna1990@mail.ru', 'Анна'),
    ('k.petrov@yandex.ru', 'Пётров'),
    ('12345@bk.ru', 'Игрок123'),
    ('dmitry.sergeyev@gmail.com', 'Сергеев'),
]
for email, expected in cases:
    result = display_name_from_email(email)
    status = 'OK' if result == expected else 'FAIL'
    print(f'{status} {email} -> {result} (expected {expected})')
```

Ожидаемый вывод: пять строк `OK`, каждая вида
`OK <email> -> <имя> (expected <имя>)`. Если хотя бы одна строка `FAIL` — пункт не пройден,
в отчёте приводится фактический и ожидаемый результат для каждого случая.

### 3. Тесты T2 проходят

```powershell
python -m pytest tests/test_t2_display_name.py -v --tb=short
```

Ожидаемый вывод: `5 passed`. Каждая тестовая функция — `PASSED`. Если хотя бы один тест `FAILED`
— пункт не пройден.

### 4. На странице лидеров нет символа @ в HTML

```python
import app as A
from services.user_helpers import display_name_from_email
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/leaders', follow_redirects=True)
if r.status_code != 200:
    r = c.get('/leaderboard', follow_redirects=True)
if r.status_code != 200:
    r = c.get('/rating', follow_redirects=True)
html = r.data.decode('utf-8')
at_count = html.count('@')
print('STATUS', r.status_code)
print('AT_COUNT', at_count)
```

Ожидаемый вывод: `STATUS 200`, `AT_COUNT 0`. Если ни один из маршрутов `/leaders`, `/leaderboard`,
`/rating` не возвращает 200 — в отчёте указывается фактический путь страницы лидеров, найденный
grep в разделе «Что уже есть», и проверка повторяется с этим путём. Если `AT_COUNT` больше нуля —
в HTML лидеров осталась почта, пункт не пройден.

### 5. Полный прогон тестов не хуже базовой линии v3

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: итоговая строка `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`, где
`failed` не больше 37, `errors` не больше 0 (или равно 0), `passed` не меньше 939 (плюс
новые тесты T2).

### 6. Коммит T2

```powershell
git add -A
git commit -m "chain2: T2 имя из почты в лидерах"
git log --oneline -1
```

Ожидаемый вывод: сообщение коммита с числом изменённых файлов; последняя строка — хеш и
сообщение коммита.

## Отчёт

Писать в `_recon/T2.md`. Обязательные разделы:

1. Заголовок и дата.
2. Фактическое состояние до правок — наличие `services/user_helpers.py`, список шаблонов
   лидеров, список маршрутов лидеров, места вывода `email` в шаблонах (с номерами строк и
   цитатами). Если чего-то нет — `NOT FOUND`.
3. Текст функции `display_name_from_email` целиком — словарь известных имён, таблица
   транслитерации, логика fallback на «Игрок» + 3 символа.
4. Таблица «вход -> выход» по всем пяти тестовым случаям.
5. Список изменённых и созданных файлов с числом строк каждого.
6. Вывод всех шести пунктов ПРИЁМКА — целиком, как есть, без пересказа.
7. Явное подтверждение, что в HTML страницы лидеров нет ни одного символа `@` — с числом
   `AT_COUNT` из ПРИЁМКА пункт 4.
8. Раздел «Отклонения» — если что-то не реализуемо буквально (маршрут лидеров имеет другое имя,
   хелпер уже существует в другом модуле) — `NOT FOUND` и объяснение, чем заменено.

Запрещённые слова в отчёте (примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается) допустимы только в этом же
перечислении настоящего абзаца. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 03 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 03 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 03 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 02 КОНЕЦ>>>

---

<<<БЛОК 03 НАЧАЛО>>>

# БЛОК 03 — T3 — КУРАТОР ПРИВЕТСТВУЕТ ПО ИМЕНИ

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 03 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 03 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 03 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. `adaptive_tasks` = 0, `users` = 0 —
  норма, приёмка на фикстурах из `tests/conftest.py`. Базовая линия тестов v3 из `_recon/R0_V3.md`:
  `passed=939 failed=37 skipped=17 errors=0`. После правок этого блока `failed` и `errors` не
  должны вырасти.
- Задача: куратор (ИИ-ассистент в приложении) должен приветствовать пользователя по имени вместо
  безличного «Привет!» или «Здравствуйте». Приветствие вида «Привет, Виктор!» для пользователя с
  почтой `victorkrvnk@gmail.com`.
- Используется функция `display_name_from_email(email)` из блока T2 (`services/user_helpers.py`).
  Если блок T2 не выполнен и функция не найдена — этот блок создаёт её сам по спецификации из T2,
  с пометкой в отчёте, что функция создана этим блоком, а не T2.
- Куратор может быть реализован двумя способами: LLM через API (тогда имя добавляется в системный
  промпт или первое сообщение контекста) или шаблонный бот (тогда имя подставляется в шаблон
  сообщения). Точный способ определяется осмотром кода.
- Имя пользователя сохраняется в сессии или контексте при входе. Если пользователь не
  залогинен — приветствие остаётся безличным («Привет!»), без ошибки.
- Дизайн-токены: `--bg:#070C18 --surface:#0E1830 --surface-2:#121F3C --border:#1C2B4F
  --text:#E6EBF7 --muted:#8C9ABC --accent:#4C7DFF --accent-hi:#6B95FF --ok:#3ECF8E --warn:#E5AC3A
  --bad:#E86A62`. Радиус карточки 14px, кнопки 10px. Шрифт Satoshi + system-ui. Контейнер 1120px.
- Запрещено: `git reset/stash/checkout/rm`, rebase, force push. Нет прод-базы и Render. Нет
  редактирования текстов задач. Ключи из `.env`. Бэкап базы перед миграциями. Ноль эмодзи.

## Что уже есть

Перед правками ROO фиксирует в отчёте с цитатами и номерами строк:

1. Наличие `services/user_helpers.py` и функции `display_name_from_email` в нём. Если функция
   найдена — привести её сигнатуру и первые 5 строк тела. Если не найдена — `NOT FOUND`, и тогда
   этот блок создаёт функцию по спецификации из T2 (словарь известных имён, транслитерация
   en->ru, fallback на «Игрок» + 3 символа).
2. Шаблон чата куратора — проверить `templates/chat.html`, `templates/base.html` и все шаблоны,
   содержащие слово «куратор», «curator», «чат», «chat», «ассистент», «assistant», «помощник».
   Построчно выписать найденные файлы и номера строк. Привести цитаты строк, где формируется
   приветственное сообщение (поиск по «Привет», «Здравствуйте», «привет», «здравствуйте»).
3. Маршрут, отдающий страницу чата — grep по `chat`, `curator`, `assistant` в `routes/` и
   `app.py`. Привести путь функции, номер строки, URL маршрута, метод (GET/POST).
4. Сервис куратора — проверить `services/` на наличие модулей с именами `curator`, `chat`,
   `assistant`, `bot`, `ai`. Если найден — привести имя файла, число строк, список функций.
   Если не найден — `NOT FOUND`.
5. Место формирования приветствия — grep по «Привет», «Здравствуйте», «привет»,
   «здравствуйте» во всём проекте (`.py`, `.html`). Построчно выписать все находки с номерами
   строк и цитатами. Отдельно отметить, какие из них относятся к куратору, а какие — к другим
   частям приложения.
6. Способ вызова LLM — если куратор обращается к LLM через API, найти вызов (grep по
   `deepseek`, `openrouter`, `openai`, `chat/completions`, `messages`, `system` в `services/`
   и `routes/`). Привести файл, номер строки, цитату вызова. Как формируется `system` промпт или
   `messages` — привести структуру. Если куратор не обращается к LLM — `NOT FOUND`.
7. Механизм сессии — как сохраняется `user_id` в сессии, есть ли `session['user_id']` или
   `session['_user_id']`, как маршрут получает текущего пользователя. Привести цитату кода
   входа/логина с номером строки.

## Что сделать

0. Первым действием, до любых команд и чтения кода, дописать в конец `_recon/CHAIN_RUN.md`:

   ```
   ## БЛОК T3 — СТАРТ
   Время старта: <YYYY-MM-DD HH:MM>
   ```

   Если файл `_recon/CHAIN_RUN.md` не существует — создать его и записать строку СТАРТ как первую
   запись. Строка СТАРТ не редактируется и не удаляется.

1. Если функция `display_name_from_email` из T2 не найдена — создать `services/user_helpers.py`
   и реализовать функцию по спецификации из блока T2 (словарь известных имён: victor->Виктор,
   anna->Анна, petr->Пётр и т.д.; транслитерация en->ru посимвольно; fallback на «Игрок» +
   первые 3 символа). Если функция найдена — шаг пропускается, в отчёте пишется «функция найдена,
   блок T2 выполнен».

2. Найти место, где куратор формирует приветственное сообщение. В зависимости от типа куратора:
   - Если куратор — LLM через API: добавить имя пользователя в системный промпт или в первое
     сообщение контекста. Имя получается вызовом `display_name_from_email(current_user.email)`
     внутри маршрута или сервиса. Формат: «Ты куратор олимпиадной математики. Обращайся к
     пользователю по имени: <имя>.» или аналогичный, с подстановкой имени в текст.
   - Если куратор — шаблонный бот: заменить статическое приветственное сообщение в шаблоне на
     динамическое с подстановкой имени через `display_name_from_email(user.email)` в контексте
     Jinja2 или через передачу имени из маршрута в `render_template`.
   - Если куратор не найден вообще — `NOT FOUND`, блок ограничивается созданием функции и
     тестов, приёмка по HTTP-проверке пункта 4 считается непройденной с явным объяснением.

3. Сохранить имя пользователя при входе. В маршруте логина (или в `before_request`, или в
   `context_processor`) вычислить `display_name_from_email(user.email)` и сделать его доступным
   в контексте шаблонов и в сервисе куратора. Если имя уже сохраняется в сессии — использовать
   существующий механизм. Если нет — добавить сохранение в сессию или в `context_processor`
   Jinja2.

4. Заменить приветствие куратора: вместо «Привет!» или «Здравствуйте» выводить
   «Привет, <имя>!». Если пользователь не залогинен — приветствие остаётся «Привет!» без имени,
   без ошибки. Поиск всех мест с «Привет» и «Здравствуйте» выполнить по grep из раздела «Что уже
   есть», пункт 5, и заменить только те, что относятся к куратору.

5. Создать тест `tests/test_t3_curator_greeting.py`:
   - Залогиненный пользователь с почтой `victorkrvnk@gmail.com` открывает чат куратора.
   - В HTML ответа содержится подстрока «Привет, Виктор!».
   - Если куратор — LLM через API, и реальный вызов в тесте не делается (нет ключа, нет
     сети), тест проверяет, что имя подставлено в системный промпт или в контекст сообщения —
     через прямой вызов функции формирования приветствия, без HTTP-запроса к LLM.
   - Если куратор — шаблонный бот, тест проверяет HTML ответа через `app.test_client()`.

6. Прогнать `python -m pytest tests/ --tb=short -q` после всех правок. Сравнить итоговую строку
   с базовой линией v3. `failed` и `errors` не должны вырасти.

7. Сделать коммит: `git add -A`, `git commit -m "chain2: T3 куратор приветствует по имени"`.

8. Написать отчёт `_recon/T3.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T3_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно в этом блоке: реальный вызов LLM в тестах не делается (нет гарантии сети и
  баланса). Тест проверяет подстановку имени в контекст или шаблон, не HTTP-ответ внешнего API.
  Значение `user.email` не печатается в отчёте целиком — печатается только вычисленное имя и
  первые 3 символа почты.

## ПРИЁМКА

### 1. Функция display_name_from_email доступна

```powershell
python -c "from services.user_helpers import display_name_from_email; print(display_name_from_email('victorkrvnk@gmail.com'))"
```

Ожидаемый вывод: `Виктор`. Если функция не найдена — `ImportError`, в отчёте пишется `NOT FOUND`,
и если этот блок создал функцию сам — вывод `Виктор` подтверждает создание.

### 2. Приветствие куратора содержит имя

```python
import app as A
from services.user_helpers import display_name_from_email
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/chat', follow_redirects=True)
if r.status_code != 200:
    r = c.get('/curator', follow_redirects=True)
html = r.data.decode('utf-8')
print('STATUS', r.status_code)
print('HAS_GREETING', 'Привет, Виктор!' in html)
```

Ожидаемый вывод: `STATUS 200`, `HAS_GREETING True`. Если ни `/chat`, ни `/curator` не возвращают
200 — в отчёте указывается фактический путь чата куратора, найденный grep в разделе «Что уже
есть», и проверка повторяется с этим путём. Если куратор — LLM через API и приветствие
формируется не в HTML, а в ответе API, то проверяется подстановка имени в контекст сообщения
через пункт 3.

### 3. Имя подставляется в контекст куратора (для LLM-куратора)

```python
import app as A
from services.user_helpers import display_name_from_email
# Если куратор LLM: проверить, что имя попадает в system prompt или messages
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
# Найти функцию формирования контекста куратора и вызвать её
# Имя функции определяется осмотром в разделе «Что уже есть»
from services.user_helpers import display_name_from_email
name = display_name_from_email('victorkrvnk@gmail.com')
print('NAME', name)
print('HAS_VICTOR', 'Виктор' in name)
```

Ожидаемый вывод: `NAME Виктор`, `HAS_VICTOR True`. Если куратор — шаблонный бот (не LLM), этот
пункт считается пройдённым автоматически, в отчёте пишется «куратор шаблонный, пункт
неприменим, имя подставляется в шаблон, см. пункт 2».

### 4. Тест T3 проходит

```powershell
python -m pytest tests/test_t3_curator_greeting.py -v --tb=short
```

Ожидаемый вывод: `1 passed` (или больше, если тест разбит на несколько функций). Каждый тест —
`PASSED`. Если тест `FAILED` — пункт не пройден, в отчёте приводится вывод pytest с `tb=short`.

### 5. Полный прогон тестов не хуже базовой линии v3

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: итоговая строка `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`, где
`failed` не больше 37, `errors` равен 0, `passed` не меньше 939 (плюс новые тесты T3).

### 6. Коммит T3

```powershell
git add -A
git commit -m "chain2: T3 куратор приветствует по имени"
git log --oneline -1
```

Ожидаемый вывод: сообщение коммита с числом изменённых файлов; последняя строка — хеш и
сообщение коммита.

## Отчёт

Писать в `_recon/T3.md`. Обязательные разделы:

1. Заголовок и дата.
2. Фактическое состояние до правок — наличие `display_name_from_email` (из T2 или создана этим
   блоком), шаблон чата куратора, маршрут чата, сервис куратора, места формирования
   приветствия (с номерами строк и цитатами), способ вызова LLM (если есть), механизм сессии.
   Если чего-то нет — `NOT FOUND`.
3. Тип куратора — LLM через API или шаблонный бот. Как именно имя подставляется: в системный
   промпт, в первое сообщение контекста или в шаблон. Цитата изменённого кода.
4. Список изменённых и созданных файлов с числом строк каждого.
5. Вывод всех шести пунктов ПРИЁМКА — целиком, как есть, без пересказа.
6. Явное подтверждение, что залогиненный пользователь с почтой `victorkrvnk@gmail.com` видит
   «Привет, Виктор!» в чате куратора — с цитатой фрагмента HTML или контекста сообщения.
7. Раздел «Отклонения» — если что-то не реализуемо буквально (куратор не найден, маршрут имеет
   другое имя, LLM недоступен для реального вызова) — `NOT FOUND` и объяснение, чем заменено.

Запрещённые слова в отчёте (примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается) допустимы только в этом же
перечислении настоящего абзаца. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 04 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 04 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 04 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 03 КОНЕЦ>>>

---

<<<БЛОК 04 НАЧАЛО>>>

# БЛОК 04 — T1 — УБОРКА ОЛИМПИАД: Каталог, Предикт, Прогресс

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 04 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 04 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 04 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render
  не трогаем. Рабочая база пуста (`adaptive_tasks` = 0, `users` = 0) — это норма, приёмка идёт
  на фикстурах из `tests/conftest.py` (блок Ф0), а не на живых данных.
- Базовая линия тестов — `_recon/R0.md`. После прогона v2 эталон: `passed=939 failed=37
  skipped=17 errors=0`. Новый прогон не должен ухудшить `passed`/`failed`/`skipped`/`errors`.
- Раздел «Олимпиады» реализован в `routes/olympiad.py` (по факту прогона v1 — 655 строк),
  blueprint зарегистрирован под именем `olympiad`, маршруты имеют префикс `/olympiad`.
  Шаблоны лежат в `templates/olympiad/` — известны `method.html` (243 строки) и
  `method_task.html` (102 строки). Точные имена шаблонов подразделов «Каталог», «Предикт
  методов», «Прогресс» и маршруты к ним ROO находит сам по факту, не по записке.
- Задача блока — убрать из раздела «Олимпиады» три подраздела: «Каталог», «Предикт методов»
  (он же «Predict»), «Прогресс» (он же «Progress»). Сам раздел «Олимпиады» и все остальные его
  подразделы (методы, теория, метод-задачи) остаются рабочими. Удаляются только три
  указанных подраздела и их маршруты, шаблоны, ссылки в навигации.
- Порядок работы строго: сначала карта всех входов по шести паттернам, затем проверка, что
  ни один вход не используется другими живыми разделами, только после этого удаление,
  каждое удаление — отдельным шагом с прогоном `pytest` после него.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус 14px/10px, шрифт Satoshi + system-ui, контейнер
  1120px, тёмно-синий стиль, ни одного эмодзи.

## Что уже есть

Перед любым удалением ROO обязан зафиксировать в отчёте с цитатами и номерами строк:

1. Полный список маршрутов, объявленных в `routes/olympiad.py` (декораторы `@bp.route`),
   с точными путями и методами. Подчеркнуть, какие относятся к «Каталогу», «Предикту методов»,
   «Прогрессу», а какие — к остающимся подразделам.
2. Полный список шаблонов в `templates/olympiad/` (имена файлов, число строк). Отметить,
   какие принадлежат трём удаляемым подразделам. Если какого-то нет — `NOT FOUND`.
3. Место регистрации blueprint `olympiad` в `app.py` — номер строки `register_blueprint`.
   Если не зарегистрирован — `NOT FOUND`.
4. Все места в `templates/`, где встречается навигация на «Каталог», «Предикт», «Прогресс» —
   ссылки `<a href>`, вызовы `url_for(...)`, пункты меню в `templates/base.html` или иных
   общих шаблонах. Построчный список: файл, номер строки, текст.
5. Все места в `routes/` и `app.py`, где есть redirect, `url_for` или ссылка на маршруты
   трёх удаляемых подразделов.

## Что сделать

0. Первым действием, до чтения кода и до любых команд, дописать в конец `_recon/CHAIN_RUN.md`
   строку `## БЛОК T1 OLYMPIAD_CLEANUP — СТАРТ` и `Время старта: <YYYY-MM-DD HH:MM>`.
   Строка СТАРТ остаётся на месте, её не редактируют и не удаляют.

1. Коммит-точка перед началом: `git add -A`, затем
   `git commit -m "t1: точка отката перед уборкой олимпиад"`. Если нет изменений —
   зафиксировать это как факт, не как ошибку. Откат (`git reset`, `git checkout`,
   `git stash`, `git rm`) запрещён категорически.

2. Карта всех входов: выполнить grep по всему проекту (исключая `.git`, `node_modules`,
   виртуальное окружение, `instance/`, `_recon/`) по шести паттернам отдельно — `каталог`,
   `предикт`, `predict`, `прогресс`, `progress`, `Каталог`. Для каждого привести полный список
   найденных строк в формате `файл:номер_строки:текст`, без сокращения на «и другие». Это
   карта — результат используется во всех следующих пунктах. Если по паттерну находок нет —
   `NOT FOUND` для этого паттерна.

3. Из карты пункта 2 выделить и явно перечислить: какие вхождения находятся в живых маршрутах
   (`routes/olympiad.py`, иных `routes/*.py`, зарегистрированных в `app.py`), какие — в живых
   шаблонах (`templates/olympiad/` и общих шаблонах навигации), какие — в мёртвом коде, какие —
   в тестах, какие — в документации/записках (эти не трогаются вообще). Каждое вхождение из
   карты получает одну из этих категорий, без вхождений без категории.

4. Проверить, что остающиеся подразделы «Олимпиад» (методы, теория, метод-задачи и иные,
   найденные в пункте 1 «Что уже есть») не содержат ссылок на три удаляемых подраздела — если
   находится хотя бы одна ссылка из остающегося шаблона на удаляемый маршрут, это блокирует
   удаление до отдельного разбора: сначала убрать ссылку (отдельный маленький шаг с собственным
   прогоном `pytest`), затем продолжать удаления.

5. Удалить маршруты трёх подразделов из `routes/olympiad.py` (точечная правка файла: убрать
   функции-обработчики и их декораторы `@bp.route`, оставить остальные маршруты нетронутыми).
   Отдельный шаг, сразу после него `python -m pytest tests/ --tb=short -q`.

6. Удалить шаблоны трёх подразделов из `templates/olympiad/` обычным удалением (`Remove-Item`
   или `os.remove`, не `git rm`). Если какого-то шаблона нет — `NOT FOUND`, шаг для него не
   выполняется. Каждый файл — отдельным шагом, с прогоном `pytest` после него.

7. Убрать ссылки на три подраздела из навигации (`templates/base.html` и иные общие шаблоны,
   найденные в пункте 4 «Что уже есть») — точечная правка, отдельный шаг, с прогоном `pytest`
   после него. Если ссылок в навигации не было — `NOT FOUND`, шаг не выполняется.

8. Проверить, что удалённые маршруты дают 404, а главная страница «Олимпиад» отвечает 200.
   Остальные остающиеся подразделы «Олимпиад» (методы, теория) также отвечают 200 — взять
   реальные пути из пункта 1 «Что уже есть».

9. После всех удалений — итоговый grep по всем шести паттернам ещё раз. Результат должен
   показывать только находки в категориях «тесты» и «документация/записки». Находок в живых
   маршрутах или живых шаблонах остаться не должно.
10. Если существуют тесты удалённых подразделов (`tests/test_olympiad*.py` или аналог),
    ссылающиеся на удалённые маршруты, — удалить или поправить их отдельным шагом, с прогоном
    `pytest` после него. Если таких тестов нет — `NOT FOUND`, шаг не выполняется.
11. Коммит: `git add -A`, `git commit -m "t1: убраны каталог, предикт, прогресс из олимпиад"`.
    Если коммит невозможен — зафиксировать как факт.

12. Написать отчёт `_recon/T1.md`.

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T1_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно в этом блоке: удалять сам раздел «Олимпиады» и остающиеся подразделы (методы,
  теория, метод-задачи) запрещено — убираются строго три подраздела «Каталог», «Предикт
  методов», «Прогресс», ничего больше.
- Удаление любого элемента раньше карты всех входов (пункт 2 «Что сделать») запрещено — порядок
  карта, затем проверка использования, затем удаление обязателен и не может быть переставлен.
- Удаление без прогона `pytest` сразу после каждого отдельного шага запрещено — один шаг
  удаления, один прогон, только затем следующий шаг.
- Удалять или изменять маршруты остающихся подразделов «Олимпиад», шаблоны `method.html`,
  `method_task.html` и таблицу `olympiad_theory` запрещено.

## ПРИЁМКА

### 1. Коммит-точка перед началом

```powershell
git add -A
git commit -m "t1: точка отката перед уборкой олимпиад"
git log --oneline -1
```

Ожидаемый вывод: сообщение коммита с числом изменённых файлов, либо
`nothing to commit, working tree clean`, если изменений нет; последняя строка — хеш и
сообщение коммита.

### 2. Карта всех входов по шести паттернам

```powershell
foreach ($p in @("каталог","предикт","predict","прогресс","progress","Каталог")) {
  Write-Output "=== $p ==="
  Select-String -Path "*.py","routes/*.py","templates/*.html","templates/**/*.html","app.py" -Pattern $p
}
```

Ожидаемый вывод: шесть блоков `=== <паттерн> ===`, каждый со списком `файл:строка:текст` или
пустым выводом. Пустой блок — приемлемый факт, писать `NOT FOUND` для этого паттерна.

### 3. Остающиеся шаблоны не ссылаются на удаляемые маршруты

```powershell
Select-String -Path "templates/olympiad/method.html","templates/olympiad/method_task.html" -Pattern "каталог|предикт|predict|прогресс|progress"
```

Ожидаемый вывод: пустой вывод (ничего не найдено). Любая найденная строка — блокирующая находка,
описывается в отчёте, удаление не начинается до её устранения.

### 4. Прогон pytest после удаления маршрутов

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`, числа `failed` и
`errors` не больше базовой линии Р0 (`failed` <= 37, `errors` <= 0).

### 5. Прогон pytest после удаления шаблонов и навигации

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: та же форма строки, числа не хуже предыдущего шага.

### 6. Главная страница Олимпиад отвечает 200

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/olympiad', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1500])
```

Ожидаемый вывод: `STATUS 200`, `LEN` больше нуля, фрагмент HTML раздела «Олимпиады» без ссылок на
«Каталог», «Предикт», «Прогресс». Если фактический путь главной Олимпиад иной (пункт 1 «Что уже
есть») — подставить его.

### 7. Удалённые маршруты дают 404

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
for path in ['/olympiad/catalog', '/olympiad/predict', '/olympiad/progress']:
    r = c.get(path, follow_redirects=True)
    print(path, r.status_code)
```

Ожидаемый вывод: три строки вида `<путь> 404`. Если фактические пути удалённых подразделов иные
(пункт 1 «Что уже есть») — подставить их. Если какой-то из трёх подразделов не найден в коде —
для его пути пишется `NOT FOUND` в отчёте, а маршрут уже даёт 404 как несуществующий.

### 8. Остающиеся подразделы Олимпиад отвечают 200

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
for path in ['<путь_методов>', '<путь_теории>']:
    r = c.get(path, follow_redirects=True)
    print(path, r.status_code)
```

Ожидаемый вывод: строки вида `<путь> 200` для каждого остающегося подраздела. Пути берутся из
пункта 1 «Что уже есть», не выдумываются.

### 9. Итоговый grep после всех удалений

```powershell
foreach ($p in @("каталог","предикт","predict","прогресс","progress")) {
  Write-Output "=== $p ==="
  Select-String -Path "*.py","routes/*.py","templates/*.html","templates/**/*.html","app.py" -Pattern $p
}
```

Ожидаемый вывод: пять блоков. В блоках могут оставаться только находки в тестах и в
документации/записках. Находок в `routes/olympiad.py`, `app.py` и живых шаблонах
`templates/olympiad/` быть не должно.

### 10. Полный прогон тестов не хуже базовой линии Р0, финальный

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: итоговая строка `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`, где
`failed` <= 37 и `errors` <= 0, а `passed` >= 939.

## Отчёт

Писать в `_recon/T1.md`. Обязательные разделы:

1. Заголовок и дата.
2. Полный список маршрутов `routes/olympiad.py` до правок с разбивкой на удаляемые и
   остающиеся.
3. Полная карта всех входов по шести паттернам (пункт 2 ПРИЁМКА) с разбивкой по категориям:
   живые маршруты, живые шаблоны, мёртвый код, тесты, документация.
4. Список всех шагов удаления в порядке выполнения, каждый с именем изменённого файла или
   строки и результатом прогона `pytest` сразу после него (полная итоговая строка вида
   `passed/failed/skipped/errors`).
5. Вывод всех десяти пунктов ПРИЁМКА — целиком, как есть, без пересказа.
6. Явное подтверждение, что раздел «Олимпиады» и остающиеся подразделы (методы, теория,
   метод-задачи) не были удалены и не изменены по содержанию — сравнение числа строк
   `routes/olympiad.py`, `method.html`, `method_task.html` до и после блока.
7. Раздел «Отклонения» — если что-то не реализуемо буквально (файл с другим именем, подраздел
   не найден, маршрут уже не существует) — `NOT FOUND` и объяснение, чем заменено.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается.
Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 05 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 05 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 05 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 04 КОНЕЦ>>>

---

<<<БЛОК 05 НАЧАЛО>>>

# БЛОК 05 — T4 — ПРОБНЫЙ ПЕРИОД: 1 день, потом paywall

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 05 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 05 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 05 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render
  не трогаем. Рабочая база пуста (`adaptive_tasks` = 0, `users` = 0) — норма, приёмка идёт на
  фикстурах из `tests/conftest.py` (блок Ф0), а не на живых данных.
- Базовая линия тестов — `_recon/R0.md`. После прогона v2 эталон: `passed=939 failed=37
  skipped=17 errors=0`. Новый прогон не должен ухудшить `passed`/`failed`/`skipped`/`errors`.
- Бизнес-модель: ученик платит 400 рублей в месяц за семь срезов. Проверка фото через Kimi —
  0.35 рубля за штуку. Кредиты на чертежи: `User.figure_credits`, три бесплатных, пакеты
  10/30/100 за 99/249/599 рублей. Подписка и триал — отдельный от кредитов механизм доступа.
- Срез: `routes/prep.py` (2907 строк), `templates/prep/probe.html` (301 строка), маршрут
  `/prep/probe`. Чертежи: `routes/figures_generator.py` (592 строки), маршрут
  `/figures/generate`, шаблон `templates/figures_generate.html` (256 строк). Задачи дня:
  `daily_tasks/routes.py` (929 строк), маршрут `/daily`. Точные пути ROO сверяет по
  `app.url_map.iter_rules()`, а не по записке.
- Модель `User` в `models.py` (фактическую длину и колонки зафиксировать до правок). Поля
  подписки и триала могут отсутствовать — ROO проверяет по факту и добавляет, если нужно.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус 14px/10px, шрифт Satoshi + system-ui, контейнер
  1120px, тёмно-синий стиль, ни одного эмодзи.

## Что уже есть

Перед правками ROO обязан зафиксировать в отчёте с цитатами и номерами строк:

1. Модель `User` в `models.py` — полный список колонок. Есть ли уже `trial_started_at`,
   поля подписки (`subscription_active`, `subscription_until`, `subscribed_until` или иные).
   Для каждого найденного — тип, значение по умолчанию, номер строки. Если поля отсутствуют —
   `NOT FOUND` и явное указание, что их нужно добавить.
2. Место регистрации пользователя в коде — функция или маршрут `register`/`signup` в
   `app.py` или `routes/`. Привести номер строки и код, где создаётся объект `User`. Если
   регистрации как маршрута нет — `NOT FOUND`.
3. Все декораторы или функции проверки доступа в проекте — `login_required`, `subscription
   _required`, `paid_required` или иные. Полный список с файлом и номером строки. Если
   механизма проверки подписки нет — `NOT FOUND`.
4. Фактические маршруты среза, чертежа и задач дня из `app.url_map.iter_rules()` — точные
   пути и методы, чтобы приёмка подставляла реальные пути, а не выдуманные.
5. Существующие фикстуры в `tests/conftest.py` — имена функций с `@pytest.fixture`, особенно
   `test_user`, `auth_client`, чтобы новые фикстуры не дублировали имена.

## Что сделать

0. Первым действием, до чтения кода и до любых команд, дописать в конец `_recon/CHAIN_RUN.md`
   строку `## БЛОК T4 TRIAL_1_DAY — СТАРТ` и `Время старта: <YYYY-MM-DD HH:MM>`. Строка СТАРТ
   остаётся на месте, её не редактируют и не удаляют.

1. Коммит-точка перед началом: `git add -A`, затем
   `git commit -m "t4: точка отката перед триалом"`. Если нет изменений — зафиксировать как
   факт. Откат запрещён категорически. Перед миграцией — копия базы
   `instance/formyla.db` с именем `formyla.db.bak_T4_pre`.

2. Добавить в модель `User` колонку `trial_started_at` (timestamp, nullable=True, по умолчанию
   `None`), если её нет. Колонку подписки — `subscription_until` (timestamp, nullable=True) —
   если поля подписки отсутствуют вовсе. Если поля подписки уже есть под другим именем —
   использовать существующее, не дублировать. Миграция: `ALTER TABLE users ADD COLUMN
   trial_started_at` (и `subscription_until` при отсутствии). Копия базы перед миграцией
   обязательна.

3. При регистрации пользователя записывать `trial_started_at = datetime.utcnow()`. Найти
   место создания `User` (пункт 2 «Что уже есть») и добавить туда установку поля. Если
   регистрации как маршрута нет — `NOT FOUND`, тогда установка происходит в любом существующем
   пути создания пользователя (например, фикстуры или админка), зафиксировать это в отчёте.

4. Реализовать функцию-помощник `is_trial_active(user)` (в `models.py` или `services/`): возвращает
   `True`, если `user.trial_started_at` не `None` и `datetime.utcnow() - trial_started_at` меньше
   24 часов (86400 секунд). Возвращает `False`, если триал истёк или поле `None`.

5. Реализовать функцию `has_active_subscription(user)`: возвращает `True`, если
   `user.subscription_until` не `None` и больше `datetime.utcnow()` (или по существующему полю
   подписки из пункта 1 «Что уже есть»). Возвращает `False` иначе.

6. Реализовать функцию `has_access(user)`: возвращает `True`, если `is_trial_active(user)` или
   `has_active_subscription(user)`. Иначе `False`. Эта функция — единая точка проверки доступа к
   платным функциям (срез, чертёж, задачи дня, методы).

7. В маршрутах среза (`/prep/probe`), чертежа (`/figures/generate`) и задач дня (`/daily`)
   добавить проверку `has_access(current_user)` перед основной логикой. Если доступа нет —
   вернуть страницу с текстом «Триальный период окончен» и предложением подписки (код не `500`,
   например `402` или редирект на `/pricing` с flash-сообщением). Базовые функции — логин,
   профиль, лидерборд (`/leaders`) — доступны всегда, проверка на них не ставится.

8. Страница «Триальный период окончен» — использовать существующий `templates/pricing.html`
   или создать `templates/trial_expired.html` в стиле эталона дизайна. Текст дословно:
   «Триальный период окончен». На этой странице — предложение подписки со ссылкой на покупку.
   Ни одного эмодзи.

9. Создать фикстуры в `tests/conftest.py` (дополнить, не перезаписывать):
   - `user_trial_active` — `trial_started_at` = текущее время минус 1 час, подписки нет.
   - `user_trial_expired` — `trial_started_at` = текущее время минус 2 дня, подписки нет.
   - `user_subscribed` — `subscription_until` = текущее время плюс 30 дней, триал может
     быть истёкшим.
   - `user_trial_expired_no_sub` — `trial_started_at` = текущее время минус 2 дня, подписки
     нет (по содержанию совпадает с `user_trial_expired`; если различия нет — использовать
     одну фикстуру, отметить в отчёте).
   Каждая фикстура — запись в `users` во временной базе, не в `instance/formyla.db`.

10. Написать тесты в `tests/test_t4_trial.py` на фикстурах: четыре пользователя, проверить
    доступ к `/prep/probe`, `/figures/generate`, `/daily`. `user_trial_active` — доступ открыт
    (200). `user_subscribed` — 200. `user_trial_expired` и `user_trial_expired_no_sub` — доступ
    закрыт, в теле «Триальный период окончен», код не `500`. Базовые маршруты (`/leaders`,
    профиль) — 200 для всех четырёх. Тесты не пишут в `instance/formyla.db`.

11. Прогнать `python -m pytest tests/ --tb=short -q` после правок. Сравнить с базовой линией.

12. Коммит: `git add -A`,
    `git commit -m "t4: триал 1 день, проверка доступа, фикстуры"`. Написать отчёт `_recon/T4.md`.

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T4_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно в этом блоке: запись фикстур в `instance/formyla.db` запрещена — все фикстуры
  работают только с временной базой. Ставить проверку `has_access` на базовые маршруты
  (логин, профиль, лидерборд) запрещено — они доступны всегда. Делать триал длиннее или короче
  24 часов запрещено — ровно 86400 секунд. Менять логику списания кредитов на чертежи (блок К1)
  запрещено — триал и кредиты отдельные механизмы.

## ПРИЁМКА

### 1. Коммит-точка перед началом

```powershell
git add -A
git commit -m "t4: точка отката перед триалом"
git log --oneline -1
```

Ожидаемый вывод: сообщение коммита с числом файлов, либо `nothing to commit, working tree clean`;
последняя строка — хеш и сообщение коммита.

### 2. Колонки trial_started_at и subscription_until в схеме

```python
import sqlite3
conn = sqlite3.connect('instance/formyla.db')
cols = [c[1] for c in conn.execute("PRAGMA table_info(users)").fetchall()]
print('HAS_TRIAL', 'trial_started_at' in cols)
print('HAS_SUB', 'subscription_until' in cols)
```

Ожидаемый вывод: `HAS_TRIAL True`, `HAS_SUB True` (либо имя существующего поля подписки из
пункта 1 «Что уже есть», если оно уже было — тогда `HAS_SUB True` по факту).

### 3. Функции is_trial_active и has_access существуют

```python
import app as A
print('HAS_TRIAL_FN', hasattr(A, 'is_trial_active') or any(hasattr(A, n) for n in dir(A)))
```

Ожидаемый вывод: функция `is_trial_active` и `has_access` найдены в модуле `app` или в
`models`/`services` (фактический путь фиксируется в отчёте). Если не найдены — `NOT FOUND`.

### 4. Тест: пользователь с активным триалом — доступ открыт

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t4_trial.py::test_trial_active_access', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест авторизуется как `user_trial_active`, запрашивает `/prep/probe`,
`/figures/generate`, `/daily` и утверждает код `200` для каждого (точные пути из пункта 4
«Что уже есть»).

### 5. Тест: пользователь с подпиской — доступ открыт

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t4_trial.py::test_subscribed_access', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест авторизуется как `user_subscribed`, утверждает `200` на срезе,
чертеже, задачах дня.

### 6. Тест: пользователь с истёкшим триалом и без подписки — доступ закрыт

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t4_trial.py::test_trial_expired_blocked', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест авторизуется как `user_trial_expired`, утверждает код не `500`
(конкретно `402` либо редирект на `/pricing` — фактический фиксируется) и наличие в теле ответа
текста «Триальный период окончен».

### 7. Тест: базовые функции доступны всегда

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t4_trial.py::test_basic_always_open', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест проходит по всем четырём пользователям и утверждает `200` на
`/leaders` и странице профиля (точный путь из `app.url_map`).

### 8. Текст «Триальный период окончен» присутствует в шаблоне

```python
import os
paths = ['templates/trial_expired.html', 'templates/pricing.html']
for p in paths:
    if os.path.exists(p):
        print(p, 'Триальный период окончен' in open(p, encoding='utf-8').read())
    else:
        print(p, 'NOT FOUND')
```

Ожидаемый вывод: хотя бы один файл содержит `True` — текст «Триальный период окончен» найден.
Если ни в одном нет — `NOT FOUND`, находка, добавить текст и повторить.

### 9. Приёмка срез, чертёж, день для истёкшего триала

```python
import app as A
from datetime import datetime, timedelta
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r_probe = c.get('/prep/probe', follow_redirects=True)
r_fig = c.get('/figures/generate', follow_redirects=True)
r_daily = c.get('/daily', follow_redirects=True)
print('PROBE', r_probe.status_code, 'Триальный период окончен' in r_probe.data.decode('utf-8'))
print('FIG', r_fig.status_code, 'Триальный период окончен' in r_fig.data.decode('utf-8'))
print('DAILY', r_daily.status_code, 'Триальный период окончен' in r_daily.data.decode('utf-8'))
```

Ожидаемый вывод: на живой базе (где `users` = 0) это даст редирект на логин или `404` — это
норма, приёмка по доступу проходит на фикстурах пунктов 4-6. Команда приводится как есть,
вывод фиксируется в отчёте.

### 10. Полный прогон тестов не хуже базовой линии

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`, где `failed` <= 37 и
`errors` <= 0, а `passed` >= 939.

## Отчёт

Писать в `_recon/T4.md`. Обязательные разделы:

1. Заголовок и дата.
2. Состояние модели `User` до правок — какие поля подписки и триала были, каких не было
   (`NOT FOUND` для отсутствующих).
3. Миграция — какие колонки добавлены, имя файла-бэкапа базы, копия `formyla.db.bak_T4_pre`.
4. Реализация функций `is_trial_active`, `has_active_subscription`, `has_access` — файл,
   номер строки, фактическая логика (цитата кода).
5. Места установки `trial_started_at` при регистрации — файл, номер строки.
6. Список маршрутов, на которые поставлена проверка доступа, с номерами строк.
7. Вывод всех десяти пунктов ПРИЁМКА — целиком, как есть, без пересказа.
8. Раздел «Отклонения» — если что-то не реализуемо буквально (маршрут регистрации отсутствует,
  поле подписки под другим именем, путь среза иной) — `NOT FOUND` и объяснение, чем заменено.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается.
Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 06 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 06 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 06 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 05 КОНЕЦ>>>

---

<<<БЛОК 06 НАЧАЛО>>>

# БЛОК 06 — T9 — ПРИОРИТЕТ ИИ ДЛЯ ПОДПИСЧИКОВ + ОЧЕРЕДЬ

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 06 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 06 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 06 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render
  не трогаем. Рабочая база пуста (`adaptive_tasks` = 0, `users` = 0) — норма, приёмка идёт на
  фикстурах из `tests/conftest.py` (блок Ф0), а не на живых данных.
- Базовая линия тестов — `_recon/R0.md`. После прогона v2 эталон: `passed=939 failed=37
  skipped=17 errors=0`. Новый прогон не должен ухудшить `passed`/`failed`/`skipped`/`errors`.
- Очередь построения чертежей — модель `FigureBuildJob` (введена слоем Ч5), таблица
  `figure_build_jobs`, статусы `queued` / `thinking` / `drawing` / `done` / `failed`. Обработка
  очереди — `process_pre_gen_queue()` в `app.py` (по факту прогона v1 — около строки 2101,
  вызывается из `before_request`). Витрина генерации: `routes/figures_generator.py` (592 строки),
  `templates/figures_generate.html` (256 строк).
- Бизнес-модель: ученик платит 400 рублей в месяц за семь срезов. Подписчики получают
  приоритет в очереди генерации чертежей — это продукт этого блока. Кредиты на чертежи
  (`User.figure_credits`, пакеты 10/30/100 за 99/249/599 рублей) — отдельный механизм,
  списание не меняется (блок К1).
- Подписка и триал: поля `trial_started_at` и `subscription_until` на `User` (если блок Т4
  выполнен раньше — поля уже есть; если нет — ROO использует существующее поле подписки или
  добавляет `subscription_until`, см. пункт 1 «Что уже есть»). Приоритет зависит от активной
  подписки, а не от триала.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус 14px/10px, шрифт Satoshi + system-ui, контейнер
  1120px, тёмно-синий стиль, ни одного эмодзи.

## Что уже есть

Перед правками ROO обязан зафиксировать в отчёте с цитатами и номерами строк:

1. Модель `FigureBuildJob` в `models.py` — полный список колонок, типы, значения по умолчанию,
   номера строк объявления. Есть ли уже колонка `priority` (или `priority_level`). Если нет —
   `NOT FOUND`, её нужно добавить. Колонка `user_id` (или аналог) — как связана с `User`.
2. Модель `User` — есть ли поле подписки (`subscription_until` или иное). Тип, значение по
   умолчанию, номер строки. Если поля нет — `NOT FOUND`.
3. Функция `process_pre_gen_queue()` в `app.py` — полный текст (или цитата), номер строки,
   как выбирается следующая задача из очереди (порядок `ORDER BY`, если есть). Если
   планировщика очереди нет — `NOT FOUND`.
4. Маршрут страницы генерации чертежа (`/figures/generate` или аналог) из
   `app.url_map.iter_rules()` — точный путь и метод. Шаблон `templates/figures_generate.html`
   — есть ли в нём уже блок очереди, JS-обновление, SSE-подключение. Привести номер строки.
5. Существующие фикстуры в `tests/conftest.py` — имена с `@pytest.fixture`, особенно
   `test_user`, `figure_build_job`, чтобы новые фикстуры не дублировали имена.

## Что сделать

0. Первым действием, до чтения кода и до любых команд, дописать в конец `_recon/CHAIN_RUN.md`
   строку `## БЛОК T9 AI_PRIORITY_QUEUE — СТАРТ` и `Время старта: <YYYY-MM-DD HH:MM>`. Строка
   СТАРТ остаётся на месте, её не редактируют и не удаляют.

1. Коммит-точка перед началом: `git add -A`, затем
   `git commit -m "t9: точка отката перед приоритетом очереди"`. Если нет изменений —
   зафиксировать как факт. Откат запрещён категорически. Перед миграцией — копия базы
   `instance/formyla.db` с именем `formyla.db.bak_T9_pre`.

2. Добавить в модель `FigureBuildJob` колонку `priority` (integer, по умолчанию 0), если её
   нет. Условные значения: `0` — обычный (бесплатный пользователь), `1` — высокий (подписчик).
   Миграция: `ALTER TABLE figure_build_jobs ADD COLUMN priority`. Копия базы перед миграцией
   обязательна. Больше колонок не добавлять.

3. При создании задачи в очереди (`FigureBuildJob`) устанавливать `priority` по подписке
   пользователя: если `has_active_subscription(user)` (или эквивалентная проверка по полю
   подписки из пункта 2 «Что уже есть») — `priority = 1`, иначе `priority = 0`. Найти место
   создания `FigureBuildJob` (в `routes/figures_generator.py` или `routes/figures.py`) и
   добавить туда установку поля. Если блока Т4 ещё нет и функции `has_active_subscription`
   нет — реализовать проверку подписки здесь же (поле `subscription_until` не `None` и больше
   `datetime.utcnow()`).

4. Изменить выборку следующей задачи в `process_pre_gen_queue()` (или эквиваленте): задачи
   сортируются сначала по `priority` по убыванию, затем по `created_at` по возрастанию
   (FIFO внутри одного приоритета). Подписчики (`priority = 1`) обрабатываются раньше
   бесплатных (`priority = 0`). Привести фактический код `ORDER BY` в отчёте.

5. Реализовать функцию `queue_position(job)` — возвращает позицию задачи в очереди для её
   пользователя: количество задач `FigureBuildJob` того же `user_id` в статусе `queued`, с
   `created_at` меньше или равным данной (то есть стоящих перед ней или на её месте). Позиция
   считается только среди задач этого же пользователя — чужие задачи в расчёт не входят.

6. Реализовать функцию `queue_total(user_id)` — общее число задач `queued` данного
   пользователя. Используется для сообщения «позиция N из M». Очередь для одного пользователя
   — только его задачи, не чужие.

7. В шаблоне `templates/figures_generate.html` добавить блок отображения позиции в очереди.
   Текст для подписчика: «Вы в приоритетной очереди: позиция N». Текст для бесплатного:
   «Вы в очереди: позиция N из M». N и M подставляются из `queue_position` и `queue_total`.
   Обновление — через JavaScript-опрос (AJAX на маршрут статуса) или SSE; способ ROO выбирает
   по факту проекта, в отчёте указывает выбранный. Ни одного эмодзи.

8. Добавить маршрут статуса очереди (например `GET /figures/queue-status`), возвращающий JSON
   `{"position": N, "total": M, "priority": 0|1}` для текущего пользователя и его последней
   задачи `queued`. Если задач `queued` нет — `{"position": 0, "total": 0, "priority": 0}`.
   Маршрут требует авторизации.

9. Создать фикстуры в `tests/conftest.py` (дополнить, не перезаписывать):
   - `user_subscribed` — `subscription_until` в будущем.
   - `user_free` — без подписки.
   - `five_priority_jobs` — пять задач `FigureBuildJob` в статусе `queued`: три от
     `user_subscribed` (`priority = 1`), две от `user_free` (`priority = 0`), с разными
     `created_at` в порядке создания.
   Все фикстуры — во временной базе, не в `instance/formyla.db`.

10. Написать тесты в `tests/test_t9_priority.py` на фикстурах: создать пять задач (три от
    подписчика, две от бесплатного), проверить, что `process_pre_gen_queue()` (или
    эквивалент) обрабатывает подписчика первым — порядок обработки: первые три задачи от
    подписчика (`priority = 1`), затем две от бесплатного (`priority = 0`). Проверить
    `queue_position` и `queue_total` — позиция и общее число только среди задач этого
    пользователя. Проверить маршрут `/figures/queue-status` — JSON с корректными `position`,
    `total`, `priority`.

11. Прогнать `python -m pytest tests/ --tb=short -q`. Сравнить с базовой линией.

12. Коммит: `git add -A`,
    `git commit -m "t9: приоритет подписчиков в очереди чертежей"`. Написать отчёт `_recon/T9.md`.

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T9_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно в этом блоке: менять логику списания кредитов (блок К1) запрещено — приоритет
  и кредиты отдельные механизмы. Считать позицию в очереди по чужим задачам запрещено — только
  задачи данного пользователя. Добавлять больше одного приоритетного уровня запрещено — ровно
  два: `0` (бесплатный) и `1` (подписчик). Трогать `User.figure_credits`,
  `FigureCreditTransaction`, `FigureGeneration` запрещено.

## ПРИЁМКА

### 1. Коммит-точка перед началом

```powershell
git add -A
git commit -m "t9: точка отката перед приоритетом очереди"
git log --oneline -1
```

Ожидаемый вывод: сообщение коммита с числом файлов, либо `nothing to commit, working tree clean`;
последняя строка — хеш и сообщение коммита.

### 2. Колонка priority в figure_build_jobs

```python
import sqlite3
conn = sqlite3.connect('instance/formyla.db')
cols = [c[1] for c in conn.execute("PRAGMA table_info(figure_build_jobs)").fetchall()]
print('HAS_PRIORITY', 'priority' in cols)
```

Ожидаемый вывод: `HAS_PRIORITY True`. Если колонки нет — `NOT FOUND`, миграция не выполнена.

### 3. Функции queue_position и queue_total существуют

```python
import app as A
for fn in ['queue_position', 'queue_total']:
    print(fn, hasattr(A, fn) or any(fn in dir(getattr(A, m, object())) for m in []))
```

Ожидаемый вывод: функции найдены в `app` или в `models`/`services`/`routes` (фактический путь
фиксируется в отчёте). Если не найдены — `NOT FOUND`.

### 4. Тест: подписчик обработан первым

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t9_priority.py::test_subscriber_first', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест создаёт пять задач (три от `user_subscribed`, две от
`user_free`), вызывает `process_pre_gen_queue()` (или эквивалент) и утверждает, что первые три
обработанные задачи принадлежат `user_subscribed` (`priority = 1`), затем две от `user_free`.

### 5. Тест: позиция и общее число только среди задач пользователя

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t9_priority.py::test_queue_position_scope', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест создаёт задачи двух пользователей и утверждает, что
`queue_position` и `queue_total` для `user_free` считают только его задачи, а не все задачи в
очереди.

### 6. Маршрут статуса очереди возвращает JSON

```python
import app as A, json
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/figures/queue-status', follow_redirects=True)
print('STATUS', r.status_code)
try:
    data = json.loads(r.data.decode('utf-8'))
    print('KEYS', sorted(data.keys()))
except Exception as e:
    print('NOT_JSON', e)
```

Ожидаемый вывод: `STATUS 200` (или иной код, если маршрут требует задач в очереди —
фиксируется), `KEYS` содержит `position`, `total`, `priority`. Если маршрут не найден —
`NOT_FOUND` (фактический путь из пункта 4 «Что уже есть» подставляется, если имя иное).

### 7. Текст позиции в очереди присутствует в шаблоне

```python
text = open('templates/figures_generate.html', encoding='utf-8').read()
print('PRIORITY_MSG', 'приоритетной очереди' in text)
print('QUEUE_MSG', 'Вы в очереди' in text)
```

Ожидаемый вывод: `PRIORITY_MSG True`, `QUEUE_MSG True`. Если хотя бы одно `False` — находка,
добавить текст, повторить команду, привести новый вывод.

### 8. ORDER BY priority в выборке очереди

```powershell
Select-String -Path "app.py" -Pattern "priority"
```

Ожидаемый вывод: строки с номером и текстом совпадения, где `priority` участвует в `ORDER BY`
или эквивалентной сортировке в `process_pre_gen_queue` или ином обработчике очереди. Пустой
вывод — `NOT FOUND`, приоритет не встроен в выборку.

### 9. Полный прогон тестов не хуже базовой линии

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`, где `failed` <= 37 и
`errors` <= 0, а `passed` >= 939.

## Отчёт

Писать в `_recon/T9.md`. Обязательные разделы:

1. Заголовок и дата.
2. Состояние модели `FigureBuildJob` до правок — колонки, была ли `priority` (`NOT FOUND`,
   если не было).
3. Миграция — добавленная колонка `priority`, имя файла-бэкапа `formyla.db.bak_T9_pre`.
4. Реализация `queue_position`, `queue_total`, установка `priority` при создании задачи —
   файлы, номера строк, цитаты кода.
5. Изменение выборки в `process_pre_gen_queue` — фактический `ORDER BY` до и после (цитата).
6. Маршрут статуса очереди — путь, метод, формат JSON.
7. Блок отображения позиции в `templates/figures_generate.html` — номер строки, текст
   сообщений, способ обновления (JS-опрос или SSE).
8. Вывод всех девяти пунктов ПРИЁМКА — целиком, как есть, без пересказа.
9. Раздел «Отклонения» — если что-то не реализуемо буквально (поле подписки под другим именем,
   `process_pre_gen_queue` отсутствует, имя маршрута иное) — `NOT FOUND` и объяснение, чем
   заменено.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается.
Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 07 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 07 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 07 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 06 КОНЕЦ>>>

---

<<<БЛОК 07 НАЧАЛО>>>

# БЛОК 07 — T8 — СЕРИЯ: дни подряд на 100%, выходные

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 07 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 07 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 07 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, рабочая база `instance/formyla.db`. Прод и Render не трогаем,
  Auto-Deploy остаётся OFF. База пуста (`adaptive_tasks` = 0, `users` = 0, `solution_attempts`
  = 0) — это норма, приёмка идёт на фикстурах из `tests/conftest.py` (фикстуры `app`,
  `auth_client`, `test_user`, `daily_set_with_items`), созданных блоком Ф0, а не на живых данных.
- Базовая линия тестов после прогона v2: `passed=939 failed=37 skipped=17 errors=0`. Рост `failed`
  или `errors` относительно этой линии — жёсткий стоп, блок помечается `ЧАСТИЧНО`.
- Механика адаптивности: `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`. Правильный ответ +1, частичный 0, неверный -2.
  Формулы и веса не меняются этим блоком. Выходной день обязан не трогать `mu` и `sigma` —
  пропуск задач не считается ни верным, ни неверным, запись в `level_engine` не идёт.
- Якоря: `algebra -> number_theory -> geometry -> combinatorics -> logic`. Куратор назначает
  7 подтем на месяц. Подтемы — finer-гранулярные единицы внутри якорей, этот блок их не правит.
- Задачи дня: `daily_tasks/routes.py` (929 строк), `daily_tasks/services.py` (2326 строк),
  шаблон `templates/daily_task.html` (фактическое имя подтвердить по `daily_tasks/routes.py`),
  `templates/daily_tasks/daily_tasks_dashboard.html`. Набор задач дня — таблицы `daily_task_sets`
  и `daily_task_items`. Завершение набора — момент, когда все `daily_task_items` набора получили
  ответ.
- Профиль ученика: `templates/profile.html` (правлен блоком В9), маршрут профиля — найти по
  `app.py` или `routes/`. Уровень и `mu`/`sigma`: `services/level_engine.py` (265 строк),
  `services/theme_probe.py` (433 строки, `record_answer`).
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус карточек 14px, кнопок 10px, шрифт Satoshi и system-ui,
  контейнер 1120px, тёмно-синий стиль, ни одного эмодзи.
- Это блок цепочки v2. Первым действием блок дописывает в конец `_recon/CHAIN_RUN.md` строку
  `## БЛОК T8 СЕРИЯ — СТАРТ` с временем старта, до чтения кода и до любых команд. Коммит блока
  идёт с префиксом `chain2:`.

## Что уже есть

Перед любой правкой ROO обязан установить и напечатать факт, а не пересказ. Для каждого пункта —
либо цитата с номером строки, либо `NOT FOUND`:

1. Греп по всему репозиторию (без `node_modules`, `.venv`, `__pycache__`) ключевых слов
   `streak`, `series`, `consecutive`, `серия`, `подряд`, `days_off`, `выходной`. Команда:
   `Select-String -Path *.py,routes\*.py,services\*.py,daily_tasks\*.py,templates\*.html -Pattern streak,series,consecutive,серия,подряд,days_off,выходной -SimpleMatch`. Каждое совпадение
   привести с файлом и номером строки. Если совпадений нет — `NOT FOUND` и вывод: системы серий
   в коде нет, создаётся с нуля.
2. Модель `User` в `models.py` — есть ли поля `current_streak`, `max_streak`, `days_off_available`,
   `last_solved_date` или поля с близкими именами. Полный список колонок `User` с типами и
   номерами строк объявления. Если ни одного поля серий нет — `NOT FOUND`.
3. Маршрут завершения набора задач дня в `daily_tasks/routes.py` — где определяется, что все
   `daily_task_items` набора отвечены, где считается доля правильных (или факт 100%).
   Привести номер строки и код проверки. Если факта 100% в коде нет — `NOT FOUND` и реализовать.
4. Место обновления `mu`/`sigma` при ответе — `services/theme_probe.py` (`record_answer`),
   `services/level_engine.py`. Привести номер строки вызова и подпись функции. Доказать, что
   выходной (пропуск задач) не доходит до этой функции — иначе `mu`/`sigma` сдвинутся.
5. Шаблон задачи дня (кандидат `templates/daily_task.html`) — есть ли в нём место для счётчика
   серии и кнопки, какая структура блока действий ученика. Привести номера строк блока кнопок.
6. Шаблон профиля `templates/profile.html` — где выводятся `mu`/`sigma`/уровень, есть ли место
   для блока серии. Привести номера строк.
7. Фикстуры `tests/conftest.py` — есть ли `daily_set_with_items`, сколько `daily_task_items`
   в фикстурном наборе, есть ли `test_user` с `id`. Если фикстуры нет — `NOT FOUND`.

## Что сделать

1. Дописать в `_recon/CHAIN_RUN.md` строку `## БЛОК T8 СЕРИЯ — СТАРТ` и время старта, до всякой
   работы. Это первое действие блока.
2. Сделать копию базы `instance/formyla.db` в `instance/formyla.db.bak_T8_pre` до миграции.
3. Если системы серий нет (пункт 1 «Что уже есть» дал `NOT FOUND`) — создать её с нуля. Если
   есть — изучить и дополнить, не ломая существующее. В отчёте явно указать, какой путь выбран.
4. Добавить модель `StreakRecord` в `models.py` с полями: `id` (PK), `user_id` (FK `users.id`,
   unique, один StreakRecord на пользователя), `current_streak` (int, default 0),
   `max_streak` (int, default 0), `days_off_available` (int, default 0),
   `last_solved_date` (date, nullable). Таблица `streak_records`. Уникальное ограничение по
   `user_id`.
5. Миграция `scripts/t8_migration.py` — `CREATE TABLE IF NOT EXISTS streak_records` с колонками
   модели. Перед запуском — копия базы из пункта 2. Миграция идемпотентна (`IF NOT EXISTS`).
6. Сервис `services/streak_service.py` с функциями:
   - `get_or_create_streak(user_id)` — возвращает `StreakRecord`, создаёт при отсутствии.
   - `check_streak_on_open(user_id, today)` — вызывается при открытии задач дня. Если
     `last_solved_date` не null и `last_solved_date < today - 1 день` (то есть был пропущен
     целый день без выходного) — `current_streak = 0`, `days_off_available = 0`. Если
     `last_solved_date == today` или `== yesterday` — серия цела, без изменений.
   - `complete_day(user_id, today, all_correct)` — вызывается после завершения набора задач
     дня. Если `all_correct` — `current_streak += 1`; если `current_streak % 3 == 0` —
     `days_off_available += 1` (награда: каждые 3 дня серии дают 1 выходной);
     `max_streak = max(max_streak, current_streak)`; `last_solved_date = today`. Если не
     `all_correct` — `current_streak = 0`, `days_off_available = 0`, `last_solved_date = today`.
   - `take_day_off(user_id, today)` — вызывается кнопкой «Взять выходной». Если
     `days_off_available <= 0` — отказ (возврат False, кнопка не должна была быть видна).
     Иначе `days_off_available -= 1`, `current_streak` без изменений, `last_solved_date = today`.
     `mu`/`sigma` не вызываются, в `level_engine` запись не идёт. Возврат True.
   - Определение `all_correct`: все `daily_task_items` текущего набора отвечены и каждый ответ
     оценён как правильный (+1). Частичный (0) или неверный (-2) — не 100%.
7. В месте завершения набора задач дня (`daily_tasks/routes.py` или `daily_tasks/services.py`,
   найденном в пункте 3 «Что уже есть») — после фиксации последнего ответа набора вызвать
   `streak_service.complete_day(user_id, today, all_correct)`. `all_correct` считается по
   фактическим результатам набора из базы, не из запроса.
8. При открытии задач дня — вызвать `streak_service.check_streak_on_open(user_id, today)` и
   `get_or_create_streak`, передать в шаблон `current_streak`, `days_off_available`.
9. Маршрут `POST /daily/take-day-off` в `daily_tasks/routes.py` — вызывает
   `streak_service.take_day_off`, редирект обратно. Доступ только если `days_off_available > 0`
   (серверная проверка, не только скрытие кнопки). При `days_off_available == 0` — возврат
   `403` или `400` с текстом на русском, без падения `500`.
10. В шаблоне задач дня — блок серии: «Серия: N дней» и, если `days_off_available > 0`, кнопка
    «Взять выходной» (radius 10px, токен `--accent`/`--accent-hi`, без эмодзи). Кнопка видна
    только при `days_off_available > 0`. Текст без эмодзи и без восклицательных знаков.
11. В шаблоне профиля `templates/profile.html` — блок серии: текущая серия, максимум, сколько
    выходных накоплено. Дизайн-токены из эталона, радиус карточки 14px.
12. Написать тесты в `tests/` (фикстуры Ф0, не пишут в `instance/formyla.db`):
    - `tests/test_t8_streak_accumulation.py` — 3 дня подряд 100% -> `current_streak == 3`,
      `days_off_available == 1`. Даты управляются прямым вызовом сервиса с параметром `today`.
    - `tests/test_t8_day_off_preserves.py` — взять выходной -> `current_streak` не изменился,
      `days_off_available` уменьшился на 1, `mu`/`sigma` пользователя не изменились.
    - `tests/test_t8_miss_resets.py` — пропуск дня без выходного (`check_streak_on_open` с
      `today` на 2 дня позже `last_solved_date`) -> `current_streak == 0`,
      `days_off_available == 0`.
    - `tests/test_t8_button_visibility.py` — через `app.test_client()`: кнопка «Взять выходной»
      в HTML присутствует при `days_off_available > 0` и отсутствует при `== 0`; маршрут
      `/daily/take-day-off` при `days_off_available == 0` возвращает не `500`.
13. Написать отчёт `_recon/T8.md` по форме из раздела «Отчёт».
14. Сделать коммит с префиксом `chain2:` (одно сообщение, например
    `chain2: T8 серия дней и выходные`).

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T8_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.

## ПРИЁМКА

Каждый пункт проверяется на фикстурах из `tests/conftest.py`, не на живой базе. Команда идёт
целиком, вывод — как есть.

### 1. Греп существующей системы серий

```powershell
Select-String -Path *.py,routes\*.py,services\*.py,daily_tasks\*.py,templates\*.html -Pattern streak,series,consecutive,серия,подряд,days_off,выходной -SimpleMatch
```

Ожидаемый вывод: либо построчный список совпадений с файлом и номером строки (если система
серий уже была — приводится дословно), либо пустой вывод, что фиксируется в отчёте как
`NOT FOUND — системы серий в коде нет, создана с нуля`.

### 2. Копия базы перед миграцией

```powershell
Copy-Item instance\formyla.db instance\formyla.db.bak_T8_pre
Get-Item instance\formyla.db.bak_T8_pre | Select-Object Name, Length
```

Ожидаемый вывод: файл `instance\formyla.db.bak_T8_pre`, `Length` больше 0.

### 3. Миграция создала таблицу streak_records

```powershell
python -c "import sqlite3;c=sqlite3.connect('instance/formyla.db');print([r for r in c.execute('SELECT name FROM sqlite_master WHERE type=\"table\" AND name=\"streak_records\"')]);print([r[1] for r in c.execute('PRAGMA table_info(streak_records)')])"
```

Ожидаемый вывод: строка с `streak_records` в первой выборке и список колонок
`['id', 'user_id', 'current_streak', 'max_streak', 'days_off_available', 'last_solved_date']`
во второй. Если колонок не хватает — находка, блок `ЧАСТИЧНО`.

### 4. 3 дня подряд 100% -> streak=3, days_off=1

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t8_streak_accumulation.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест через `test_user` вызывает
`streak_service.complete_day(user_id, date(2026,8,1), True)`,
`complete_day(..., date(2026,8,2), True)`,
`complete_day(..., date(2026,8,3), True)` и утверждает `current_streak == 3`,
`days_off_available == 1`, `max_streak == 3`.

### 5. Взять выходной сохраняет серию, не трогает mu/sigma

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t8_day_off_preserves.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест: после 3 дней 100% (`current_streak == 3`,
`days_off_available == 1`) вызывает `take_day_off(user_id, date(2026,8,4))`, утверждает
`current_streak == 3` (не изменился), `days_off_available == 0`, и `mu`/`sigma` пользователя
равны значениям до вызова (сравнение до и после).

### 6. Пропуск без выходного обнуляет серию

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t8_miss_resets.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест: `current_streak == 3`, `last_solved_date == date(2026,8,3)`,
вызывает `check_streak_on_open(user_id, date(2026,8,5))` (два дня спустя, без выходного),
утверждает `current_streak == 0`, `days_off_available == 0`.

### 7. Кнопка видна только при days_off_available > 0, маршрут не падает 500

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t8_button_visibility.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест через `auth_client`: при `days_off_available == 0` HTML
задач дня не содержит подстроки `Взять выходной`, а POST на `/daily/take-day-off` возвращает
код не `500` (конкретный код фиксируется в отчёте). При `days_off_available > 0` кнопка в HTML
присутствует.

### 8. Блок серии в профиле

```python
text = open('templates/profile.html', encoding='utf-8').read()
print('HAS_STREAK_BLOCK', 'Серия' in text or 'серия' in text or 'streak' in text.lower())
print('HAS_MAX', 'макс' in text.lower() or 'max' in text.lower())
```

Ожидаемый вывод: `HAS_STREAK_BLOCK True`, `HAS_MAX True`. Если `False` — находка, добавить блок,
повторить команду, привести новый вывод.

### 9. Полный прогон приёмочных тестов T8

```powershell
python -m pytest tests/test_t8_streak_accumulation.py tests/test_t8_day_off_preserves.py tests/test_t8_miss_resets.py tests/test_t8_button_visibility.py --tb=short -q
```

Ожидаемый вывод: `4 passed in <T>s`, ноль `failed`, ноль `errors`. Меньше четырёх — статус
`ЧАСТИЧНО`.

### 10. Полный прогон всего набора, сверка с базовой линией

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`. Рост `failed` или
`errors` против базовой линии `939/37/17/0` — жёсткий стоп, блок `ЧАСТИЧНО`, без разбора причины
дальнейшие изменения не коммитятся.

## Отчёт

Писать в `_recon/T8.md`. Обязательные разделы:

1. Заголовок и дата.
2. Строка СТАРТ из `_recon/CHAIN_RUN.md` — дословно.
3. Раздел «Ревизия до правок» — построчный факт по всем семи пунктам «Что уже есть», включая
   вывод грепа из пункта 1 (совпадения или `NOT FOUND`) и список колонок `User`.
4. Результат копии базы (пункт 2 ПРИЁМКИ).
5. Результат миграции (пункт 3 ПРИЁМКИ) — список колонок `streak_records`.
6. Вывод всех десяти пунктов ПРИЁМКИ — команда, фактический вывод, вердикт пройдено/не пройдено.
7. Таблица правил серии: условие -> действие над `current_streak`, `days_off_available`,
   `last_solved_date`, `mu`/`sigma`. Строки: 100% день; не 100% день; выходной взят; пропуск
   без выходного. Для `mu`/`sigma` во всех строках — «не меняются».
8. Список изменённых и созданных файлов с числом строк каждого.
9. Раздел «Не сделано / NOT FOUND» — буквально, без подмены слов.

Запрещённые слова в отчёте (примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается) недопустимы, кроме этого же
перечисления. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 08 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 08 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 08 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 07 КОНЕЦ>>>

---

<<<БЛОК 08 НАЧАЛО>>>

# БЛОК 08 — T7 — ПЛАН НА МЕСЯЦ 2+: авто-продление

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 08 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 08 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 08 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, рабочая база `instance/formyla.db`. Прод и Render не трогаем,
  Auto-Deploy остаётся OFF. База пуста (`adaptive_tasks` = 0, `users` = 0) — норма, приёмка идёт
  на фикстурах из `tests/conftest.py` (`app`, `auth_client`, `test_user`), созданных блоком Ф0.
- Базовая линия тестов после прогона v2: `passed=939 failed=37 skipped=17 errors=0`. Рост `failed`
  или `errors` относительно этой линии — жёсткий стоп, блок помечается `ЧАСТИЧНО`.
- Куратор назначает 7 подтем на месяц. Якоря: `algebra -> number_theory -> geometry ->
  combinatorics -> logic` (`data/anchors.jsonl`, 35 якорей, level = 3, перезапись запрещена).
  Подтемы — более мелкие единицы внутри якорей. Этот блок структуру подтем не правит, работает с
  планом как со списком строк. Механика: `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`,
  `mu` в [1, 5], `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`; правильный +1, частичный 0, неверный
  -2; формулы не трогать.
- Задачи дня и ротация: `daily_tasks/services.py` (2326), `daily_tasks/routes.py` (929),
  `services/daily_task_rotation.py` (1055), `services/daily_quest_service.py` (716),
  `services/level_engine.py` (265). Планировщики: APScheduler в `app.py`, Celery и Redis в
  `tasks/daily_pool.py` (281) — подтвердить заново, не принимать на веру. Маршруты куратора
  искать по `app.py` и `routes/` (grep `curator`, `admin`, `plan`, `подтем`, `месяц`, `month`).
  Профиль ученика: `templates/profile.html`. Шаблоны админки — `templates/admin/`.
- Эталон дизайна: токены `--bg:#070C18 --surface:#0E1830 --surface-2:#121F3C --border:#1C2B4F
  --text:#E6EBF7 --muted:#8C9ABC --accent:#4C7DFF --accent-hi:#6B95FF --ok:#3ECF8E --warn:#E5AC3A
  --bad:#E86A62`; радиус 14px/10px, Satoshi+system-ui, контейнер 1120px, без эмодзи.

## Что уже есть

Перед правкой ROO обязан установить и напечатать факт, а не пересказ. Для каждого пункта —
цитата с номером строки или `NOT FOUND`:

1. Греп по репозиторию (без `node_modules`, `.venv`, `__pycache__`) ключевых слов `curator`,
   `curator_plan`, `plan`, `subtopic`, `подтем`, `месяц`, `month`, `advance`, `автопродолж`.
   Команда:
   `Select-String -Path *.py,routes\*.py,services\*.py,daily_tasks\*.py,templates\*.html -Pattern curator,plan,subtopic,подтем,месяц,month,advance -SimpleMatch`.
   Каждое совпадение — файл и номер строки. Если совпадений нет — `NOT FOUND`, план куратора
   отсутствует, создаётся с нуля.
2. Поиск таблицы в схеме `instance/formyla.db`: `SELECT name FROM sqlite_master WHERE type='table'
   AND name LIKE '%curator%' OR name LIKE '%plan%' OR name LIKE '%subtopic%'`. Если ни одной
   таблицы — `NOT FOUND`.
3. Модель `User` в `models.py` — есть ли поля `current_month`, `study_month`,
   `month_started_at`, `curator_month` или близкие. Полный список колонок `User` с типами и
   номерами строк. Если поля месяца нет — `NOT FOUND`.
4. Маршрут назначения подтем куратором — grep `@app.route`/`@bp.route` со словами `curator`,
   `plan`, `assign`, `subtopic` в `app.py` и `routes/`. Привести путь, метод, номер строки. Если
   маршрута нет — `NOT FOUND`.
5. Логика смены месяца — grep `advance`, `next_month`, `rotate`, `month_change`,
   `new_month`. Есть ли функция, которая при наступлении нового месяца берёт следующий блок из 7
   подтем. Если нет — `NOT FOUND`, автовыполнение отсутствует, реализуется этим блоком.
6. Место, где задачи дня выбираются по подтеме месяца — как текущий месяц влияет на выдачу. Если
   связи месяца с выдачей нет — `NOT FOUND`.
7. Фикстуры `tests/conftest.py` — есть ли `test_user`, `auth_client`, фикстуры плана куратора.
   Если нет — `NOT FOUND`, тесты создают свои данные.

## Что сделать

1. Дописать в `_recon/CHAIN_RUN.md` строку `## БЛОК T7 ПЛАН — СТАРТ` и время старта, до всякой
   работы. Первое действие блока.
2. Сделать копию базы `instance/formyla.db` в `instance/formyla.db.bak_T7_pre` до миграции.
3. Если плана куратора нет (пункты 1-2 «Что уже есть» дали `NOT FOUND`) — создать с нуля. Если
   есть — изучить и дополнить, не ломая существующее. В отчёте явно указать выбранный путь.
4. Добавить в `models.py`:
   - `CuratorPlanItem` — глобальный шаблон плана, задаётся куратором один раз. Поля: `id` (PK),
     `subtopic` (text, название подтемы), `month_number` (int, номер месяца, начиная с 1),
     `position` (int, позиция внутри месяца, 1-7), `created_at`. Таблица `curator_plan_items`.
     Уникальное ограничение `(month_number, position)`.
   - `UserSubtopicAssignment` — активные назначения конкретного ученика. Поля: `id` (PK),
     `user_id` (FK `users.id`), `subtopic` (text), `month_number` (int), `position` (int),
     `assigned_at`. Таблица `user_subtopic_assignments`. Уникальное ограничение
     `(user_id, month_number, position)` — основа идемпотентности.
   - Поле `User.current_month` (int, default 1) — текущий учебный месяц ученика.
5. Миграция `scripts/t7_migration.py` — `CREATE TABLE IF NOT EXISTS` для обеих таблиц и
   `ALTER TABLE users ADD COLUMN current_month INTEGER DEFAULT 1` (с проверкой существования
   колонки, чтобы миграция была идемпотентна). Перед запуском — копия базы из пункта 2.
6. Сервис `services/curator_plan_service.py` с функциями:
   - `set_plan(items)` — куратор передаёт полный список подтем по месяцам (список кортежей
     `(subtopic, month_number, position)`). Очищает `curator_plan_items` и заполняет заново
     (или UPSERT). Используется один раз при настройке.
   - `get_active_subtopics(user_id)` — возвращает 7 подтем для `User.current_month` из
     `user_subtopic_assignments`. Если назначений для текущего месяца нет — возвращает пустой
     список и выставляет флаг `plan_missing=True`.
   - `activate_month(user_id, month_number)` — берёт из `curator_plan_items` все строки с
     `month_number == N` и вставляет их в `user_subtopic_assignments` для пользователя. Вставка
     через `INSERT ... ON CONFLICT(user_id, month_number, position) DO NOTHING` (или эквивалент
     SQLAlchemy) — идемпотентность: повторный запуск не создаёт дублей. Если в
     `curator_plan_items` нет строк для этого месяца — пишет в лог запись
     `план на месяц N не задан` и возвращает флаг `plan_missing=True`.
   - `advance_study_month(user_id)` — `User.current_month += 1`, вызывает
     `activate_month(user_id, new_month)`. Если `plan_missing` — лог и флаг для куратора.
     Идемпотентность: повторный вызов для того же нового месяца не дублирует назначения
     (защита уникальным ограничением), а `current_month` повторно не инкрементируется, если
     месяц уже активирован (проверка: есть ли назначения для `current_month` — если есть и
     `current_month` уже равен новому, повторного инкремента нет).
   - `check_plan_status()` — для куратора: возвращает список месяцев, для которых в
     `curator_plan_items` нет 7 строк (неполные или отсутствующие), и список учеников с
     `plan_missing=True`.
7. Маршрут куратора `GET/POST /curator/plan` — форма ввода полного плана (по месяцам, по 7
   подтем в каждом). POST вызывает `set_plan`. Только для роли куратора (проверка роли, не
   доверять скрытию ссылки). Шаблон `templates/admin/curator_plan.html`.
8. Маршрут куратора `GET /curator/plan-status` — показывает `check_plan_status()`: какие месяцы
   заданы, какие отсутствуют, у каких учеников `plan_missing`. Текст «план на следующий месяц не
   задан» выводится для отсутствующих.
9. В месте выдачи задач дня (пункте 6 «Что уже есть») — использовать
   `get_active_subtopics(user_id)` для выбора подтем текущего месяца. Если связи месяца с выдачей
   не было — добавить вызов `get_active_subtopics` в точку выбора подтемы.
10. Триггер смены месяца — через APScheduler (найденный в `app.py`) или вызов
    `advance_study_month` при открытии задач дня, если с момента `month_started_at` прошло 30
    дней (или по календарной границе — фактическое правило зафиксировать в отчёте с
    обоснованием по коду).
11. Написать тесты в `tests/` (фикстуры Ф0, не пишут в `instance/formyla.db`):
    - `tests/test_t7_plan_three_months.py` — `set_plan` с 21 подтемой (3 месяца по 7). Для
      `test_user` (`current_month == 1`) вызывает `activate_month(user_id, 1)` -> назначения
      1-7. `advance_study_month` -> `current_month == 2`, назначения 8-14. Ещё раз ->
      `current_month == 3`, назначения 15-21. На каждом шаге ровно 7 строк.
    - `tests/test_t7_idempotent.py` — дважды вызвать `activate_month(user_id, 2)` и утверждать,
      что в `user_subtopic_assignments` для `(user_id, 2)` ровно 7 строк, дублей нет. Проверка
      через `SELECT ... GROUP BY user_id, month_number, position` — ни одна группа не больше 1.
    - `tests/test_t7_missing_month.py` — `set_plan` с планом на 3 месяца. Вызвать
      `advance_study_month` три раза (`current_month` доходит до 4, плана на 4 нет). Утвердить:
      `get_active_subtopics` пустой список, `plan_missing == True`, в логе есть подстрока
      `план на месяц 4 не задан`, на `/curator/plan-status` виден текст «план на следующий месяц
      не задан».
    - `tests/test_t7_curator_route.py` — через `auth_client` с ролью куратора: `GET /curator/plan`
      возвращает 200, `POST /curator/plan` с 21 подтемой сохраняет план (21 строка в
      `curator_plan_items`), `GET /curator/plan-status` возвращает 200 и для отсутствующего месяца
      содержит подстроку `план на следующий месяц не задан`.
12. Написать отчёт `_recon/T7.md` по форме из раздела «Отчёт» и сделать коммит с префиксом
    `chain2:` (например `chain2: T7 план на месяц 2+ автовыполнение`).

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T7_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.

## ПРИЁМКА

Каждый пункт проверяется на фикстурах из `tests/conftest.py`, не на живой базе. Команда идёт целиком, вывод — как есть.

### 1. Греп существующего плана куратора

```powershell
Select-String -Path *.py,routes\*.py,services\*.py,daily_tasks\*.py,templates\*.html -Pattern curator,plan,subtopic,подтем,месяц,month,advance -SimpleMatch
```

Ожидаемый вывод: построчный список совпадений (если план был — дословно), либо пустой вывод,
что фиксируется как `NOT FOUND — плана куратора в коде нет, создан с нуля`.

### 2. Копия базы перед миграцией

```powershell
Copy-Item instance\formyla.db instance\formyla.db.bak_T7_pre
Get-Item instance\formyla.db.bak_T7_pre | Select-Object Name, Length
```

Ожидаемый вывод: файл `instance\formyla.db.bak_T7_pre`, `Length` больше 0.

### 3. Миграция создала таблицы и колонку

```powershell
python -c "import sqlite3;c=sqlite3.connect('instance/formyla.db');print([r[0] for r in c.execute('SELECT name FROM sqlite_master WHERE type=\"table\" AND name IN (\"curator_plan_items\",\"user_subtopic_assignments\")')]);print([r[1] for r in c.execute('PRAGMA table_info(users)') if r[1]=='current_month'])"
```

Ожидаемый вывод: список из двух таблиц `['curator_plan_items', 'user_subtopic_assignments']` и
`['current_month']`. Если чего-то нет — находка, блок `ЧАСТИЧНО`.

### 4. План на 3 месяца: месяц 1 -> подтемы 1-7, месяц 2 -> 8-14, месяц 3 -> 15-21

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t7_plan_three_months.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест: `set_plan` с 21 подтемой (3 месяца по 7). После
`activate_month(user_id, 1)` — `get_active_subtopics` возвращает позиции 1-7. После
`advance_study_month` — `current_month == 2`, позиции 8-14. После второго — `current_month == 3`,
позиции 15-21. На каждом шаге ровно 7 строк.

### 5. Идемпотентность: повторный запуск не создаёт дублей

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t7_idempotent.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест: дважды вызывает `activate_month(user_id, 2)` (или
`advance_study_month` дважды до одного и того же месяца с контролем), утверждает, что в
`user_subtopic_assignments` для `(user_id, 2)` ровно 7 строк, а запрос
`SELECT user_id, month_number, position, COUNT(*) ... GROUP BY ...` не даёт ни одной группы с
`COUNT > 1`.

### 6. Отсутствующий месяц: лог и флаг, пустой список, текст куратору

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t7_missing_month.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест: план на 3 месяца, `advance_study_month` до `current_month
== 4` (плана на 4 нет). Утверждает: `get_active_subtopics` пустой список, `plan_missing ==
True`, в логе подстрока `план на месяц 4 не задан`.

### 7. Маршрут куратора: план и статус

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t7_curator_route.py', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест через `auth_client` с ролью куратора: `GET /curator/plan`
возвращает `200`, `POST /curator/plan` с 21 подтемой сохраняет план (21 строка в
`curator_plan_items`), `GET /curator/plan-status` возвращает `200` и для отсутствующего месяца
содержит подстроку `план на следующий месяц не задан`.

### 8. Колонка current_month в User

```powershell
python -c "import sqlite3;c=sqlite3.connect('instance/formyla.db');print([r for r in c.execute('PRAGMA table_info(users)') if r[1]=='current_month'])"
```

Ожидаемый вывод: одна строка с колонкой `current_month`, тип `INTEGER`, `default 1`. Если
колонки нет — `NOT FOUND`, блок `ЧАСТИЧНО`.

### 9. Полный прогон приёмочных тестов T7

```powershell
python -m pytest tests/test_t7_plan_three_months.py tests/test_t7_idempotent.py tests/test_t7_missing_month.py tests/test_t7_curator_route.py --tb=short -q
```

Ожидаемый вывод: `4 passed in <T>s`, ноль `failed`, ноль `errors`. Меньше четырёх — статус
`ЧАСТИЧНО`.

### 10. Полный прогон всего набора, сверка с базовой линией

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <N> failed, <N> skipped, <N> errors in <T>s`. Рост `failed` или
`errors` против базовой линии `939/37/17/0` — жёсткий стоп, блок `ЧАСТИЧНО`, изменения не
коммитятся без разбора причины.

## Отчёт

Писать в `_recon/T7.md`. Обязательные разделы:

1. Заголовок и дата.
2. Строка СТАРТ из `_recon/CHAIN_RUN.md` — дословно.
3. Раздел «Ревизия до правок» — построчный факт по всем семи пунктам «Что уже есть», включая
   вывод грепа (совпадения или `NOT FOUND`), результат поиска таблиц в схеме, список колонок
   `User` до миграции.
4. Результат копии базы (пункт 2 ПРИЁМКИ).
5. Результат миграции (пункт 3 ПРИЁМКИ) — список таблиц и колонка `current_month`.
6. Решение о правиле смены месяца (пункт 10 «Что сделать») — какое правило выбрано (30 дней от
   `month_started_at` или календарная граница), с обоснованием по коду APScheduler.
7. Вывод всех десяти пунктов ПРИЁМКИ — команда, фактический вывод, вердикт пройдено/не пройдено.
8. Таблица: месяц -> список позиций подтем (1: 1-7, 2: 8-14, 3: 15-21, 4: отсутствует).
9. Список изменённых и созданных файлов с числом строк каждого.
10. Раздел «Не сделано / NOT FOUND» — буквально, без подмены слов.

Запрещённые слова в отчёте (примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается) недопустимы, кроме этого же
перечисления. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 09 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 09 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 09 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 08 КОНЕЦ>>>

---

<<<БЛОК 09 НАЧАЛО>>>

# БЛОК 09 — T10 — РОДИТЕЛЬ/УЧИТЕЛЬ + ГРУППЫ + ДАШБОРД

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 09 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 09 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 09 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Рабочая база пуста (`adaptive_tasks` = 0, `users` = 0) — это норма, приёмка идёт на
  фикстурах из `tests/conftest.py`, созданных блоком Ф0, а не на живых данных. Единственная запись в
  рабочую базу — однократная миграция схемы (пункт 2 «Что сделать»), и только после создания копии
  `instance/formyla.db.bak_T10_pre`, как требует регламент.
- Роли пользователей — существующие записи `User`, к которым добавляется поле `role` со значениями
  `teacher`, `parent`, `student` (по умолчанию `student`). Учитель может иметь несколько групп.
  Родитель привязан ровно к одному ребёнку по его email: на учётной записи родителя хранится
  `child_email`, по которому ищется запись ребёнка в `users`. Если ребёнка с таким email нет —
  дашборд родителя показывает `NOT FOUND`, без ошибки.
- Механика и формулы уровня не меняются: `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`,
  `mu` в [1, 5], `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`. Ответ в срезе: верный +1, частичный 0,
  неверный −2. Пять якорей в порядке `algebra`, `number_theory`, `geometry`, `combinatorics`,
  `logic`. Эти формулы блок только читает при выводе последних `mu`/`sigma` по якорям ученика, не
  пересчитывает.
- Блок опирается на две функции из предыдущих слоёв: `display_name_from_email` (блок Т2) — формирует
  отображаемое имя ученика из email; `streak` (блок Т8) — считает число дней подряд, в которые
  ученик закрыл дневное задание на 100 процентов. Обе функции блок обязан сначала найти в коде и
  использовать как есть; если хоть одной нет — писать `NOT FOUND` и реализовать локальный эквивалент
  в новом модуле `services/parent_teacher_helpers.py`, отметив это в отчёте как отклонение.
- Порог дневной активности для дашборда учителя — четыре задачи: среднее за неделю `>= 4` — цвет
  строки зелёный (токен `--ok:#3ECF8E`), `< 4` — красный (токен `--bad:#E86A62`). Среднее количество
  решённых задач дня по группе считается за последние семь дней (скользящее окно).
- Подписка проекта — 400 рублей в месяц с ученика — только фон бизнес-модели, на логику групп не
  влияет; платный контур в этом блоке не трогается.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус карточек 14px, кнопок 10px, шрифт Satoshi и system-ui,
  контейнер 1120px, тёмно-синий стиль, ни одного эмодзи. Посторонних hex-цветов в шаблонах быть не
  должно, все цвета берутся из переменных.

## Что уже есть

Перед правкой ROO обязан зафиксировать в отчёте фактическое состояние, с цитатами кода и номерами
строк:

1. Модель `User` в `models.py` — полный список колонок, есть ли уже `role`, `child_email`,
   `share_progress`, их типы и значения по умолчанию, номера строк. Нет поля — `NOT FOUND` для него.
2. Есть ли уже модели `Group` и `GroupMember` — колонки и номера строк; нет — `NOT FOUND` для обеих.
3. Функция `display_name_from_email` — файл, номер строки, сигнатура, тело целиком. Нет — `NOT FOUND`.
4. Функция `streak` — файл, номер строки, сигнатура, тело целиком. Нет — `NOT FOUND`.
5. Маршруты `/teacher`, `/parent`, `/profile` — выписать из `app.url_map.iter_rules()`; нет — `NOT FOUND`.
6. Фикстуры в `tests/conftest.py` по ролям (`test_user`, `auth_client` и прочие) — полный список имён
   с `@pytest.fixture`.
7. Как фиксируется решённая задача дня — таблица и колонка статуса (`daily_task_items` /
   `solution_attempts` / иное), какой признак считать «решённой в этот день».
8. Шаблоны в `templates/teacher/` и `templates/parent/` — нет директорий или файлов, `NOT FOUND`.

## Что сделать

1. Перед любой миграцией создать копию рабочей базы: скопировать `instance/formyla.db` в
   `instance/formyla.db.bak_T10_pre`. Если файл базы отсутствует — создать `instance/` и пустую базу,
   затем копию. Копия обязательна до `ALTER TABLE` и `CREATE TABLE` из пунктов 2-3.

2. Добавить в модель `User` три поля, если их ещё нет: `role` (строка, значения `teacher`, `parent`,
   `student`, по умолчанию `student`, `nullable=False`), `child_email` (строка, `nullable=True`, для
   роли `parent`), `share_progress` (булево, по умолчанию `True`, `nullable=False`). Миграция —
   `ALTER TABLE users ADD COLUMN role TEXT NOT NULL DEFAULT 'student'`,
   `ALTER TABLE users ADD COLUMN child_email TEXT`,
   `ALTER TABLE users ADD COLUMN share_progress BOOLEAN NOT NULL DEFAULT 1`. Колонка уже есть —
   пропускать, не падать.

3. Создать модели `Group` (`id`, `name` — не пустая строка, `teacher_id` — FK на `users.id`,
   `invite_code` — строка ровно шесть символов, уникальный, `created_at`) и `GroupMember` (`id`,
   `group_id` — FK на `groups.id`, `user_id` — FK на `users.id`, `role` — по умолчанию `'student'`,
   `joined_at`). Уникальный индекс `(group_id, user_id)`. Миграция — `CREATE TABLE groups` и
   `CREATE TABLE group_members`.

4. Реализовать генерацию `invite_code` в новом модуле `services/parent_teacher_helpers.py`: шесть
   символов из заглавных букв и цифр (без `O`, `0`, `I`, `1`), проверка уникальности по таблице
   `groups` с повторной генерацией при совпадении. Вызывается при создании группы в маршруте.

5. В том же модуле реализовать две функции, если готовых из блоков Т2 и Т8 нет (если есть —
   использовать их и не дублировать): `display_name_from_email(email)` — имя для вывода: часть email
   до `@` с заглавной первой буквой, пустой email — `NOT FOUND`; `student_streak(user_id)` — число
   дней подряд, в которые ученик закрыл дневное задание на 100 процентов (пять верных из пяти
   позиций набора), считая от сегодняшнего дня назад, нет дней — `0`. Опирается на признак
   «решённой» задачи из пункта 7 «Что уже есть». Если `streak` из блока Т8 совместима по сигнатуре —
   вызывать её.

6. Маршруты (новый blueprint `routes/parent_teacher.py`, регистрируется в `app.py` через
   `app.register_blueprint`):
   - `GET /teacher` — список групп учителя и форма создания; роль не `teacher` — `abort(403)`.
   - `POST /teacher/group/create` — принимает `name`, создаёт группу с `invite_code`, редирект на
     `/teacher`.
   - `GET /teacher/group/<int:gid>` — дашборд группы: ученики (имена из `display_name_from_email`,
     число решённых задач дня за семь дней, цвет по порогу четыре, среднее по группе, последние
     `mu`/`sigma` по якорям, серия `student_streak`). Чужая или отсутствующая группа — `NOT FOUND`
     (`404`).
   - `POST /teacher/group/<int:gid>/rename` — новое `name`, редирект на `/teacher/group/<gid>`.
   - `POST /teacher/group/<int:gid>/delete` — каскадное удаление группы и `GroupMember`, редирект
     на `/teacher`.
   - `GET /parent` — ищет `User` с `email == current_user.child_email`; нет ребёнка — страница
     `NOT FOUND` без `500`; `share_progress` ребёнка `False` — страница «Ученик закрыл доступ».
   - `POST /profile/join-group` — принимает `invite_code`, создаёт `GroupMember` с `role='student'`,
     редирект на `/profile`. Неверный код — `NOT FOUND` (`404` или `400`).
   - `GET /student/<int:sid>` — детальная страница ученика: прогресс по темам, история срезов
     (последние `mu`/`sigma` по якорям), серия `student_streak`. Доступ только учителю этого ученика
     (через `GroupMember`) или родителю с совпадающим `child_email`; прочим — `403`. При
     `share_progress=False` и запросителе не ученике — `403`.

7. Шаблоны: `templates/teacher/dashboard.html` (список групп, форма создания, ссылки на группы),
   `templates/teacher/group.html` (таблица учеников с цветами порога, среднее по группе, код
   приглашения, кнопки переименовать и удалить, ссылки на профили), `templates/parent/dashboard.html`
   (прогресс одного ребёнка, без списка группы). В `templates/student/profile_detail.html` —
   переключатель `share_progress` (да/нет) и поле ввода кода группы. В существующий шаблон профиля
   ученика добавить блок «моя группа, мой учитель, мой прогресс» (имя группы и имя учителя из
   `display_name_from_email`, иначе «Вы не состоите в группе»), форму `POST /profile/share` с полем
   `share_progress` и форму `POST /profile/join-group`. Все цвета — из переменных дизайна, посторонних
   hex в разметке нет.

8. Дашборд учителя считает: среднее количество решённых задач дня по группе за последние семь
   дней (сумма решённых по всем ученикам за семь дней, делённая на семь и на число учеников), по
   каждому ученику — сумма решённых задач дня за семь дней, цвет строки зелёный при среднем за
   неделю `>= 4`, красный при `< 4` (порог применяется к среднему за неделю ученика, не к одному дню
   — зафиксировать комментарием на русском без эмодзи). Последние `mu`/`sigma` по якорям берутся из
   фактической таблицы срезов (последняя запись каждого якоря для ученика), серия — из
   `student_streak`.

9. Фикстуры в `tests/conftest.py` (дополнить, не перезаписывая фикстуры Ф0): `teacher_user` (роль
   `teacher`), `parent_user` (роль `parent`, `child_email` равен email первого ученика),
   `student_users` (список из трёх учеников с ролью `student`, email вида
   `student_t10_1@example.invalid`, `..._2`, `..._3`), `group_with_students` (зависит от
   `teacher_user` и `student_users`, создаёт одну группу и три `GroupMember`).

10. Тесты в `tests/test_t10_groups.py` на фикстурах из пункта 9: создание группы (`invite_code`
    длиной шесть), вход ученика по коду, переименование и удаление группы, дашборд учителя показывает
    трёх учеников, дашборд родителя показывает одного ребёнка, ученик без группы не видит дашборд
    группы (статус не `500`), ученик с `share_progress=False` закрывает доступ, неверный код —
    `NOT FOUND`.

11. Сверить дизайн-токены в трёх новых шаблонах с эталоном — цвета переменных, радиус карточек
    14px, кнопок 10px, отсутствие эмодзи. Несовпадения перечислить и исправить.

12. Коммит `git add -A` и `git commit -m "chain2: T10 родитель учитель группы"` (префикс `chain2:`
    обязателен). Push в `main` не делать.

13. Написать отчёт `_recon/T10.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T10_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.

## ПРИЁМКА

Каждый пункт проверяется на фикстурах из `tests/conftest.py` (Ф0 и Т10), не на живой базе.
Команда запускается целиком, вывод — как есть. Для HTTP-проверок сессия поднимается через
`session_transaction()` с `_user_id` равным строковому `id` фикстурного пользователя.

### 1. Копия базы перед миграцией

```powershell
python -c "import os; p='instance/formyla.db.bak_T10_pre'; print('BAK_EXISTS', os.path.exists(p))"
```

Ожидаемый вывод: `BAK_EXISTS True`. `False` — миграция без копии, статус ЧАСТИЧНО.

### 2. Модели Group и GroupMember появились, поля User добавлены

```python
import app as A
with A.app.app_context():
    import models as M
    print('GROUP', hasattr(M, 'Group'), [c.name for c in M.Group.__table__.columns])
    print('MEMBER', hasattr(M, 'GroupMember'), [c.name for c in M.GroupMember.__table__.columns])
    print('USER_ROLE', 'role' in M.User.__table__.columns)
    print('USER_CHILD', 'child_email' in M.User.__table__.columns)
    print('USER_SHARE', 'share_progress' in M.User.__table__.columns)
```

Ожидаемый вывод: `GROUP True ['id', 'name', 'teacher_id', 'invite_code', 'created_at']`,
`MEMBER True ['id', 'group_id', 'user_id', 'role', 'joined_at']`, `USER_ROLE True`,
`USER_CHILD True`, `USER_SHARE True`. Любое `False` — статус ЧАСТИЧНО.

### 3. Создание группы учителем генерирует код длиной шесть

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t10_groups.py::test_teacher_creates_group_code_len', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест создаёт группу от лица `teacher_user` и утверждает, что
`invite_code` имеет длину ровно шесть и состоит из разрешённых символов.

### 4. CRUD группы: переименование и удаление

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t10_groups.py::test_group_rename_and_delete', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест переименовывает группу, проверяет новое имя, удаляет группу и
утверждает, что `GroupMember` для неё не осталось и сама группа отсутствует.

### 5. Ученик входит в группу по коду приглашения

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t10_groups.py::test_student_joins_by_code', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест отправляет `POST /profile/join-group` от лица ученика без группы и
утверждает создание `GroupMember` с `role='student'` и редирект на `/profile`.

### 6. Дашборд учителя показывает трёх учеников

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = str(A.models.User.query.filter_by(role='teacher').first().id)
r = c.get('/teacher', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1500])
```

Ожидаемый вывод: `STATUS 200`, длина тела больше нуля, в HTML присутствуют имена всех трёх учеников
(часть email до `@`) и имя группы. В HTML нет трёх имён — статус ЧАСТИЧНО. В отчёт — фактический
фрагмент HTML дословно.

### 7. Дашборд родителя показывает одного ребёнка

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = str(A.models.User.query.filter_by(role='parent').first().id)
r = c.get('/parent', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1500])
```

Ожидаемый вывод: `STATUS 200`, длина больше нуля, в HTML присутствует имя ребёнка по `child_email`
и нет имён других учеников. Родитель не видит ребёнка или видит чужого — статус ЧАСТИЧНО.

### 8. Ученик без группы не видит дашборд группы

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = str(A.models.User.query.filter_by(role='student', child_email=None).first().id)
r = c.get('/teacher', follow_redirects=True)
print('STATUS', r.status_code)
r2 = c.get('/teacher/group/1', follow_redirects=True)
print('STATUS2', r2.status_code)
```

Ожидаемый вывод: `STATUS 403` (ученик не учитель), `STATUS2 403` или `404` (чужая группа). Код `500`
недопустим — статус ЧАСТИЧНО.

### 9. Ученик закрывает доступ, родитель получает заглушку

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t10_groups.py::test_share_progress_off_blocks_parent', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест ставит `share_progress=False`, заходит на `/parent` от лица
родителя и утверждает, что в теле нет числовых данных прогресса, а есть заглушка «Ученик закрыл
доступ».

### 10. Неверный код приглашения — NOT FOUND

```python
import pytest, sys
sys.exit(pytest.main(['-q', 'tests/test_t10_groups.py::test_wrong_invite_code_not_found', '--tb=short']))
```

Ожидаемый вывод: `1 passed`. Тест отправляет `POST /profile/join-group` с кодом `ZZZZZZ`, которого
нет в базе, и утверждает код ответа не `200` и не `500` (`404` или `400`) и наличие в теле текста
`NOT FOUND`.

### 11. Дамп базы: группы и участники на фикстуре

```powershell
python -c "import app as A; A.app.app_context().push(); import models as M; [print('GROUP', g.id, g.name, g.invite_code) for g in M.Group.query.all()]; [print('MEMBER', m.group_id, m.user_id, m.role) for m in M.GroupMember.query.all()]"
```

Ожидаемый вывод: одна строка `GROUP 1 <имя> <код6>` и три строки `MEMBER 1 <uid> student` (по одной на
каждого ученика). Если строк меньше — статус ЧАСТИЧНО.

### 12. Дизайн-токены в трёх новых шаблонах совпадают с эталоном

```python
tokens = ['#070C18', '#0E1830', '#121F3C', '#1C2B4F', '#E6EBF7', '#8C9ABC',
          '#4C7DFF', '#6B95FF', '#3ECF8E', '#E5AC3A', '#E86A62']
for fname in ('templates/teacher/dashboard.html', 'templates/teacher/group.html', 'templates/parent/dashboard.html'):
    text = open(fname, encoding='utf-8').read()
    missing = [t for t in tokens if t not in text]
    print(fname, 'MISSING_TOKENS', missing if missing else 'NONE')
```

Ожидаемый вывод: `MISSING_TOKENS NONE` для всех трёх файлов, либо явный список отсутствующих токенов
— в этом случае токены подключаются через общий CSS проекта (точечная правка), и в отчёте
указывается имя файла общего CSS и подтверждение его подключения во всех трёх шаблонах.

### 13. Полный прогон приёмочных тестов Т10

```powershell
python -m pytest tests/test_t10_groups.py --tb=short -q
```

Ожидаемый вывод: итоговая строка вида `6 passed in <T>s` (или больше, если добавлены тесты сверх
минимума), ноль `failed`, ноль `errors`. Меньше шести пройденных проверок означает статус
`ЧАСТИЧНО`, а не `ГОТОВ`.

## Отчёт

Писать в `_recon/T10.md`. Обязательные разделы:

1. Заголовок и дата.
2. Фактическое состояние до правок (раздел «Что уже есть» — с цитатами кода и номерами строк,
   включая вердикт по `display_name_from_email` и `streak`: найдены или `NOT FOUND`).
3. Список изменённых и созданных файлов с итоговым числом строк каждого: `routes/parent_teacher.py`,
   `services/parent_teacher_helpers.py`, `templates/teacher/dashboard.html`,
   `templates/teacher/group.html`, `templates/parent/dashboard.html`,
   `templates/student/profile_detail.html`, `tests/conftest.py` (дополнения),
   `tests/test_t10_groups.py`, `models.py` (дополнения), `app.py` (регистрация blueprint).
4. Миграции — список `ALTER TABLE` и `CREATE TABLE`, факт наличия копии `formyla.db.bak_T10_pre`.
5. Вывод всех тринадцати пунктов ПРИЁМКА целиком, без пересказа.
6. Карта маршрутов блока с HTTP-методами и статусами доступа по ролям.
7. Раздел «Отклонения» — если `display_name_from_email` или `streak` не найдены и реализованы локально,
   писать `NOT FOUND` для исходной и объяснять, чем заменено. Если поле `User` уже существовало до
   миграции — указать это.
8. Раздел «ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА» — рассылка пригласительного кода по email этим блоком не
   реализуется и не имитируется; вопрос передачи кода ученику сформулировать явно.

Запрещённые слова в отчёте (примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается) допустимы только в этом же
перечислении настоящего абзаца. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 10 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 10 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 10 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 09 КОНЕЦ>>>

---

<<<БЛОК 10 НАЧАЛО>>>

# БЛОК 10 — T6 — ПЕРЕНОС ФУНКЦИЙ ИЗ ПРОЧЕЕ НА ГЛАВНУЮ

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 10 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 10 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 10 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Auto-Deploy OFF. База пуста (`users` = 0, `adaptive_tasks` = 0) — норма, приёмка идёт
  на фикстурах из `tests/conftest.py` (блок Ф0), а не на живой базе.
- Главная страница рендерится маршрутом в `app.py` (12632 строки), шаблон — в `templates/`.
  Фактическое имя шаблона найти по коду маршрута (кандидаты: `templates/index.html`,
  `templates/home.html`), не предполагать.
- В шаблонах есть раздел «Прочее» — набор ссылок или виджетов вне основной навигации. Точное
  местоположение найти по grep `Прочее` по `templates/**/*.html`.
- Модели в `models.py` (фактическое число строк зафиксировать). Пользователь — модель `User`.
  Базовая линия тестов: 939 passed, 37 failed, 17 skipped, 0 errors (после v2). Новый блок не
  должен ухудшить `failed` и `errors`.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус карточек 14px, кнопок 10px, шрифт Satoshi + system-ui,
  контейнер 1120px, тёмно-синий стиль, ни одного эмодзи.
- Подписка 400₽/мес, 7 срезов. Кредиты на чертежи: 3 бесплатных, пакеты 10/30/100 за 99/249/599₽.
  Kimi: 0.35₽/фото. DeepSeek для чертежей. Ключи из `.env`, в отчётах пишется `СКРЫТО`.

## Что уже есть

Перед любой правкой ROO фиксирует фактом, без пересказа:

1. Grep `Прочее` по всем `templates/**/*.html` — полный список файлов и номеров строк. Для
   каждого найденного файла привести окрестность (10 строк до и после). Если слово не найдено —
   искать кандидатов: «Дополнительно», «Другое», «Разное», «More». Если и они не найдены —
   `NOT FOUND`.
2. Полный список ссылок или виджетов внутри раздела «Прочее» — для каждого: текст ссылки,
   `href` или `url_for`, назначение (маршрут, шаблон). Если в «Прочее» нет ни одного элемента —
   `NOT FOUND`.
3. Шаблон главной страницы — фактическое имя, маршрут в `app.py` (номер строки), какой контекст
   передаётся, какие блоки уже рендерятся.
4. Модель `User` в `models.py` — номер строки объявления, полный список полей. Есть ли поля,
   связанные с настройками дашборда. Шаблон профиля или настроек — имя, маршрут. Модели
   `UserDashboardItem` или похожей — если есть, полный текст; если нет — `NOT FOUND`.
5. Фикстуры `tests/conftest.py` — структура `test_user` и `auth_client`, какую базу используют.

## Что сделать

1. Первой строкой в журнале `_recon/CHAIN_RUN.md` написать `## БЛОК T6 CODE — СТАРТ` до начала
   любой работы. Если файла нет — создать.

2. Сделать копию базы `instance/formyla.db` в `instance/formyla.db.bak_T6_pre` до миграции.

3. В `models.py` создать модель `UserDashboardItem` с полями: `id` (PK), `user_id` (FK к
   `users.id`), `widget_key` (строка до 64 символов), `position` (целое, по умолчанию 0),
   `visible` (булево, по умолчанию `False`), `created_at`, `updated_at`. Уникальное ограничение
   `(user_id, widget_key)`.

4. Создать `scripts/t6_migration.py` — `CREATE TABLE user_dashboard_items` в переносимой форме
   DDL (без SQLite-специфики, совместимо с PostgreSQL). После миграции — прогон pytest, числа не
   хуже базовой линии (939/37/17/0).

5. Создать `services/dashboard_widgets.py` со списком `AVAILABLE_WIDGETS` — список словарей
   `{'key': <строка>, 'title': <название>, 'template': <путь к partial>, 'description': <описание>}`.
   Ключи берутся из раздела «Прочее» по данным пункта 1 «Что уже есть». Список обязан совпадать с
   фактическим содержимым «Прочее», не выдумывать виджеты.

6. Для каждого виджета создать partial-шаблон в `templates/widgets/` — компактная карточка:
   фон `--surface`, граница `--border`, радиус 14px, заголовок `--text`, вторичный текст
   `--muted`. Содержимое — тот же контент, что в ссылке из «Прочее», в виде карточки с кнопкой
   перехода.

7. Маршрут `/dashboard/settings` (GET/POST) в `app.py` или соответствующем blueprint. GET —
   рендерит `templates/dashboard_settings.html` со списком `AVAILABLE_WIDGETS` и текущими
   записями `UserDashboardItem`. POST — принимает форму с парами `(widget_key, position, visible)`
   для каждого виджета, обновляет или создаёт записи. Валидация: `widget_key` из
   `AVAILABLE_WIDGETS` (иначе 400), `position` — целое 0–99, `visible` — `0` или `1`.

8. Создать `templates/dashboard_settings.html` — список виджетов, переключатель видимости
   (чекбокс или toggle по токенам: `--accent` для включённого, `--border` для выключенного),
   drag-and-drop для порядка — нативный HTML5 (атрибут `draggable`, события `dragstart`,
   `dragover`, `drop`), без внешних библиотек, если они не подключены ранее (проверить `static/`
   и `templates/base.html`). Скрытое поле `position` обновляется JS при перестановке. Кнопка
   «Сохранить» — цвет `--accent`, радиус 10px. Контейнер 1120px, шрифт Satoshi + system-ui.

9. На главной: читать `UserDashboardItem` для текущего пользователя, где `visible = True`,
   сортировать по `position`. Для каждого рендерить соответствующий partial из
   `templates/widgets/`. Если записей нет (новый пользователь) — главная не меняется, виджеты
   из «Прочее» скрыты по умолчанию.

10. Раздел «Прочее» в исходном шаблоне не удалять и не менять — виджеты на главной дополняют,
    а не заменяют его. Сверить токены в новых шаблонах — только hex из набора эталона, ни одного
    эмодзи.

11. Написать тесты в `tests/test_t6_dashboard.py` на фикстурах Ф0 (`app`, `auth_client`,
    `test_user`):
    - Пользователь добавил 2 виджета (visible = True, position 0 и 1) — на главной контент
      обоих виджетов в порядке position.
    - Пользователь убрал виджеты (visible = False) — на главной их нет.
    - Новый пользователь без записей — на главной виджетов из «Прочее» нет.
    - POST с неизвестным `widget_key` — 400.
    - GET `/dashboard/settings` — 200, HTML содержит список виджетов.
    - Тесты не пишут в `instance/formyla.db`, только в тестовую базу фикстур.

12. Написать отчёт `_recon/T6.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T6_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно: не удалять раздел «Прочее» из исходного шаблона; не менять существующие
  маршруты главной (только добавлять рендер виджетов); не подключать новые внешние JS-библиотеки
  для drag-and-drop, если они не подключены ранее; не использовать посторонние hex вне токенов.

## ПРИЁМКА

Каждый пункт проверяется на фикстурах из `tests/conftest.py` (блок Ф0). Команда целиком, вывод
как есть.

### 1. Таблица user_dashboard_items создана

```python
import sqlite3
conn = sqlite3.connect('instance/formyla.db')
r = conn.execute("SELECT name FROM sqlite_master WHERE type='table' AND name='user_dashboard_items'").fetchall()
print('TABLE_EXISTS', bool(r))
if r:
    cols = [c[1] for c in conn.execute('PRAGMA table_info(user_dashboard_items)').fetchall()]
    print('COLUMNS', cols)
```

Ожидаемый вывод: `TABLE_EXISTS True`, `COLUMNS` содержит `id`, `user_id`, `widget_key`,
`position`, `visible`, `created_at`, `updated_at`.

### 2. GET /dashboard/settings — 200

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/dashboard/settings', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1200])
```

Ожидаемый вывод: `STATUS 200`, `LEN` больше нуля, фрагмент HTML с виджетами из «Прочее».

### 3. Пользователь добавил 2 виджета — видны на главной в порядке

```python
import app as A
from services.dashboard_widgets import AVAILABLE_WIDGETS
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
keys = [w['key'] for w in AVAILABLE_WIDGETS[:2]]
c.post('/dashboard/settings', data={
    f'widget_{keys[0]}_visible': '1', f'widget_{keys[0]}_position': '0',
    f'widget_{keys[1]}_visible': '1', f'widget_{keys[1]}_position': '1',
}, follow_redirects=True)
r = c.get('/', follow_redirects=True)
text = r.data.decode('utf-8')
print('STATUS', r.status_code)
print('HAS_0', keys[0] in text, 'HAS_1', keys[1] in text)
print('ORDER_OK', text.find(keys[0]) < text.find(keys[1]) if keys[0] in text and keys[1] in text else 'NOT FOUND')
```

Ожидаемый вывод: `STATUS 200`, `HAS_0 True`, `HAS_1 True`, `ORDER_OK True`.

### 4. Пользователь убрал виджеты — скрыты

```python
import app as A
from services.dashboard_widgets import AVAILABLE_WIDGETS
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
keys = [w['key'] for w in AVAILABLE_WIDGETS[:2]]
c.post('/dashboard/settings', data={
    f'widget_{keys[0]}_visible': '0', f'widget_{keys[0]}_position': '0',
    f'widget_{keys[1]}_visible': '0', f'widget_{keys[1]}_position': '1',
}, follow_redirects=True)
text = c.get('/', follow_redirects=True).data.decode('utf-8')
print('HIDDEN_0', keys[0] not in text, 'HIDDEN_1', keys[1] not in text)
```

Ожидаемый вывод: `HIDDEN_0 True`, `HIDDEN_1 True`.

### 5. Новый пользователь без настроек — виджетов нет

```python
import app as A
from services.dashboard_widgets import AVAILABLE_WIDGETS
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '9999'
text = c.get('/', follow_redirects=True).data.decode('utf-8')
for w in AVAILABLE_WIDGETS:
    print('ABSENT', w['key'], w['key'] not in text)
```

Ожидаемый вывод: `ABSENT <key> True` для каждого виджета.

### 6. POST с неизвестным widget_key — 400

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.post('/dashboard/settings', data={
    'widget_fake_key_visible': '1', 'widget_fake_key_position': '0',
}, follow_redirects=True)
print('STATUS', r.status_code)
```

Ожидаемый вывод: `STATUS 400`.

### 7. Дизайн-токены и эмодзи в новых шаблонах

```python
import re, glob
ALLOWED = {'#070C18','#0E1830','#121F3C','#1C2B4F','#E6EBF7','#8C9ABC','#4C7DFF','#6B95FF','#3ECF8E','#E5AC3A','#E86A62'}
emoji = re.compile(r'[\U0001F300-\U0001FAFF\u2600-\u27BF\u2190-\u21FF\u2700-\u27BF]')
for path in ['templates/dashboard_settings.html'] + glob.glob('templates/widgets/*.html'):
    text = open(path, encoding='utf-8').read()
    foreign = set(m.upper() for m in re.findall(r'#[0-9A-Fa-f]{6}', text)) - ALLOWED
    print(path, 'FOREIGN_HEX', foreign if foreign else 'NONE', 'EMOJI', bool(emoji.search(text)))
```

Ожидаемый вывод: `FOREIGN_HEX NONE`, `EMOJI False` для каждого файла.

### 8. Полный прогон тестов не хуже базовой линии

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <M> failed, <K> skipped, <E> errors in <T>s`, где `M` не больше 37,
`E` не больше 0, `N` не меньше 939.

## Отчёт

Писать в `_recon/T6.md`. Обязательные разделы:

1. Заголовок и дата.
2. Раздел «Прочее» — фактическое содержимое до правок: файл, номер строки, полный список
   ссылок, каждая с `href` и назначением.
3. Модель `UserDashboardItem` — полный текст объявления, номер строки в `models.py`.
4. Миграция — текст `scripts/t6_migration.py`, результат применения.
5. Список `AVAILABLE_WIDGETS` — каждый виджет: ключ, название, шаблон, описание.
6. Список созданных partial-шаблонов `templates/widgets/` — имя, число строк.
7. Маршрут `/dashboard/settings` — файл, номер строки, методы, валидация.
8. `templates/dashboard_settings.html` — число строк, реализация drag-and-drop.
9. Вывод всех восьми пунктов ПРИЁМКА — целиком, без пересказа.
10. Раздел «Отклонения» — если что-то не реализуемо буквально (раздел «Прочее» не найден,
    виджетов нет, библиотека drag-and-drop не подключена) — `NOT FOUND` и объяснение.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается. Эти слова допустимы только
в перечислении. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 11 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 11 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 11 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 10 КОНЕЦ>>>

---

<<<БЛОК 11 НАЧАЛО>>>

# БЛОК 11 — T5 — АУДИТ СТОИМОСТИ КАЖДОЙ ФУНКЦИИ

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 11 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 11 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 11 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Auto-Deploy OFF. База пуста — норма, приёмка на фикстурах из `tests/conftest.py` (Ф0).
- Бизнес-модель: подписка 400₽/мес, включает 7 срезов в месяц. Кредиты на чертежи: 3 бесплатных
  при регистрации, пакеты 10/30/100 за 99/249/599₽. Проверка фото через Kimi: 0.35₽/фото, входит
  в подписку. DeepSeek для генерации чертежей: реальная стоимость для платформы 0.5₽/чертёж.
  OpenRouter для задач дня. Олимпиадные методы: 102 записи, доступны бесплатно.
- `templates/pricing.html` переписан блоком К1 — показывает пакеты кредитов и цены. Маршрут
  `/pricing` существует. `services/yookassa_stub.py` — заглушка оплаты. Модели `User.figure_credits`,
  `FigureCreditTransaction`, `FigureGeneration` — живые.
- `app.py` — 12632 строки, 161 маршрут. `models.py` — фактическое число строк зафиксировать.
- Базовая линия тестов: 939 passed, 37 failed, 17 skipped, 0 errors (после v2). Новый блок не
  должен ухудшить `failed` и `errors`.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус карточек 14px, кнопок 10px, Satoshi + system-ui,
  контейнер 1120px, ни одного эмодзи. Ключи из `.env`, в отчётах пишется `СКРЫТО`.

## Что уже есть

Перед правками ROO фиксирует фактом, без пересказа:

1. Полный текст `templates/pricing.html` — какие цены, пакеты и функции указаны. Совпадают ли
   числа с записанными: 10/99, 30/249, 100/599, 3 бесплатных, подписка 400₽. Каждое
   расхождение выписать отдельно.
2. Маршрут `/pricing` в `app.py` — номер строки декоратора и функции, какой шаблон рендерится,
   какой контекст передаётся.
3. Места в коде, где фигурируют цены и стоимости: grep `400`, `99`, `249`, `599`, `0.35`,
   `0.5` по `*.py` и `templates/**/*.html` — полный список файлов и строк с контекстом.
4. Модель `User` — есть ли поле `figure_credits`, значение по умолчанию. Модели
   `FigureCreditTransaction`, `FigureGeneration` — список полей. Модель подписки: есть ли
   поля `subscription_status`, `subscription_expires`, `trial_end` или аналогичные. Если
   полей подписки нет — `NOT FOUND`.
5. Сервис `services/cost_calculation.py` — если существует, полный текст. Если нет — `NOT FOUND`.
6. Маршруты и функции для каждой проверяемой функции: срез (`/prep/probe` в `routes/prep.py`),
   задачи дня (`daily_tasks/routes.py`), генерация чертежа (`/figures` в `routes/figures.py`),
   проверка фото Kimi (`services/kimi_review.py`), олимпиадные методы (`routes/olympiad.py`).
   Для каждого — есть ли проверка подписки, пробного периода, paywall. Привести номера строк.
7. Фикстуры `tests/conftest.py` — есть ли фикстуры, моделирующие подписку, пробный период,
   paywall.

## Что сделать

1. Первой строкой в журнале `_recon/CHAIN_RUN.md` написать `## БЛОК T5 CODE — СТАРТ` до начала
   любой работы. Если файла нет — создать.

2. Создать `services/cost_calculation.py` с функциями расчёта стоимости для каждой функции
   платформы. Каждая функция возвращает кортеж `(user_cost, platform_cost)` — стоимость для
   пользователя в рублях и реальная стоимость для платформы в рублях:
   - `slice_cost(is_subscribed)` — срез: 0₽ для пользователя (входит в подписку 400₽/мес),
     стоимость для платформы: 0₽ (локальный расчёт уровня, без внешних API).
   - `daily_task_cost(is_subscribed)` — задача дня: 0₽ для пользователя, стоимость для
     платформы: вызов OpenRouter (считать по тарифу, ключ `СКРЫТО`, если ключа нет — `NOT FOUND`
     и оценку не писать, только структуру).
   - `figure_cost(figure_credits, has_subscription)` — генерация чертежа: если `figure_credits`
     больше 0 — 0₽ для пользователя (бесплатный кредит), иначе цена пакета; стоимость для
     платформы: 0.5₽ (DeepSeek API).
   - `kimi_check_cost(is_subscribed)` — проверка фото: 0₽ для пользователя (входит в подписку),
     стоимость для платформы: 0.35₽.
   - `olympiad_methods_cost()` — олимпиадные методы: 0₽ для пользователя, 0₽ для платформы
     (статический контент, 102 записи).
   - `subscription_cost()` — подписка: 400₽ для пользователя, 0₽ для платформы (выручка).
   - `figure_package_cost(package_size)` — пакет кредитов: 99₽ для 10, 249₽ для 30, 599₽ для 100;
     стоимость для платформы: `package_size * 0.5`₽ (DeepSeek API).
   - `monthly_platform_cost_per_user()` — сводная стоимость одного ученика в месяц: 7 срезов
     (0₽ платформе) + проверка фото (14₽, из расчёта 40 проверок в месяц по 0.35₽) + чертежи
     (по факту использования кредитов).

3. Обновить `templates/pricing.html` — страница должна показывать все цены и пакеты:
   - Подписка: 400₽/мес, что входит: 7 срезов, проверка фото через Kimi, олимпиадные методы.
   - Кредиты на чертежи: 3 бесплатных при регистрации, пакеты 10/30/100 за 99/249/599₽.
   - Задачи дня: бесплатно, входят в подписку.
   - Олимпиадные методы: бесплатно.
   - Срез: входит в подписку (отдельно не оплачивается).
   - Проверка фото: входит в подписку.
   - Все цены в рублях, дизайн по токенам эталона, ни одного эмодзи.

4. Проверить, что `templates/pricing.html` показывает актуальные цены. Сверить каждое число в
   шаблоне с записанными значениями (400, 99, 249, 599, 10, 30, 100, 3, 7). Если в шаблоне
   другие числа — исправить, привести в отчёте «было / стало».

5. Создать или обновить маршрут `/pricing` так, чтобы он передавал в шаблон данные из
   `services/cost_calculation.py` — список функций с их стоимостью для пользователя. Шаблон
   рендерит таблицу: функция, стоимость для пользователя, что входит. Не дублировать цены
   жёстко в шаблоне — брать из контекста, передаваемого маршрутом, где это возможно (числа
   пакетов и цен можно оставить в шаблоне, если они статичны, но стоимость подписки и состав
   передавать из `cost_calculation`).

6. Сверить отображаемые цены с реальными: для каждой функции из пункта 2 проверить, что цена,
   которую видит пользователь в интерфейсе (`/pricing`, кнопки покупки, paywall), совпадает с
   расчётом `services/cost_calculation.py`. Расхождения — списком в отчёте, исправить
   точечно.

7. Написать тесты в `tests/test_t5_cost.py` на фикстурах Ф0:
   - `slice_cost(True)` возвращает `(0, 0)`.
   - `figure_cost(3, True)` возвращает `(0, 0.5)` — пользователь платит 0 (бесплатный кредит),
     платформа 0.5₽.
   - `figure_cost(0, True)` — цена пакета для пользователя (не 0), платформа 0.5₽.
   - `kimi_check_cost(True)` возвращает `(0, 0.35)`.
   - `olympiad_methods_cost()` возвращает `(0, 0)`.
   - `subscription_cost()` возвращает `(400, 0)`.
   - `figure_package_cost(10)` возвращает `(99, 5)`, `figure_package_cost(30)` — `(249, 15)`,
     `figure_package_cost(100)` — `(599, 50)`.
   - GET `/pricing` — 200, HTML содержит числа 400, 99, 249, 599.
   - Тесты не пишут в `instance/formyla.db`.

8. Написать отчёт `_recon/T5.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T5_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно: не удалять и не переписывать `services/yookassa_stub.py` и логику списания
  кредитов из блока К1; не менять реальные вызовы внешних API (DeepSeek, Kimi, OpenRouter) —
  только расчёт стоимости; не печатать значения ключей из `.env`; не менять модель подписки,
  если она существует — только фиксировать факт.

## ПРИЁМКА

Каждый пункт проверяется на фикстурах из `tests/conftest.py` (Ф0). Команда целиком, вывод как есть.

### 1. services/cost_calculation.py — функции возвращают правильные стоимости

```python
from services.cost_calculation import (
    slice_cost, daily_task_cost, figure_cost, kimi_check_cost,
    olympiad_methods_cost, subscription_cost, figure_package_cost,
    monthly_platform_cost_per_user,
)
print('SLICE', slice_cost(True))
print('FIGURE_FREE', figure_cost(3, True))
print('FIGURE_NO_CREDITS', figure_cost(0, True))
print('KIMI', kimi_check_cost(True))
print('METHODS', olympiad_methods_cost())
print('SUB', subscription_cost())
print('PKG_10', figure_package_cost(10))
print('PKG_30', figure_package_cost(30))
print('PKG_100', figure_package_cost(100))
print('MONTHLY', monthly_platform_cost_per_user())
```

Ожидаемый вывод: `SLICE (0, 0)`, `FIGURE_FREE (0, 0.5)`, `FIGURE_NO_CREDITS` — цена пакета
для пользователя (больше 0) и `0.5` для платформы, `KIMI (0, 0.35)`, `METHODS (0, 0)`,
`SUB (400, 0)`, `PKG_10 (99, 5)`, `PKG_30 (249, 15)`, `PKG_100 (599, 50)`, `MONTHLY` —
сводная сумма для платформы (включает 14₽ за проверки Kimi).

### 2. GET /pricing — 200, цены в HTML

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/pricing', follow_redirects=True)
text = r.data.decode('utf-8')
print('STATUS', r.status_code)
for num in ['400', '99', '249', '599', '10', '30', '100']:
    print(f'HAS_{num}', num in text)
```

Ожидаемый вывод: `STATUS 200`, `HAS_400 True`, `HAS_99 True`, `HAS_249 True`, `HAS_599 True`,
`HAS_10 True`, `HAS_30 True`, `HAS_100 True`.

### 3. Цены пакетов совпадают с записанными

```python
text = open('templates/pricing.html', encoding='utf-8').read()
pairs = [('10', '99'), ('30', '249'), ('100', '599')]
for count, price in pairs:
    print(count, price, count in text and price in text)
```

Ожидаемый вывод: три строки, каждая заканчивается `True`.

### 4. Подписка и состав в pricing.html

```python
text = open('templates/pricing.html', encoding='utf-8').read()
print('HAS_SUB_400', '400' in text)
print('HAS_7_SLICES', '7' in text and ('срез' in text.lower() or 'slice' in text.lower()))
print('HAS_KIMI', 'kimi' in text.lower() or 'фото' in text.lower())
print('HAS_METHODS_FREE', 'бесплатн' in text.lower())
```

Ожидаемый вывод: `HAS_SUB_400 True`, `HAS_7_SLICES True`, `HAS_KIMI True`, `HAS_METHODS_FREE
True`.

### 5. Дизайн-токены и эмодзи в pricing.html

```python
import re
ALLOWED = {'#070C18','#0E1830','#121F3C','#1C2B4F','#E6EBF7','#8C9ABC','#4C7DFF','#6B95FF','#3ECF8E','#E5AC3A','#E86A62'}
emoji = re.compile(r'[\U0001F300-\U0001FAFF\u2600-\u27BF\u2190-\u21FF\u2700-\u27BF]')
text = open('templates/pricing.html', encoding='utf-8').read()
foreign = set(m.upper() for m in re.findall(r'#[0-9A-Fa-f]{6}', text)) - ALLOWED
print('FOREIGN_HEX', foreign if foreign else 'NONE')
print('EMOJI', bool(emoji.search(text)))
```

Ожидаемый вывод: `FOREIGN_HEX NONE`, `EMOJI False`.

### 6. Маршрут /pricing передаёт данные из cost_calculation

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/pricing', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1500])
```

Ожидаемый вывод: `STATUS 200`, `LEN` больше нуля, фрагмент HTML с таблицей или карточками цен.

### 7. Полный прогон тестов не хуже базовой линии

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <M> failed, <K> skipped, <E> errors in <T>s`, где `M` не больше 37,
`E` не больше 0, `N` не меньше 939.

## Отчёт

Писать в `_recon/T5.md`. Обязательные разделы:

1. Заголовок и дата.
2. Таблица «функция -> стоимость для пользователя -> стоимость для платформы» — все функции из
   пункта 2 «Что сделать», с фактическими числами.
3. Фактическое состояние `templates/pricing.html` до правок — какие цены были, что не совпадало
   с записанными значениями.
4. Текст `services/cost_calculation.py` — список функций с сигнатурами и возвращаемыми
   значениями.
5. Обновлённый `templates/pricing.html` — что добавлено, что исправлено (было/стало по каждому
   расхождению).
6. Вывод всех семи пунктов ПРИЁМКА — целиком, без пересказа.
7. Раздел «Сверка цен» — для каждой функции: цена в интерфейсе, цена из `cost_calculation`,
   совпадает или нет. Расхождения — списком с исправлением.
8. Раздел «Отклонения» — если функция не найдена, маршрут отсутствует, ключ API недоступен —
   `NOT FOUND` и объяснение.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается. Эти слова допустимы только
в перечислении. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 12 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 12 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 12 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 11 КОНЕЦ>>>

---

<<<БЛОК 12 НАЧАЛО>>>

# БЛОК 12 — T11 — ПРОВЕРКА ПАМЯТИ RENDER НА 300 УЧЕНИКОВ

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 12 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 12 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 12 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Auto-Deploy OFF. База пуста — норма. Приёмка на фикстурах из `tests/conftest.py` (Ф0).
- Прод: Render `srv-d73br5ffte5s73euc56g`, домен `formyla.net`, база PostgreSQL, Auto-Deploy OFF.
  На проде 8778 задач и 9039 пользователей. Выкладка выполняется человеком вручную.
- `app.py` — 12632 строки. `models.py` — фактическое число строк зафиксировать. Локальная база
  `instance/formyla.db` — 32 МБ. Базовая линия тестов: 939 passed, 37 failed, 17 skipped, 0 errors.
- Render-планы: Free — 512 МБ RAM, Starter — 512 МБ RAM, Standard — 2 ГБ RAM, Pro — 4 ГБ RAM.
  Какой план используется — выяснить без касания прод-сервера, по статическим данным.
- Memory-intensive операции: генерация чертежей через DeepSeek (ответ до 300 с), проверка фото
  Kimi (base64 в памяти), рендеринг SVG (`geometric_engine/engine.py` — 1353 строки), очередь
  построений (`drawing_async.py` — in-memory `_STORE`, TTL 1800 с). OpenRouter — таймаут 300 с.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`. Ни одного эмодзи. Ключи из `.env`, в отчётах пишется `СКРЫТО`.
- Этот блок НЕ правит код. Только статический анализ, расчёт и отчёт. Нагрузочное тестирование
  запрещено. Касание прод-сервера и Render API запрещено.

## Что уже есть

Перед расчётом ROO фиксирует фактом, без пересказа:

1. Размер `app.py`, `models.py`, `routes/prep.py`, `daily_tasks/services.py`,
   `geometric_engine/engine.py`, `services/drawing_async.py` — число строк каждого. Размер
   `instance/formyla.db` в байтах. Для каждого отсутствующего — `NOT FOUND`.
2. Количество моделей — grep `class .*db\.Model` по всем `*.py`. Количество маршрутов —
   grep `@.*\.route\(` по всем `*.py`. Точные числа.
3. Размер базы `instance/formyla.db` — `os.path.getsize` в МБ. Число таблиц (`SELECT count(*)`
   из `sqlite_master`). Если базы нет — `NOT FOUND`.
4. Наличие `render.yaml`, `Dockerfile`, `Procfile` — содержимое каждого. Какой план Render
   указан. Если ни одного нет — `NOT FOUND`.
5. Наличие переменных окружения, указывающих на план Render: grep `RENDER`, `PLAN`, `RAM`,
   `MEMORY` по `.env`, `.env.example`, `render.yaml`. Если не найдено — `NOT FOUND`.
6. Memory-intensive операции — найти в коде:
   - Вызовы DeepSeek: grep `deepseek\|DEEPSEEK\|FIGURE_MODEL` — файлы, строки, таймаут.
   - Вызовы Kimi: grep `kimi\|KIMI` — где хранится base64, сколько памяти на одно фото.
   - SVG-рендеринг: `geometric_engine/engine.py` — структуры данных, кэширование.
   - Очередь: `drawing_async.py` — размер `_STORE`, TTL, что хранится.
   - OpenRouter: grep `openrouter\|OPENROUTER` — файлы, строки, таймаут.
7. Средний размер страницы — для `/`, `/figures`, `/pricing` получить размер HTML через
   `app.test_client()` на фикстурах Ф0. Записать `len(r.data)` для каждого.
8. WSGI-сервер — grep `gunicorn`, `waitress` по `requirements.txt`. Сколько воркеров по
   умолчанию. Тяжёлые зависимости: numpy, pandas, matplotlib, PIL, torch — что установлено.

## Что сделать

1. Первой строкой в журнале `_recon/CHAIN_RUN.md` написать `## БЛОК T11 CODE — СТАРТ` до начала
   любой работы. Если файла нет — создать.

2. Определить текущий план Render по статическим данным (пункт 4 «Что уже есть»). Если в
   `render.yaml` указан план — привести дословно. Если файла нет — `NOT FOUND` и явно указать,
   что план неизвестен из локальных данных, привести список планов с лимитами RAM.

3. Составить статический расчёт потребления памяти для 300 одновременных учеников:
   - Базовое потребление процесса Flask (B): оценить по размеру `app.py` и числу моделей.
     Размер исходного кода не равен памяти, но коррелирует с числом импортированных модулей.
     Если `psutil` или `resource` доступны — запустить процесс на локальной фикстурной базе и
     привести фактическое потребление. Если нет — оценка на основе размера кода и зависимостей.
   - Сессии (S): 300 одновременных сессий. Flask по умолчанию использует cookie-based сессии —
     память на сервере не расходуется. Если в проекте серверные сессии (grep `SESSION_TYPE`,
     `flask_session`) — оценить размер одной и умножить на 300. Если cookie-based — `S = 0`.
   - Рендеринг страниц (R): средний размер страницы из пункта 7, коэффициент одновременности
     0.3 (90 одновременных рендеров). `R = 90 * средний_размер_страницы`.
   - Очередь построений (Q): до 300 записей в `_STORE`, размер одной — средний размер SVG из
     `data/figures/` или `_recon/ch6_svg/`. Если SVG недоступны — `NOT FOUND` и оценка по
     типичному размеру SVG (5–50 КБ).
   - Проверки Kimi (K): фото после сжатия до 1500px/0.8 — 200–500 КБ, base64 добавляет 33
     процента. Одно фото — 300–700 КБ. При 90 одновременных (коэффициент 0.3) — 27–63 МБ.
   - Итого: `B + S + R + Q + K` в МБ. Для каждого компонента, где точное число недоступно —
     `NOT FOUND` и оценка на основе известных размеров.

4. Множитель воркеров: если WSGI-сервер — gunicorn с N воркеров, базовое потребление
   умножается на N (каждый воркер — отдельный процесс со своей памятью). Указать число
   воркеров или `NOT FOUND`, если неизвестно.

5. Рекомендация: для каждого плана Render (Free 512 МБ, Starter 512 МБ, Standard 2 ГБ,
   Pro 4 ГБ) — помещается ли пиковое потребление. При каком числе одновременных учеников план
   перестаёт справляться. Достаточно ли текущего плана или нужен апгрейд.

6. Написать отчёт `_recon/T11.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T11_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно: никакой правки кода — этот блок только читает и считает; нагрузочное
  тестирование запрещено (`locust`, `wrk`, `ab`, `pytest-benchmark`); обращение к Render API,
  SSH на прод, подключение к прод-базе PostgreSQL запрещены; если данных для точного расчёта
  нет — писать `NOT FOUND` и давать оценку на основе известных размеров.

## ПРИЁМКА

Каждый пункт — статический расчёт или проверка на локальных данных. Команда целиком, вывод как есть.

### 1. Размер app.py и ключевых файлов

```python
import os
files = ['app.py', 'models.py', 'routes/prep.py', 'daily_tasks/services.py',
         'geometric_engine/engine.py', 'services/drawing_async.py', 'instance/formyla.db']
for f in files:
    if os.path.exists(f):
        size = os.path.getsize(f)
        lines = len(open(f, encoding='utf-8', errors='ignore').readlines()) if f.endswith('.py') else 'N/A'
        print(f'{f} : size={size} bytes lines={lines}')
    else:
        print(f'{f} : NOT FOUND')
```

Ожидаемый вывод: для каждого файла — размер и число строк, либо `NOT FOUND`.

### 2. Число моделей и маршрутов

```python
import re, glob
models = routes = 0
for path in glob.glob('**/*.py', recursive=True):
    if '/.git/' in path: continue
    text = open(path, encoding='utf-8', errors='ignore').read()
    models += len(re.findall(r'class\s+\w+.*db\.Model', text))
    routes += len(re.findall(r'@\w+\.route\(', text))
print('MODELS', models)
print('ROUTES', routes)
```

Ожидаемый вывод: `MODELS <N>`, `ROUTES <N>` — фактические числа.

### 3. Размер базы и число таблиц

```python
import os, sqlite3
path = 'instance/formyla.db'
if os.path.exists(path):
    print('DB_SIZE_MB', round(os.path.getsize(path) / 1024 / 1024, 2))
    conn = sqlite3.connect(path)
    print('TABLES', conn.execute("SELECT count(*) FROM sqlite_master WHERE type='table'").fetchone()[0])
else:
    print('DB NOT FOUND')
```

Ожидаемый вывод: `DB_SIZE_MB 32.0` (или фактическое), `TABLES 63` (или фактическое).

### 4. План Render по статическим данным

```python
import os
for f in ['render.yaml', 'Dockerfile', 'Procfile']:
    if os.path.exists(f):
        print(f'=== {f} ===')
        print(open(f, encoding='utf-8').read()[:800])
    else:
        print(f'{f} : NOT FOUND')
```

Ожидаемый вывод: содержимое найденных файлов, либо `NOT FOUND` для каждого. Если план указан —
дословно. Если не указан — `NOT FOUND` и список планов с лимитами.

### 5. Средний размер страниц

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
for path in ['/', '/figures', '/pricing']:
    r = c.get(path, follow_redirects=True)
    print(path, 'STATUS', r.status_code, 'SIZE', len(r.data))
```

Ожидаемый вывод: для каждого маршрута — код ответа и размер HTML в байтах.

### 6. Memory-intensive операции — grep

```python
import glob, re
patterns = {'deepseek': r'deepseek|DEEPSEEK|FIGURE_MODEL', 'kimi': r'kimi|KIMI',
            'openrouter': r'openrouter|OPENROUTER', 'store': r'_STORE|drawing_async'}
for name, pat in patterns.items():
    hits = []
    for path in glob.glob('**/*.py', recursive=True):
        if '/.git/' in path: continue
        for i, line in enumerate(open(path, encoding='utf-8', errors='ignore').readlines(), 1):
            if re.search(pat, line, re.IGNORECASE):
                hits.append(f'{path}:{i}:{line.strip()[:90]}')
    print(f'=== {name} ({len(hits)}) ===')
    for h in hits[:8]: print(h)
```

Ожидаемый вывод: для каждого паттерна — число находок и первые 8 строк.

### 7. Тяжёлые зависимости и WSGI-сервер

```python
import os
text = open('requirements.txt', encoding='utf-8').read() if os.path.exists('requirements.txt') else ''
for h in ['numpy', 'pandas', 'matplotlib', 'Pillow', 'torch', 'gunicorn', 'waitress']:
    print(h, h.lower() in text.lower())
```

Ожидаемый вывод: `True` или `False` для каждой зависимости.

### 8. Отчёт о памяти существует

```python
import os
print('REPORT_EXISTS', os.path.exists('_recon/T11.md'))
```

Ожидаемый вывод: `REPORT_EXISTS True` после написания отчёта.

## Отчёт

Писать в `_recon/T11.md`. Обязательные разделы:

1. Заголовок и дата.
2. Текущий план Render — дословно из `render.yaml` или `NOT FOUND`. Список всех планов с
   лимитами RAM: Free 512 МБ, Starter 512 МБ, Standard 2 ГБ, Pro 4 ГБ.
3. Статический размер проекта: `app.py` (строки), `models.py` (строки), число моделей, число
   маршрутов, размер базы (МБ), число таблиц.
4. Средний размер страниц — таблица: маршрут, размер HTML (байты).
5. Memory-intensive операции — для каждой: файл, строка, таймаут, что хранится в памяти, оценка
   размера одной операции.
6. Расчёт пикового потребления для 300 одновременных учеников:
   - B (базовое), S (сессии), R (рендеринг, коэффициент 0.3), Q (очередь), K (Kimi, коэффициент
     0.3) — каждый с числом или `NOT FOUND` и оценкой.
   - Итого: `B + S + R + Q + K` в МБ.
7. Множитель воркеров — число воркеров или `NOT FOUND`, итоговое потребление с учётом.
8. Тяжёлые зависимости — список установленных, оценка вклада каждой.
9. Рекомендация: для каждого плана — помещается ли, при каком числе учеников перестаёт
   справляться, достаточно ли текущего плана или нужен апгрейд.
10. Раздел «Отклонения» — `NOT FOUND` для каждого недоступного компонента, с указанием, на
    основе каких известных размеров сделана оценка.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается. Эти слова допустимы только
в перечислении. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Твоя работа окончена. Карточка записана в `_recon/CHAIN_RUN.md`.
Блок 13 следующий.

1. Проверь свой статус. Если ЧАСТИЧНО или СОРВАЛСЯ — следующий диалог
   не создавай, остановись здесь и объясни человеку, что мешает.
2. Открой файл `_promts/FORMYLA_CHAIN_V3.md`.
3. Найди строку, состоящую целиком из `<<<БЛОК 13 НАЧАЛО>>>`,
   и строку, состоящую целиком из `<<<БЛОК 13 КОНЕЦ>>>`.
   Упоминания этих строк внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Создай новый диалог через `new_task` и передай ему этот текст как задание.
   В начале сообщения напиши:

   > Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ
   > целиком. Затем выполни задание ниже.

6. Подтверди в журнале, что новый диалог создан.
<<<БЛОК 12 КОНЕЦ>>>

---

<<<БЛОК 13 НАЧАЛО>>>

# БЛОК 13 — P_FINAL_V3 — ФИНАЛ: полный контроль всех 11 слоёв

## Прежде чем начать

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Порядок входа обязателен
и выполняется до любой другой работы.

1. Открой файл `_promts/FORMYLA_CHAIN_V3.md` и прочитай раздел УСТАВ целиком.
   Он задаёт запреты, порог приёмки, порог по тестам, формат карточки и правило
   контрольных блоков. Устав обязателен без исключений.
2. Открой `_recon/CHAIN_RUN.md`. Если блок 13 уже отмечен как ГОТОВ —
   работу не повторяй, сразу иди к разделу ПЕРЕДАЧА в конце этого задания.
3. Найди строку, состоящую целиком из `<<<БЛОК 13 НАЧАЛО>>>`, и строку,
   состоящую целиком из `<<<БЛОК 13 КОНЕЦ>>>`. Упоминания этих строк
   внутри текста других блоков маркерами не считаются.
4. Скопируй весь текст между ними. Сами строки-маркеры не включай.
5. Выполни его как своё задание. Ничего из остальных блоков сейчас не читай
   и не выполняй.

---

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Auto-Deploy OFF. База пуста — норма, приёмка на фикстурах из `tests/conftest.py` (Ф0).
- Это финальный блок цепочки v3. Идёт после всех одиннадцати блоков: Т1–Т11. Следующего диалога
  этот блок не создаёт — цепочка на нём закрывается.
- Прод: Render `srv-d73br5ffte5s73euc56g`, домен `formyla.net`, база PostgreSQL, Auto-Deploy OFF.
  На проде 8778 задач и 9039 пользователей. Выкладка выполняется человеком вручную.
- Базовая линия тестов: 939 passed, 37 failed, 17 skipped, 0 errors (после v2, `_recon/R0.md`).
  Новый прогон не должен ухудшить `failed` и `errors`.
- `_recon/CHAIN_RUN.md` — журнал цепочки, куда каждый блок обязан отписаться карточкой. Если
  файла нет — `NOT FOUND` и восстановление списка по наличию отчётов в `_recon/`.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`. Радиус 14px/10px. Satoshi + system-ui. Контейнер 1120px.
  Ни одного эмодзи. Ключи из `.env`, в отчётах пишется `СКРЫТО`.
- Правило блока: нашёл дефект — чинишь на месте, доказываешь повторным прогоном той же команды.
  Обязательна строка НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО. Починить нельзя без нарушения запретов —
  писать `ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА`.

## Что уже есть

1. Полный список файлов в `_recon/` — отчёты `T1.md`–`T11.md`, `R0.md`. Для каждого — существует
   или `NOT FOUND`.
2. Содержимое `_recon/CHAIN_RUN.md` — для каждого кода Т1–Т11: отписался или
   `БЛОК Т<N> НЕ ОТПИСАЛСЯ`.
3. Результат последнего полного прогона тестов из отчётов предыдущих блоков — числа
   `passed/failed/skipped/errors` на момент начала этого блока.
4. Список шаблонов, созданных или изменённых блоками Т1–Т11, собранный по текстам их отчётов.
   Если отчёт блока отсутствует — `NOT FOUND` для списка шаблонов этого блока.

## Что сделать

1. Первой строкой в журнале `_recon/CHAIN_RUN.md` написать `## БЛОК P_FINAL_V3 CODE — СТАРТ`
   до начала любой работы.

2. Для каждого из одиннадцати блоков (Т1–Т11) проверить: отчёт в `_recon/` существует; карточка
   в `_recon/CHAIN_RUN.md` присутствует; тесты не хуже базовой линии (939/37/17/0) — сверить с
   числами в отчёте блока.

3. Специфические проверки по блокам (на фикстурах Ф0, через `app.test_client()`):
   - **Т1** — удалённые разделы дают 404. Список удалённых маршрутов из `_recon/T1.md`, GET на
     каждый — 404.
   - **Т2** — ни одного `@` в HTML лидеров. GET страницы лидеров, `text.count('@')` равно 0.
   - **Т3** — куратор приветствует по имени. GET главной — HTML содержит имя пользователя.
   - **Т4** — пробный период 1 день работает, paywall работает. Пользователь с `trial_end` в
     прошлом — paywall блокирует. С `trial_end` в будущем — доступ открыт.
   - **Т5** — таблица стоимости существует, цены актуальны. `services/cost_calculation.py`
     существует, `templates/pricing.html` содержит 400, 99, 249, 599.
   - **Т6** — виджеты переносятся на главную. POST `/dashboard/settings` с visible=True — виджет
     на главной. visible=False — скрыт.
   - **Т7** — месяц 2 выдаёт следующие 7 подтем. GET с month=1 и month=2 — разные списки.
   - **Т8** — серия 100%, выходные работают. Серия верных ответов — прогресс 100%. Расписание
     выходных — задачи не выдаются в выходной.
   - **Т9** — приоритет подписчиков в очереди, позиция видна. Подписчик впереди не-подписчика.
   - **Т10** — CRUD группы, дашборд учителя, дашборд родителя. Создать/прочитать/обновить/
     удалить — корректные коды. Дашборды — 200.
   - **Т11** — отчёт о памяти Render существует. `_recon/T11.md` присутствует, содержит расчёт
     пикового потребления и рекомендацию по плану.

4. Общие проверки:
   - Ноль эмодзи в коде и шаблонах (кроме `avatar_emoji` и пользовательских данных).
   - Дизайн-токены во всех новых шаблонах — только hex из эталона, никаких посторонних.
   - `git status` — без признаков `git push` или обращения к Render API.

5. Финальная регрессия: полный `python -m pytest tests/ --tb=short -q`. Таблица «Р0 / сейчас»
   по `passed/failed/skipped/errors`. Каждое ухудшение — тройка НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.

6. Сводный список нерешённого: из отчётов всех блоков собрать каждый пункт `NOT FOUND`,
   «Отклонения» или `ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА`. Единый пронумерованный список с блоком-источником.

7. Финальный вердикт: `ГОТОВ К ВЫКЛАДКЕ`, если все одиннадцать блоков отписались, регрессия не
   хуже Р0, эмодзи 0, нерешённое пусто или не блокирует выкладку. Либо
   `ЧАСТИЧНО: <причины списком>` — каждая причина с указанием блока.

8. Написать отчёт `_recon/P_FINAL_V3.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_P_FINAL_V3_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно: ничего не выкладывать, не пушить, не трогать Render и прод-базу. Поле
  `avatar_emoji` и пользовательские данные не трогать при вычистке эмодзи. Если починка
  требует нарушить запрет — писать `ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА`.

## ПРИЁМКА

Каждый пункт — на фикстурах Ф0. Команда целиком, вывод как есть. Строка НАЙДЕНО / ПОЧИНЕНО /
ДОКАЗАНО по каждому.

### 1. Отчёты всех одиннадцати блоков существуют

```python
import os
for n in range(1, 12):
    print(f'T{n}', 'EXISTS' if os.path.exists(f'_recon/T{n}.md') else 'NOT FOUND')
```

Ожидаемый вывод: `T1 EXISTS` ... `T11 EXISTS`, либо `NOT FOUND` для отсутствующих.

### 2. Карточки блоков в CHAIN_RUN.md

```python
import os
if os.path.exists('_recon/CHAIN_RUN.md'):
    text = open('_recon/CHAIN_RUN.md', encoding='utf-8').read()
    for n in range(1, 12):
        print(f'T{n}', 'ОТПИСАЛСЯ' if f'Т{n}' in text or f'T{n}' in text else 'НЕ ОТПИСАЛСЯ')
else:
    print('CHAIN_RUN NOT FOUND')
```

Ожидаемый вывод: для каждого блока — `ОТПИСАЛСЯ` или `НЕ ОТПИСАЛСЯ`.

### 3. Т1–Т4 — удалённые 404, @ в лидерах, приветствие, paywall

```python
import app as A, os
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
print('T1_REPORT', 'EXISTS' if os.path.exists('_recon/T1.md') else 'NOT FOUND')
# ROO проверяет удалённые маршруты из _recon/T1.md: каждый STATUS 404
rl = c.get('/leaders', follow_redirects=True)
print('T2_AT_COUNT', rl.data.decode('utf-8').count('@'))
rh = c.get('/', follow_redirects=True)
print('T3_STATUS', rh.status_code)
rd = c.get('/dashboard', follow_redirects=True)
print('T4_STATUS', rd.status_code)
```

Ожидаемый вывод: `T1_REPORT EXISTS`, `T2_AT_COUNT 0`, `T3_STATUS 200`, `T4_STATUS 200` или
редирект на paywall. ROO проверяет оба сценария Т4 (истёк / активен). По каждому — НАЙДЕНО /
ПОЧИНЕНО / ДОКАЗАНО.

### 4. Т5–Т7 — стоимость, виджеты, темы

```python
import app as A, os
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
print('COST_SERVICE', os.path.exists('services/cost_calculation.py'))
text = open('templates/pricing.html', encoding='utf-8').read() if os.path.exists('templates/pricing.html') else ''
print('PRICES', all(n in text for n in ['400','99','249','599']))
print('T6_SETTINGS', c.get('/dashboard/settings', follow_redirects=True).status_code)
r1 = c.get('/methods?month=1', follow_redirects=True)
r2 = c.get('/methods?month=2', follow_redirects=True)
print('T7_M1_M2', r1.status_code, len(r1.data), r2.status_code, len(r2.data))
```

Ожидаемый вывод: `COST_SERVICE True`, `PRICES True`, `T6_SETTINGS 200`, оба `T7` — `200`, `M2`
отличается от `M1`. ROO проверяет добавление/скрытие виджетов по `_recon/T6.md`. По каждому —
НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.

### 5. Т8–Т11 — серия, очередь, группы, память

```python
import app as A, os
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
print('T8_PROBE', c.get('/prep/probe', follow_redirects=True).status_code)
print('T9_FIGURES', c.get('/figures', follow_redirects=True).status_code)
print('T10_GROUPS', c.get('/groups', follow_redirects=True).status_code)
print('T11_REPORT', os.path.exists('_recon/T11.md'))
```

Ожидаемый вывод: `T8_PROBE 200`, `T9_FIGURES 200`, `T10_GROUPS 200` или 302, `T11_REPORT True`.
ROO проверяет серию 100% и выходные по `_recon/T8.md`, приоритет подписчиков по `_recon/T9.md`,
полный CRUD и дашборды по `_recon/T10.md`. По каждому — НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.

### 6. Эмодзи — ноль в коде и шаблонах

```python
import re, glob
pattern = re.compile(r'[\U0001F300-\U0001FAFF\u2600-\u27BF\u2190-\u21FF\u2700-\u27BF]')
hits = []
for path in glob.glob('templates/**/*.html', recursive=True) + glob.glob('**/*.py', recursive=True):
    if 'group_chats' in path or '.env' in path:
        continue
    try:
        text = open(path, encoding='utf-8', errors='ignore').read()
    except Exception:
        continue
    for i, line in enumerate(text.splitlines(), 1):
        if pattern.search(line):
            hits.append((path, i))
print('EMOJI_HITS', len(hits))
```

Ожидаемый вывод: `EMOJI_HITS 0`.

### 7. Дизайн-токены во всех новых шаблонах

```python
import re, os
ALLOWED = {'#070C18','#0E1830','#121F3C','#1C2B4F','#E6EBF7','#8C9ABC','#4C7DFF','#6B95FF','#3ECF8E','#E5AC3A','#E86A62'}
templates = []  # ROO заполняет по отчётам Т1-Т11
for path in templates:
    if os.path.exists(path):
        text = open(path, encoding='utf-8').read()
        foreign = set(m.upper() for m in re.findall(r'#[0-9A-Fa-f]{6}', text)) - ALLOWED
        print(path, 'FOREIGN_HEX', foreign if foreign else 'NONE')
```

Ожидаемый вывод: `FOREIGN_HEX NONE` для каждого файла.

### 8. Полный прогон тестов — не хуже базовой линии

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <M> failed, <K> skipped, <E> errors in <T>s`, где `M` не больше 37,
`E` не больше 0, `N` не меньше 939. Таблица «Р0 / сейчас» по четырём числам.

## Отчёт

Писать в `_recon/P_FINAL_V3.md`. Обязательные разделы:

1. Заголовок и дата, ссылка на базовую линию (`_recon/R0.md`).
2. Статус всех одиннадцати блоков — отписался или `БЛОК Т<N> НЕ ОТПИСАЛСЯ`, для каждого: отчёт
   существует, приёмка пройдена, тесты не хуже базовой линии.
3. По каждому из восьми пунктов ПРИЁМКА — вывод, правка (если была), вывод после, строка
   НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.
4. Таблица «Р0 / сейчас» по `passed/failed/skipped/errors`, разбор ухудшений.
5. Сводный список нерешённого — пронумерованный, с формулировкой и блоком-источником.
6. Подтверждение, что push и обращения к Render не выполнялись.
7. Раздел «Не сделано / NOT FOUND» — буквально.
8. Финальный вердикт — ровно одна строка: `ГОТОВ К ВЫКЛАДКЕ` либо
   `ЧАСТИЧНО: <причины списком>`.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается. Эти слова допустимы только
в перечислении. Нет файла или функции — писать ровно `NOT FOUND`.

---

## ПЕРЕДАЧА

Ты последний блок. Следующего нет. Карточка записана в `_recon/CHAIN_RUN.md`.

Проверь, что в журнале есть карточки всех 13 блоков. Для пропавших напиши
явную строку: БЛОК N НЕ ОТПИСАЛСЯ.

Дай итоговый вердикт: ГОТОВ К ВЫКЛАДКЕ или ЧАСТИЧНО с перечнем.
<<<БЛОК 13 КОНЕЦ>>>

---

