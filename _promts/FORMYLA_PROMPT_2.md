# ПРОМПТ 2 — стоимость, память Render, финальный отчёт

Это второе из двух заданий. Оно содержит три блока: Т5, Т11 и финал.
Выполняются подряд, в этом диалоге, по порядку.

Задание 1 (блоки Т10 и Т6) выполнено в предыдущем диалоге. Проверь это
первым делом: открой `_recon/CHAIN_RUN.md` и найди карточки Т10 и Т6.
Если хотя бы одной нет или её статус не ГОТОВ — не начинай работу,
сообщи человеку и остановись.

---

## УСТАВ

Читается целиком до начала работы. Обязателен без исключений.

### Кто ты

Ты правишь код проекта FORMYLA в Roo Code, режим Code. Проект локальный:
`C:\Users\Redmi\Desktop\Новая папка (2)`, Windows, PowerShell, VS Code.
Рабочая база `instance/formyla.db`. Всё на русском.

Прод: Render `srv-d73br5ffte5s73euc56g`, formyla.net, Auto-Deploy OFF,
PostgreSQL. Репозиторий `victorkrivenko4949-commits / FORMYLA`, ветка `main`.

### Что уже сделано и переделывать не надо

Из одиннадцати задач человека закрыты шесть. Их работу не откатывать
и не переписывать:

- Т1 — из раздела Олимпиады убраны Каталог, Предикт методов, Прогресс.
- Т2 — `display_name_from_email()` в `services/user_helpers.py`,
  лидеры без почтовых адресов.
- Т3 — куратор приветствует по имени.
- Т4 — триал одни сутки, `trial_started_at`, `has_access()`.
- Т7 — план куратора на месяц 2 и далее, `curator_plan_service.py`.
- Т8 — серия дней на 100 процентов, `streak_service.py`, выходные.
- Т9 — приоритет подписчиков в очереди чертежей.

### Код блока — не намёк на прошлую работу

Коды блоков здесь начинаются на Т. Коды прошлой цепочки начинались
на Ч, Х, В, Д, П, К, И, Л. Числовое совпадение ничего не значит.

Блок Т5 не имеет отношения к слою Ч5. Блок Т6 не имеет отношения
к слою Ч6. Это разные задачи, совпадение цифр случайное. В прошлый
заход ROO их спутал и пропустил оба. Повторять эту ошибку запрещено.

Блок нельзя объявить выполненным на основании похожего номера в чужом
отчёте. Единственное допустимое основание пропустить блок — запись
именно этого кода со статусом ГОТОВ в `_recon/CHAIN_RUN.md`.

### Базовая линия снимается фактически

Первое, что делает первый блок задания:

```powershell
python -m pytest -q --tb=line
```

Четыре числа записываются в отчёт. Ориентир: passed около 956,
failed 37, skipped 17, errors 0. Ориентир не подставляется в отчёт
вместо фактического вывода.

В отчёте Т1 прошлого захода стоит 959 passed и 39 failed — рост failed
на две единицы против 37. Установи, есть ли этот рост сейчас. Если есть —
назови оба упавших теста поимённо и запиши в
`ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА`, когда причина не в твоей работе.

### Состояние базы — это норма

`adaptive_tasks` = 0. `users` = 0. `solution_attempts` = 0. Таблицы
задач дня пусты. `method_tasks` = 860.

Любая приёмка, требующая данных, идёт на фикстурах из
`tests/conftest.py`. Пустая база не дефект и не повод поставить пункту
приёмки «пройдено» без проверки.

### Журнал

Перед началом каждого блока дописать в конец `_recon/CHAIN_RUN.md`:

```
## БЛОК <КОД> — СТАРТ
Время старта: <YYYY-MM-DD HH:MM>
```

По завершении блока — карточка:

```
## БЛОК <КОД> — <ГОТОВ|ЧАСТИЧНО|СОРВАЛСЯ>
Время старта: <YYYY-MM-DD HH:MM>
Время финиша: <YYYY-MM-DD HH:MM>
Файлы изменены: <список с числом строк каждого>
Файлы созданы: <список>
Миграции: <список или НЕТ>
Копия базы: <имя файла или НЕТ>
Коммит: <хеш и сообщение или НЕТ, причина>
Тесты базовая линия: passed=<N> failed=<N> skipped=<N> errors=<N>
Тесты после: passed=<N> failed=<N> skipped=<N> errors=<N>
Приёмка: <пройдено> из <всего> (<процент>)
NOT FOUND: <список или НЕТ>
СМЕЖНОЕ: <дефекты для чужих блоков или НЕТ>
ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА: <список или НЕТ>
Подробный отчёт: _recon/<КОД>.md
```

Журнал дописывается в конец. Чужие записи не трогаются.

### Порог приёмки

80 процентов и выше — ГОТОВ, переходишь к следующему блоку задания.
Ниже 80 процентов — ЧАСТИЧНО, останавливаешься и объясняешь человеку,
что помешало. Следующий блок в этом случае не начинаешь.

Пункт, проверка которого не выполнялась, считается непройденным.

### Порог по тестам

Если `failed` или `errors` выросли хотя бы на единицу против снятой
базовой линии — разбери причину и верни показатели. Не удалось —
статус СОРВАЛСЯ, остановись.

Удалять тест, помечать `skip` или `xfail` ради зелёного прогона запрещено.

### Коммит

```
git add -A
git commit -m "chain3b: <КОД БЛОКА> <краткая суть>"
```

Каждый блок коммитится отдельно, сразу после своей приёмки.

### Доказательство

Отчёт состоит из фактов. Доказательство — HTTP через `app.test_client()`,
дамп базы, фактический фрагмент HTML или вывод pytest.

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('<путь>', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1500])
```

### Запрещённые слова в отчёте

примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается.

Нет файла или функции — пишется ровно `NOT FOUND`.

### Общие запреты

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase,
  изменение истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
  Push в `main` не делается.
- Формулы уровня не меняются: `mu += 0.22*(sigma+0.3)`,
  `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5], `DEFAULT_MU = 3.0`,
  `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена. 35 якорей, все level = 3.
- Редактирование текстов задач запрещено. Генерация условий задач кодом
  для рабочей базы запрещена. Синтетические тексты только в фикстурах
  `tests/` и только с пометкой `[TEST]`.
- Ключи только из `.env`. В отчётах, логах и коммитах — `СКРЫТО`.
- Перед любой миграцией — копия базы `formyla.db.bak_<КОД БЛОКА>_pre`.
- Методы (`olympiad_theory`, 102 записи) не трогать.
- Никаких ЕГЭ и ОГЭ. Продукт только олимпиадный.
- Ни одного эмодзи в коде, шаблонах, отчётах и сообщениях.
- Работу шести выполненных блоков не переделывать и не откатывать.

### Механика, которую менять нельзя

Пять якорей в порядке algebra, number_theory, geometry, combinatorics,
logic. Верный ответ +1, частичный 0, неверный −2. Пятибалльная шкала.
43 темы. В подтеме пять задач.

### Эталон дизайна

`--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
`--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF`
`--ok:#3ECF8E` `--warn:#E5AC3A` `--bad:#E86A62`

Радиус карточек 14px, кнопок 10px. Satoshi и system-ui. Контейнер 1120px.

### Когда остановиться

- приёмка блока ниже 80 процентов;
- `failed` или `errors` выросли и вернуть их не удалось;
- копию базы сделать не удалось, а блок требует миграции;
- `import app` падает;
- выполнение требует нарушить любой запрет устава.

При остановке — карточка в журнал, объяснение человеку, следующий блок
не начинать.

---

# БЛОК 1 из 3 — Т5 — ПЕРЕСЧЁТ СТОИМОСТИ КАЖДОЙ ФУНКЦИИ

## Контекст

- **Важно про код блока.** Этот блок называется Т5. Он не имеет никакого отношения
  к слою Ч5 из прошлой цепочки (маршрут `/figures` и фоновая очередь) и не является
  его продолжением. Совпадение цифры 5 случайное. Отчёт `_recon/CH5.md` смежную
  область не покрывает. Пропускать этот блок под предлогом того, что Ч5 уже сделан,
  запрещено. Задача Т5 — пересчёт стоимости каждой функции на сайте, и она
  не выполнялась ни разу.
- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Auto-Deploy OFF. База пуста — норма, приёмка на фикстурах из `tests/conftest.py` (Ф0).
- Бизнес-модель: подписка 400₽/мес, включает 7 срезов в месяц. Кредиты на чертежи: 3 бесплатных
  при регистрации, пакеты 10/30/100 за 99/249/599₽. Проверка фото через Kimi: 0.35₽/фото, входит
  в подписку. DeepSeek для генерации чертежей: реальная стоимость для платформы 0.5₽/чертёж.
  OpenRouter для задач дня. Олимпиадные методы: 102 записи, доступны бесплатно.
- `templates/pricing.html` переписан блоком К1 — показывает пакеты кредитов и цены. Маршрут
  `/pricing` существует. `services/yookassa_stub.py` — заглушка оплаты. Модели `User.figure_credits`,
  `FigureCreditTransaction`, `FigureGeneration` — живые.
- `app.py` — 12632 строки, 161 маршрут. `models.py` — фактическое число строк зафиксировать.
- Базовая линия тестов: 939 passed, 37 failed, 17 skipped, 0 errors (после v2). Новый блок не
  должен ухудшить `failed` и `errors`.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`; радиус карточек 14px, кнопок 10px, Satoshi + system-ui,
  контейнер 1120px, ни одного эмодзи. Ключи из `.env`, в отчётах пишется `СКРЫТО`.

## Что уже есть

Перед правками ROO фиксирует фактом, без пересказа:

1. Полный текст `templates/pricing.html` — какие цены, пакеты и функции указаны. Совпадают ли
   числа с записанными: 10/99, 30/249, 100/599, 3 бесплатных, подписка 400₽. Каждое
   расхождение выписать отдельно.
2. Маршрут `/pricing` в `app.py` — номер строки декоратора и функции, какой шаблон рендерится,
   какой контекст передаётся.
3. Места в коде, где фигурируют цены и стоимости: grep `400`, `99`, `249`, `599`, `0.35`,
   `0.5` по `*.py` и `templates/**/*.html` — полный список файлов и строк с контекстом.
4. Модель `User` — есть ли поле `figure_credits`, значение по умолчанию. Модели
   `FigureCreditTransaction`, `FigureGeneration` — список полей. Модель подписки: есть ли
   поля `subscription_status`, `subscription_expires`, `trial_end` или аналогичные. Если
   полей подписки нет — `NOT FOUND`.
5. Сервис `services/cost_calculation.py` — если существует, полный текст. Если нет — `NOT FOUND`.
6. Маршруты и функции для каждой проверяемой функции: срез (`/prep/probe` в `routes/prep.py`),
   задачи дня (`daily_tasks/routes.py`), генерация чертежа (`/figures` в `routes/figures.py`),
   проверка фото Kimi (`services/kimi_review.py`), олимпиадные методы (`routes/olympiad.py`).
   Для каждого — есть ли проверка подписки, пробного периода, paywall. Привести номера строк.
7. Фикстуры `tests/conftest.py` — есть ли фикстуры, моделирующие подписку, пробный период,
   paywall.

## Что сделать

1. Первой строкой в журнале `_recon/CHAIN_RUN.md` написать `## БЛОК T5 CODE — СТАРТ` до начала
   любой работы. Если файла нет — создать.

2. Создать `services/cost_calculation.py` с функциями расчёта стоимости для каждой функции
   платформы. Каждая функция возвращает кортеж `(user_cost, platform_cost)` — стоимость для
   пользователя в рублях и реальная стоимость для платформы в рублях:
   - `slice_cost(is_subscribed)` — срез: 0₽ для пользователя (входит в подписку 400₽/мес),
     стоимость для платформы: 0₽ (локальный расчёт уровня, без внешних API).
   - `daily_task_cost(is_subscribed)` — задача дня: 0₽ для пользователя, стоимость для
     платформы: вызов OpenRouter (считать по тарифу, ключ `СКРЫТО`, если ключа нет — `NOT FOUND`
     и оценку не писать, только структуру).
   - `figure_cost(figure_credits, has_subscription)` — генерация чертежа: если `figure_credits`
     больше 0 — 0₽ для пользователя (бесплатный кредит), иначе цена пакета; стоимость для
     платформы: 0.5₽ (DeepSeek API).
   - `kimi_check_cost(is_subscribed)` — проверка фото: 0₽ для пользователя (входит в подписку),
     стоимость для платформы: 0.35₽.
   - `olympiad_methods_cost()` — олимпиадные методы: 0₽ для пользователя, 0₽ для платформы
     (статический контент, 102 записи).
   - `subscription_cost()` — подписка: 400₽ для пользователя, 0₽ для платформы (выручка).
   - `figure_package_cost(package_size)` — пакет кредитов: 99₽ для 10, 249₽ для 30, 599₽ для 100;
     стоимость для платформы: `package_size * 0.5`₽ (DeepSeek API).
   - `monthly_platform_cost_per_user()` — сводная стоимость одного ученика в месяц: 7 срезов
     (0₽ платформе) + проверка фото (14₽, из расчёта 40 проверок в месяц по 0.35₽) + чертежи
     (по факту использования кредитов).

3. Обновить `templates/pricing.html` — страница должна показывать все цены и пакеты:
   - Подписка: 400₽/мес, что входит: 7 срезов, проверка фото через Kimi, олимпиадные методы.
   - Кредиты на чертежи: 3 бесплатных при регистрации, пакеты 10/30/100 за 99/249/599₽.
   - Задачи дня: бесплатно, входят в подписку.
   - Олимпиадные методы: бесплатно.
   - Срез: входит в подписку (отдельно не оплачивается).
   - Проверка фото: входит в подписку.
   - Все цены в рублях, дизайн по токенам эталона, ни одного эмодзи.

4. Проверить, что `templates/pricing.html` показывает актуальные цены. Сверить каждое число в
   шаблоне с записанными значениями (400, 99, 249, 599, 10, 30, 100, 3, 7). Если в шаблоне
   другие числа — исправить, привести в отчёте «было / стало».

5. Создать или обновить маршрут `/pricing` так, чтобы он передавал в шаблон данные из
   `services/cost_calculation.py` — список функций с их стоимостью для пользователя. Шаблон
   рендерит таблицу: функция, стоимость для пользователя, что входит. Не дублировать цены
   жёстко в шаблоне — брать из контекста, передаваемого маршрутом, где это возможно (числа
   пакетов и цен можно оставить в шаблоне, если они статичны, но стоимость подписки и состав
   передавать из `cost_calculation`).

6. Сверить отображаемые цены с реальными: для каждой функции из пункта 2 проверить, что цена,
   которую видит пользователь в интерфейсе (`/pricing`, кнопки покупки, paywall), совпадает с
   расчётом `services/cost_calculation.py`. Расхождения — списком в отчёте, исправить
   точечно.

7. Написать тесты в `tests/test_t5_cost.py` на фикстурах Ф0:
   - `slice_cost(True)` возвращает `(0, 0)`.
   - `figure_cost(3, True)` возвращает `(0, 0.5)` — пользователь платит 0 (бесплатный кредит),
     платформа 0.5₽.
   - `figure_cost(0, True)` — цена пакета для пользователя (не 0), платформа 0.5₽.
   - `kimi_check_cost(True)` возвращает `(0, 0.35)`.
   - `olympiad_methods_cost()` возвращает `(0, 0)`.
   - `subscription_cost()` возвращает `(400, 0)`.
   - `figure_package_cost(10)` возвращает `(99, 5)`, `figure_package_cost(30)` — `(249, 15)`,
     `figure_package_cost(100)` — `(599, 50)`.
   - GET `/pricing` — 200, HTML содержит числа 400, 99, 249, 599.
   - Тесты не пишут в `instance/formyla.db`.

8. Написать отчёт `_recon/T5.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T5_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно: не удалять и не переписывать `services/yookassa_stub.py` и логику списания
  кредитов из блока К1; не менять реальные вызовы внешних API (DeepSeek, Kimi, OpenRouter) —
  только расчёт стоимости; не печатать значения ключей из `.env`; не менять модель подписки,
  если она существует — только фиксировать факт.

## ПРИЁМКА

Каждый пункт проверяется на фикстурах из `tests/conftest.py` (Ф0). Команда целиком, вывод как есть.

### 1. services/cost_calculation.py — функции возвращают правильные стоимости

```python
from services.cost_calculation import (
    slice_cost, daily_task_cost, figure_cost, kimi_check_cost,
    olympiad_methods_cost, subscription_cost, figure_package_cost,
    monthly_platform_cost_per_user,
)
print('SLICE', slice_cost(True))
print('FIGURE_FREE', figure_cost(3, True))
print('FIGURE_NO_CREDITS', figure_cost(0, True))
print('KIMI', kimi_check_cost(True))
print('METHODS', olympiad_methods_cost())
print('SUB', subscription_cost())
print('PKG_10', figure_package_cost(10))
print('PKG_30', figure_package_cost(30))
print('PKG_100', figure_package_cost(100))
print('MONTHLY', monthly_platform_cost_per_user())
```

Ожидаемый вывод: `SLICE (0, 0)`, `FIGURE_FREE (0, 0.5)`, `FIGURE_NO_CREDITS` — цена пакета
для пользователя (больше 0) и `0.5` для платформы, `KIMI (0, 0.35)`, `METHODS (0, 0)`,
`SUB (400, 0)`, `PKG_10 (99, 5)`, `PKG_30 (249, 15)`, `PKG_100 (599, 50)`, `MONTHLY` —
сводная сумма для платформы (включает 14₽ за проверки Kimi).

### 2. GET /pricing — 200, цены в HTML

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/pricing', follow_redirects=True)
text = r.data.decode('utf-8')
print('STATUS', r.status_code)
for num in ['400', '99', '249', '599', '10', '30', '100']:
    print(f'HAS_{num}', num in text)
```

Ожидаемый вывод: `STATUS 200`, `HAS_400 True`, `HAS_99 True`, `HAS_249 True`, `HAS_599 True`,
`HAS_10 True`, `HAS_30 True`, `HAS_100 True`.

### 3. Цены пакетов совпадают с записанными

```python
text = open('templates/pricing.html', encoding='utf-8').read()
pairs = [('10', '99'), ('30', '249'), ('100', '599')]
for count, price in pairs:
    print(count, price, count in text and price in text)
```

Ожидаемый вывод: три строки, каждая заканчивается `True`.

### 4. Подписка и состав в pricing.html

```python
text = open('templates/pricing.html', encoding='utf-8').read()
print('HAS_SUB_400', '400' in text)
print('HAS_7_SLICES', '7' in text and ('срез' in text.lower() or 'slice' in text.lower()))
print('HAS_KIMI', 'kimi' in text.lower() or 'фото' in text.lower())
print('HAS_METHODS_FREE', 'бесплатн' in text.lower())
```

Ожидаемый вывод: `HAS_SUB_400 True`, `HAS_7_SLICES True`, `HAS_KIMI True`, `HAS_METHODS_FREE
True`.

### 5. Дизайн-токены и эмодзи в pricing.html

```python
import re
ALLOWED = {'#070C18','#0E1830','#121F3C','#1C2B4F','#E6EBF7','#8C9ABC','#4C7DFF','#6B95FF','#3ECF8E','#E5AC3A','#E86A62'}
emoji = re.compile(r'[\U0001F300-\U0001FAFF\u2600-\u27BF\u2190-\u21FF\u2700-\u27BF]')
text = open('templates/pricing.html', encoding='utf-8').read()
foreign = set(m.upper() for m in re.findall(r'#[0-9A-Fa-f]{6}', text)) - ALLOWED
print('FOREIGN_HEX', foreign if foreign else 'NONE')
print('EMOJI', bool(emoji.search(text)))
```

Ожидаемый вывод: `FOREIGN_HEX NONE`, `EMOJI False`.

### 6. Маршрут /pricing передаёт данные из cost_calculation

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
r = c.get('/pricing', follow_redirects=True)
print('STATUS', r.status_code)
print('LEN', len(r.data))
print(r.data.decode('utf-8')[:1500])
```

Ожидаемый вывод: `STATUS 200`, `LEN` больше нуля, фрагмент HTML с таблицей или карточками цен.

### 7. Полный прогон тестов не хуже базовой линии

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <M> failed, <K> skipped, <E> errors in <T>s`, где `M` не больше 37,
`E` не больше 0, `N` не меньше 939.

## Отчёт

Писать в `_recon/T5.md`. Обязательные разделы:

1. Заголовок и дата.
2. Таблица «функция -> стоимость для пользователя -> стоимость для платформы» — все функции из
   пункта 2 «Что сделать», с фактическими числами.
3. Фактическое состояние `templates/pricing.html` до правок — какие цены были, что не совпадало
   с записанными значениями.
4. Текст `services/cost_calculation.py` — список функций с сигнатурами и возвращаемыми
   значениями.
5. Обновлённый `templates/pricing.html` — что добавлено, что исправлено (было/стало по каждому
   расхождению).
6. Вывод всех семи пунктов ПРИЁМКА — целиком, без пересказа.
7. Раздел «Сверка цен» — для каждой функции: цена в интерфейсе, цена из `cost_calculation`,
   совпадает или нет. Расхождения — списком с исправлением.
8. Раздел «Отклонения» — если функция не найдена, маршрут отсутствует, ключ API недоступен —
   `NOT FOUND` и объяснение.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается. Эти слова допустимы только
в перечислении. Нет файла или функции — писать ровно `NOT FOUND`.

---

# БЛОК 2 из 3 — Т11 — ПРОВЕРКА ПАМЯТИ RENDER НА 300 УЧЕНИКОВ

Начинай только если блок Т5 закрыт статусом ГОТОВ.

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Auto-Deploy OFF. База пуста — норма. Приёмка на фикстурах из `tests/conftest.py` (Ф0).
- Прод: Render `srv-d73br5ffte5s73euc56g`, домен `formyla.net`, база PostgreSQL, Auto-Deploy OFF.
  На проде 8778 задач и 9039 пользователей. Выкладка выполняется человеком вручную.
- `app.py` — 12632 строки. `models.py` — фактическое число строк зафиксировать. Локальная база
  `instance/formyla.db` — 32 МБ. Базовая линия тестов: 939 passed, 37 failed, 17 skipped, 0 errors.
- Render-планы: Free — 512 МБ RAM, Starter — 512 МБ RAM, Standard — 2 ГБ RAM, Pro — 4 ГБ RAM.
  Какой план используется — выяснить без касания прод-сервера, по статическим данным.
- Memory-intensive операции: генерация чертежей через DeepSeek (ответ до 300 с), проверка фото
  Kimi (base64 в памяти), рендеринг SVG (`geometric_engine/engine.py` — 1353 строки), очередь
  построений (`drawing_async.py` — in-memory `_STORE`, TTL 1800 с). OpenRouter — таймаут 300 с.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`. Ни одного эмодзи. Ключи из `.env`, в отчётах пишется `СКРЫТО`.
- Этот блок НЕ правит код. Только статический анализ, расчёт и отчёт. Нагрузочное тестирование
  запрещено. Касание прод-сервера и Render API запрещено.

## Что уже есть

Перед расчётом ROO фиксирует фактом, без пересказа:

1. Размер `app.py`, `models.py`, `routes/prep.py`, `daily_tasks/services.py`,
   `geometric_engine/engine.py`, `services/drawing_async.py` — число строк каждого. Размер
   `instance/formyla.db` в байтах. Для каждого отсутствующего — `NOT FOUND`.
2. Количество моделей — grep `class .*db\.Model` по всем `*.py`. Количество маршрутов —
   grep `@.*\.route\(` по всем `*.py`. Точные числа.
3. Размер базы `instance/formyla.db` — `os.path.getsize` в МБ. Число таблиц (`SELECT count(*)`
   из `sqlite_master`). Если базы нет — `NOT FOUND`.
4. Наличие `render.yaml`, `Dockerfile`, `Procfile` — содержимое каждого. Какой план Render
   указан. Если ни одного нет — `NOT FOUND`.
5. Наличие переменных окружения, указывающих на план Render: grep `RENDER`, `PLAN`, `RAM`,
   `MEMORY` по `.env`, `.env.example`, `render.yaml`. Если не найдено — `NOT FOUND`.
6. Memory-intensive операции — найти в коде:
   - Вызовы DeepSeek: grep `deepseek\|DEEPSEEK\|FIGURE_MODEL` — файлы, строки, таймаут.
   - Вызовы Kimi: grep `kimi\|KIMI` — где хранится base64, сколько памяти на одно фото.
   - SVG-рендеринг: `geometric_engine/engine.py` — структуры данных, кэширование.
   - Очередь: `drawing_async.py` — размер `_STORE`, TTL, что хранится.
   - OpenRouter: grep `openrouter\|OPENROUTER` — файлы, строки, таймаут.
7. Средний размер страницы — для `/`, `/figures`, `/pricing` получить размер HTML через
   `app.test_client()` на фикстурах Ф0. Записать `len(r.data)` для каждого.
8. WSGI-сервер — grep `gunicorn`, `waitress` по `requirements.txt`. Сколько воркеров по
   умолчанию. Тяжёлые зависимости: numpy, pandas, matplotlib, PIL, torch — что установлено.

## Что сделать

1. Первой строкой в журнале `_recon/CHAIN_RUN.md` написать `## БЛОК T11 CODE — СТАРТ` до начала
   любой работы. Если файла нет — создать.

2. Определить текущий план Render по статическим данным (пункт 4 «Что уже есть»). Если в
   `render.yaml` указан план — привести дословно. Если файла нет — `NOT FOUND` и явно указать,
   что план неизвестен из локальных данных, привести список планов с лимитами RAM.

3. Составить статический расчёт потребления памяти для 300 одновременных учеников:
   - Базовое потребление процесса Flask (B): оценить по размеру `app.py` и числу моделей.
     Размер исходного кода не равен памяти, но коррелирует с числом импортированных модулей.
     Если `psutil` или `resource` доступны — запустить процесс на локальной фикстурной базе и
     привести фактическое потребление. Если нет — оценка на основе размера кода и зависимостей.
   - Сессии (S): 300 одновременных сессий. Flask по умолчанию использует cookie-based сессии —
     память на сервере не расходуется. Если в проекте серверные сессии (grep `SESSION_TYPE`,
     `flask_session`) — оценить размер одной и умножить на 300. Если cookie-based — `S = 0`.
   - Рендеринг страниц (R): средний размер страницы из пункта 7, коэффициент одновременности
     0.3 (90 одновременных рендеров). `R = 90 * средний_размер_страницы`.
   - Очередь построений (Q): до 300 записей в `_STORE`, размер одной — средний размер SVG из
     `data/figures/` или `_recon/ch6_svg/`. Если SVG недоступны — `NOT FOUND` и оценка по
     типичному размеру SVG (5–50 КБ).
   - Проверки Kimi (K): фото после сжатия до 1500px/0.8 — 200–500 КБ, base64 добавляет 33
     процента. Одно фото — 300–700 КБ. При 90 одновременных (коэффициент 0.3) — 27–63 МБ.
   - Итого: `B + S + R + Q + K` в МБ. Для каждого компонента, где точное число недоступно —
     `NOT FOUND` и оценка на основе известных размеров.

4. Множитель воркеров: если WSGI-сервер — gunicorn с N воркеров, базовое потребление
   умножается на N (каждый воркер — отдельный процесс со своей памятью). Указать число
   воркеров или `NOT FOUND`, если неизвестно.

5. Рекомендация: для каждого плана Render (Free 512 МБ, Starter 512 МБ, Standard 2 ГБ,
   Pro 4 ГБ) — помещается ли пиковое потребление. При каком числе одновременных учеников план
   перестаёт справляться. Достаточно ли текущего плана или нужен апгрейд.

6. Написать отчёт `_recon/T11.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_T11_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно: никакой правки кода — этот блок только читает и считает; нагрузочное
  тестирование запрещено (`locust`, `wrk`, `ab`, `pytest-benchmark`); обращение к Render API,
  SSH на прод, подключение к прод-базе PostgreSQL запрещены; если данных для точного расчёта
  нет — писать `NOT FOUND` и давать оценку на основе известных размеров.

## ПРИЁМКА

Каждый пункт — статический расчёт или проверка на локальных данных. Команда целиком, вывод как есть.

### 1. Размер app.py и ключевых файлов

```python
import os
files = ['app.py', 'models.py', 'routes/prep.py', 'daily_tasks/services.py',
         'geometric_engine/engine.py', 'services/drawing_async.py', 'instance/formyla.db']
for f in files:
    if os.path.exists(f):
        size = os.path.getsize(f)
        lines = len(open(f, encoding='utf-8', errors='ignore').readlines()) if f.endswith('.py') else 'N/A'
        print(f'{f} : size={size} bytes lines={lines}')
    else:
        print(f'{f} : NOT FOUND')
```

Ожидаемый вывод: для каждого файла — размер и число строк, либо `NOT FOUND`.

### 2. Число моделей и маршрутов

```python
import re, glob
models = routes = 0
for path in glob.glob('**/*.py', recursive=True):
    if '/.git/' in path: continue
    text = open(path, encoding='utf-8', errors='ignore').read()
    models += len(re.findall(r'class\s+\w+.*db\.Model', text))
    routes += len(re.findall(r'@\w+\.route\(', text))
print('MODELS', models)
print('ROUTES', routes)
```

Ожидаемый вывод: `MODELS <N>`, `ROUTES <N>` — фактические числа.

### 3. Размер базы и число таблиц

```python
import os, sqlite3
path = 'instance/formyla.db'
if os.path.exists(path):
    print('DB_SIZE_MB', round(os.path.getsize(path) / 1024 / 1024, 2))
    conn = sqlite3.connect(path)
    print('TABLES', conn.execute("SELECT count(*) FROM sqlite_master WHERE type='table'").fetchone()[0])
else:
    print('DB NOT FOUND')
```

Ожидаемый вывод: `DB_SIZE_MB 32.0` (или фактическое), `TABLES 63` (или фактическое).

### 4. План Render по статическим данным

```python
import os
for f in ['render.yaml', 'Dockerfile', 'Procfile']:
    if os.path.exists(f):
        print(f'=== {f} ===')
        print(open(f, encoding='utf-8').read()[:800])
    else:
        print(f'{f} : NOT FOUND')
```

Ожидаемый вывод: содержимое найденных файлов, либо `NOT FOUND` для каждого. Если план указан —
дословно. Если не указан — `NOT FOUND` и список планов с лимитами.

### 5. Средний размер страниц

```python
import app as A
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
for path in ['/', '/figures', '/pricing']:
    r = c.get(path, follow_redirects=True)
    print(path, 'STATUS', r.status_code, 'SIZE', len(r.data))
```

Ожидаемый вывод: для каждого маршрута — код ответа и размер HTML в байтах.

### 6. Memory-intensive операции — grep

```python
import glob, re
patterns = {'deepseek': r'deepseek|DEEPSEEK|FIGURE_MODEL', 'kimi': r'kimi|KIMI',
            'openrouter': r'openrouter|OPENROUTER', 'store': r'_STORE|drawing_async'}
for name, pat in patterns.items():
    hits = []
    for path in glob.glob('**/*.py', recursive=True):
        if '/.git/' in path: continue
        for i, line in enumerate(open(path, encoding='utf-8', errors='ignore').readlines(), 1):
            if re.search(pat, line, re.IGNORECASE):
                hits.append(f'{path}:{i}:{line.strip()[:90]}')
    print(f'=== {name} ({len(hits)}) ===')
    for h in hits[:8]: print(h)
```

Ожидаемый вывод: для каждого паттерна — число находок и первые 8 строк.

### 7. Тяжёлые зависимости и WSGI-сервер

```python
import os
text = open('requirements.txt', encoding='utf-8').read() if os.path.exists('requirements.txt') else ''
for h in ['numpy', 'pandas', 'matplotlib', 'Pillow', 'torch', 'gunicorn', 'waitress']:
    print(h, h.lower() in text.lower())
```

Ожидаемый вывод: `True` или `False` для каждой зависимости.

### 8. Отчёт о памяти существует

```python
import os
print('REPORT_EXISTS', os.path.exists('_recon/T11.md'))
```

Ожидаемый вывод: `REPORT_EXISTS True` после написания отчёта.

## Отчёт

Писать в `_recon/T11.md`. Обязательные разделы:

1. Заголовок и дата.
2. Текущий план Render — дословно из `render.yaml` или `NOT FOUND`. Список всех планов с
   лимитами RAM: Free 512 МБ, Starter 512 МБ, Standard 2 ГБ, Pro 4 ГБ.
3. Статический размер проекта: `app.py` (строки), `models.py` (строки), число моделей, число
   маршрутов, размер базы (МБ), число таблиц.
4. Средний размер страниц — таблица: маршрут, размер HTML (байты).
5. Memory-intensive операции — для каждой: файл, строка, таймаут, что хранится в памяти, оценка
   размера одной операции.
6. Расчёт пикового потребления для 300 одновременных учеников:
   - B (базовое), S (сессии), R (рендеринг, коэффициент 0.3), Q (очередь), K (Kimi, коэффициент
     0.3) — каждый с числом или `NOT FOUND` и оценкой.
   - Итого: `B + S + R + Q + K` в МБ.
7. Множитель воркеров — число воркеров или `NOT FOUND`, итоговое потребление с учётом.
8. Тяжёлые зависимости — список установленных, оценка вклада каждой.
9. Рекомендация: для каждого плана — помещается ли, при каком числе учеников перестаёт
   справляться, достаточно ли текущего плана или нужен апгрейд.
10. Раздел «Отклонения» — `NOT FOUND` для каждого недоступного компонента, с указанием, на
    основе каких известных размеров сделана оценка.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается. Эти слова допустимы только
в перечислении. Нет файла или функции — писать ровно `NOT FOUND`.

---

# БЛОК 3 из 3 — ФИНАЛ — КОНТРОЛЬ ВСЕХ ОДИННАДЦАТИ ЗАДАЧ

Начинай только если блок Т11 закрыт статусом ГОТОВ.

## Контекст

- Проект FORMYLA, Flask, SQLite, база `instance/formyla.db`. Работаем локально, прод и Render не
  трогаем. Auto-Deploy OFF. База пуста — норма, приёмка на фикстурах из `tests/conftest.py` (Ф0).
- Это финальный блок цепочки v3. Идёт после всех одиннадцати блоков: Т1–Т11. Следующего диалога
  этот блок не создаёт — цепочка на нём закрывается.
- Прод: Render `srv-d73br5ffte5s73euc56g`, домен `formyla.net`, база PostgreSQL, Auto-Deploy OFF.
  На проде 8778 задач и 9039 пользователей. Выкладка выполняется человеком вручную.
- Базовая линия тестов: 939 passed, 37 failed, 17 skipped, 0 errors (после v2, `_recon/R0.md`).
  Новый прогон не должен ухудшить `failed` и `errors`.
- `_recon/CHAIN_RUN.md` — журнал цепочки, куда каждый блок обязан отписаться карточкой. Если
  файла нет — `NOT FOUND` и восстановление списка по наличию отчётов в `_recon/`.
- Эталон дизайна: `--bg:#070C18` `--surface:#0E1830` `--surface-2:#121F3C` `--border:#1C2B4F`
  `--text:#E6EBF7` `--muted:#8C9ABC` `--accent:#4C7DFF` `--accent-hi:#6B95FF` `--ok:#3ECF8E`
  `--warn:#E5AC3A` `--bad:#E86A62`. Радиус 14px/10px. Satoshi + system-ui. Контейнер 1120px.
  Ни одного эмодзи. Ключи из `.env`, в отчётах пишется `СКРЫТО`.
- Правило блока: нашёл дефект — чинишь на месте, доказываешь повторным прогоном той же команды.
  Обязательна строка НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО. Починить нельзя без нарушения запретов —
  писать `ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА`.

## Что уже есть

1. Полный список файлов в `_recon/` — отчёты `T1.md`–`T11.md`, `R0.md`. Для каждого — существует
   или `NOT FOUND`.
2. Содержимое `_recon/CHAIN_RUN.md` — для каждого кода Т1–Т11: отписался или
   `БЛОК Т<N> НЕ ОТПИСАЛСЯ`.
3. Результат последнего полного прогона тестов из отчётов предыдущих блоков — числа
   `passed/failed/skipped/errors` на момент начала этого блока.
4. Список шаблонов, созданных или изменённых блоками Т1–Т11, собранный по текстам их отчётов.
   Если отчёт блока отсутствует — `NOT FOUND` для списка шаблонов этого блока.

## Что сделать

1. Первой строкой в журнале `_recon/CHAIN_RUN.md` написать `## БЛОК P_FINAL_V3 CODE — СТАРТ`
   до начала любой работы.

2. Для каждого из одиннадцати блоков (Т1–Т11) проверить: отчёт в `_recon/` существует; карточка
   в `_recon/CHAIN_RUN.md` присутствует; тесты не хуже базовой линии (939/37/17/0) — сверить с
   числами в отчёте блока.

3. Специфические проверки по блокам (на фикстурах Ф0, через `app.test_client()`):
   - **Т1** — удалённые разделы дают 404. Список удалённых маршрутов из `_recon/T1.md`, GET на
     каждый — 404.
   - **Т2** — ни одного `@` в HTML лидеров. GET страницы лидеров, `text.count('@')` равно 0.
   - **Т3** — куратор приветствует по имени. GET главной — HTML содержит имя пользователя.
   - **Т4** — пробный период 1 день работает, paywall работает. Пользователь с `trial_end` в
     прошлом — paywall блокирует. С `trial_end` в будущем — доступ открыт.
   - **Т5** — таблица стоимости существует, цены актуальны. `services/cost_calculation.py`
     существует, `templates/pricing.html` содержит 400, 99, 249, 599.
   - **Т6** — виджеты переносятся на главную. POST `/dashboard/settings` с visible=True — виджет
     на главной. visible=False — скрыт.
   - **Т7** — месяц 2 выдаёт следующие 7 подтем. GET с month=1 и month=2 — разные списки.
   - **Т8** — серия 100%, выходные работают. Серия верных ответов — прогресс 100%. Расписание
     выходных — задачи не выдаются в выходной.
   - **Т9** — приоритет подписчиков в очереди, позиция видна. Подписчик впереди не-подписчика.
   - **Т10** — CRUD группы, дашборд учителя, дашборд родителя. Создать/прочитать/обновить/
     удалить — корректные коды. Дашборды — 200.
   - **Т11** — отчёт о памяти Render существует. `_recon/T11.md` присутствует, содержит расчёт
     пикового потребления и рекомендацию по плану.

4. Общие проверки:
   - Ноль эмодзи в коде и шаблонах (кроме `avatar_emoji` и пользовательских данных).
   - Дизайн-токены во всех новых шаблонах — только hex из эталона, никаких посторонних.
   - `git status` — без признаков `git push` или обращения к Render API.

5. Финальная регрессия: полный `python -m pytest tests/ --tb=short -q`. Таблица «Р0 / сейчас»
   по `passed/failed/skipped/errors`. Каждое ухудшение — тройка НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.

6. Сводный список нерешённого: из отчётов всех блоков собрать каждый пункт `NOT FOUND`,
   «Отклонения» или `ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА`. Единый пронумерованный список с блоком-источником.

7. Финальный вердикт: `ГОТОВ К ВЫКЛАДКЕ`, если все одиннадцать блоков отписались, регрессия не
   хуже Р0, эмодзи 0, нерешённое пусто или не блокирует выкладку. Либо
   `ЧАСТИЧНО: <причины списком>` — каждая причина с указанием блока.

8. Написать отчёт `_recon/P_FINAL_V3.md` по форме из раздела «Отчёт».

## Что запрещено

- `git reset`, `git stash`, `git checkout`, `git rm`, force push, rebase, правка истории.
- Прод-база и Render не трогаются. Auto-Deploy остаётся OFF.
- Формулы `mu` и `sigma` не меняются:
  `mu += 0.22*(sigma+0.3)`, `sigma = max(0.35, sigma*0.94)`, `mu` в [1, 5],
  `DEFAULT_MU = 3.0`, `DEFAULT_SIGMA = 1.5`.
- Перезапись `data/anchors.jsonl` запрещена (35 якорей, все level = 3).
- Редактирование текстов задач запрещено. Генерация условий кодом запрещена.
- Ключи только из `.env`, в отчётах пишется `СКРЫТО`.
- Перед любой миграцией — копия базы `instance/formyla.db` с именем
  `formyla.db.bak_P_FINAL_V3_pre`.
- Ни одного эмодзи в коде, шаблонах, отчётах.
- Банк `grade_tasks` (1600 задач) заморожен, в выдаче не участвует.
- Никаких ЕГЭ и ОГЭ. Методы (`olympiad_theory`, 102 записи) не трогать.
- Дополнительно: ничего не выкладывать, не пушить, не трогать Render и прод-базу. Поле
  `avatar_emoji` и пользовательские данные не трогать при вычистке эмодзи. Если починка
  требует нарушить запрет — писать `ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА`.

## ПРИЁМКА

Каждый пункт — на фикстурах Ф0. Команда целиком, вывод как есть. Строка НАЙДЕНО / ПОЧИНЕНО /
ДОКАЗАНО по каждому.

### 1. Отчёты всех одиннадцати блоков существуют

```python
import os
for n in range(1, 12):
    print(f'T{n}', 'EXISTS' if os.path.exists(f'_recon/T{n}.md') else 'NOT FOUND')
```

Ожидаемый вывод: `T1 EXISTS` ... `T11 EXISTS`, либо `NOT FOUND` для отсутствующих.

### 2. Карточки блоков в CHAIN_RUN.md

```python
import os
if os.path.exists('_recon/CHAIN_RUN.md'):
    text = open('_recon/CHAIN_RUN.md', encoding='utf-8').read()
    for n in range(1, 12):
        print(f'T{n}', 'ОТПИСАЛСЯ' if f'Т{n}' in text or f'T{n}' in text else 'НЕ ОТПИСАЛСЯ')
else:
    print('CHAIN_RUN NOT FOUND')
```

Ожидаемый вывод: для каждого блока — `ОТПИСАЛСЯ` или `НЕ ОТПИСАЛСЯ`.

### 3. Т1–Т4 — удалённые 404, @ в лидерах, приветствие, paywall

```python
import app as A, os
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
print('T1_REPORT', 'EXISTS' if os.path.exists('_recon/T1.md') else 'NOT FOUND')
# ROO проверяет удалённые маршруты из _recon/T1.md: каждый STATUS 404
rl = c.get('/leaders', follow_redirects=True)
print('T2_AT_COUNT', rl.data.decode('utf-8').count('@'))
rh = c.get('/', follow_redirects=True)
print('T3_STATUS', rh.status_code)
rd = c.get('/dashboard', follow_redirects=True)
print('T4_STATUS', rd.status_code)
```

Ожидаемый вывод: `T1_REPORT EXISTS`, `T2_AT_COUNT 0`, `T3_STATUS 200`, `T4_STATUS 200` или
редирект на paywall. ROO проверяет оба сценария Т4 (истёк / активен). По каждому — НАЙДЕНО /
ПОЧИНЕНО / ДОКАЗАНО.

### 4. Т5–Т7 — стоимость, виджеты, темы

```python
import app as A, os
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
print('COST_SERVICE', os.path.exists('services/cost_calculation.py'))
text = open('templates/pricing.html', encoding='utf-8').read() if os.path.exists('templates/pricing.html') else ''
print('PRICES', all(n in text for n in ['400','99','249','599']))
print('T6_SETTINGS', c.get('/dashboard/settings', follow_redirects=True).status_code)
r1 = c.get('/methods?month=1', follow_redirects=True)
r2 = c.get('/methods?month=2', follow_redirects=True)
print('T7_M1_M2', r1.status_code, len(r1.data), r2.status_code, len(r2.data))
```

Ожидаемый вывод: `COST_SERVICE True`, `PRICES True`, `T6_SETTINGS 200`, оба `T7` — `200`, `M2`
отличается от `M1`. ROO проверяет добавление/скрытие виджетов по `_recon/T6.md`. По каждому —
НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.

### 5. Т8–Т11 — серия, очередь, группы, память

```python
import app as A, os
c = A.app.test_client()
with c.session_transaction() as s:
    s['_user_id'] = '1'
print('T8_PROBE', c.get('/prep/probe', follow_redirects=True).status_code)
print('T9_FIGURES', c.get('/figures', follow_redirects=True).status_code)
print('T10_GROUPS', c.get('/groups', follow_redirects=True).status_code)
print('T11_REPORT', os.path.exists('_recon/T11.md'))
```

Ожидаемый вывод: `T8_PROBE 200`, `T9_FIGURES 200`, `T10_GROUPS 200` или 302, `T11_REPORT True`.
ROO проверяет серию 100% и выходные по `_recon/T8.md`, приоритет подписчиков по `_recon/T9.md`,
полный CRUD и дашборды по `_recon/T10.md`. По каждому — НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.

### 6. Эмодзи — ноль в коде и шаблонах

```python
import re, glob
pattern = re.compile(r'[\U0001F300-\U0001FAFF\u2600-\u27BF\u2190-\u21FF\u2700-\u27BF]')
hits = []
for path in glob.glob('templates/**/*.html', recursive=True) + glob.glob('**/*.py', recursive=True):
    if 'group_chats' in path or '.env' in path:
        continue
    try:
        text = open(path, encoding='utf-8', errors='ignore').read()
    except Exception:
        continue
    for i, line in enumerate(text.splitlines(), 1):
        if pattern.search(line):
            hits.append((path, i))
print('EMOJI_HITS', len(hits))
```

Ожидаемый вывод: `EMOJI_HITS 0`.

### 7. Дизайн-токены во всех новых шаблонах

```python
import re, os
ALLOWED = {'#070C18','#0E1830','#121F3C','#1C2B4F','#E6EBF7','#8C9ABC','#4C7DFF','#6B95FF','#3ECF8E','#E5AC3A','#E86A62'}
templates = []  # ROO заполняет по отчётам Т1-Т11
for path in templates:
    if os.path.exists(path):
        text = open(path, encoding='utf-8').read()
        foreign = set(m.upper() for m in re.findall(r'#[0-9A-Fa-f]{6}', text)) - ALLOWED
        print(path, 'FOREIGN_HEX', foreign if foreign else 'NONE')
```

Ожидаемый вывод: `FOREIGN_HEX NONE` для каждого файла.

### 8. Полный прогон тестов — не хуже базовой линии

```powershell
python -m pytest tests/ --tb=short -q
```

Ожидаемый вывод: `<N> passed, <M> failed, <K> skipped, <E> errors in <T>s`, где `M` не больше 37,
`E` не больше 0, `N` не меньше 939. Таблица «Р0 / сейчас» по четырём числам.

## Отчёт

Писать в `_recon/P_FINAL_V3.md`. Обязательные разделы:

1. Заголовок и дата, ссылка на базовую линию (`_recon/R0.md`).
2. Статус всех одиннадцати блоков — отписался или `БЛОК Т<N> НЕ ОТПИСАЛСЯ`, для каждого: отчёт
   существует, приёмка пройдена, тесты не хуже базовой линии.
3. По каждому из восьми пунктов ПРИЁМКА — вывод, правка (если была), вывод после, строка
   НАЙДЕНО / ПОЧИНЕНО / ДОКАЗАНО.
4. Таблица «Р0 / сейчас» по `passed/failed/skipped/errors`, разбор ухудшений.
5. Сводный список нерешённого — пронумерованный, с формулировкой и блоком-источником.
6. Подтверждение, что push и обращения к Render не выполнялись.
7. Раздел «Не сделано / NOT FOUND» — буквально.
8. Финальный вердикт — ровно одна строка: `ГОТОВ К ВЫКЛАДКЕ` либо
   `ЧАСТИЧНО: <причины списком>`.

Запрещённые слова в отчёте: примерно, около, ~, архитектурно, выходит за рамки, pre-existing,
не связано с нашими изменениями, ожидаемо, выполняется, ожидается. Эти слова допустимы только
в перечислении. Нет файла или функции — писать ровно `NOT FOUND`.

---

# ФИНАЛЬНЫЙ ОТЧЁТ ЧЕЛОВЕКУ

Новый диалог не создавай, `new_task` не вызывай. Это конец всей работы.

Выведи человеку финальный отчёт ровно в таком виде, без сокращений
и без общих слов. Каждая строка таблицы — по фактической проверке,
а не по памяти о чужом отчёте.

```
ФИНАЛЬНЫЙ ОТЧЁТ FORMYLA — ОДИННАДЦАТЬ ЗАДАЧ

| № | Задача | Статус | Чем доказано |
|---|--------|--------|--------------|
| 1 | Убрать Каталог из Олимпиад | | |
| 2 | Убрать Предикт методов и Прогресс | | |
| 3 | Пересчитать стоимость каждой функции | | |
| 4 | Разделы родителя и учителя, группы | | |
| 5 | Приоритет ИИ для подписчиков, место в очереди | | |
| 6 | План на месяц 2 и далее | | |
| 7 | Перенос функций из Прочее на главную | | |
| 8 | Пробный период | | |
| 9 | Лидеры без почт, имя из почты | | |
| 10 | Куратор приветствует по имени | | |
| 11 | Памяти Render хватает на 300 учеников | | |
| 12 | Серия дней подряд | | |

Статус: ГОТОВО, ЧАСТИЧНО или НЕ СДЕЛАНО.
В графе «Чем доказано» — код ответа маршрута, имя прошедшего теста
или строка вывода команды. Не название отчёта.

Тесты базовая линия: passed=<N> failed=<N> skipped=<N> errors=<N>
Тесты сейчас: passed=<N> failed=<N> skipped=<N> errors=<N>
Эмодзи по проекту: <N>
Посторонних hex в шаблонах: <N>

НЕ РАБОТАЕТ И ПОЧЕМУ:
<перечень с причиной по каждому пункту>

ТРЕБУЕТ РЕШЕНИЯ ЧЕЛОВЕКА:
<перечень вопросов, по каждому — что именно нужно от человека>

ВЕРДИКТ: ГОТОВ К ВЫКЛАДКЕ | НЕ ГОТОВ, мешает: <перечень>
```

Задача под номером 12 в таблице — серия дней подряд, она была двенадцатым
пунктом в списке человека. Всего пунктов одиннадцать плюс серия, потому
что пункт про генерацию рисунков человек снял.
