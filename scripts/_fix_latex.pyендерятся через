#!/usr/bin/env python3 K
"""Fix broken LaTeX in adaptive_tasks via admin endpoint."""
import requestsaTeX
4. ✅

url = "https://formyla-com.onrender.com/admin/fix_latex_rac"
print("Calling Голые `\sqrt`, `\f fixrac`, `\le_latex_rac endpointq` оборачиваются в `$.....")
r = requests.post(url, timeout=60)
print(f"Status: {r.status_code}")
print(f.$`
5. ✅ `\"Response: {r.text[:500]}")sqrt
